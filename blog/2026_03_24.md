[[cyb]] mobile web + own wallet + security audit closed

[[Keplr]] shutdown pushed us to build what we always wanted — a native wallet. AES-256-GCM encryption, PBKDF2 1M iterations, hardware [[Ledger]] support, 15-min auto-lock. No extensions. No dependencies.

mobile web is live. all pages accessible — [[teleport]], [[studio]], [[sigma]], [[brain]]. responsive layouts, commander with multi-line input, action buttons for search and cyberlink.

31 [[security]] audits. 108 findings. 0 open.

full audit: [[security_audit_mnemonic]]

today's fixes (audit 32):

- mnemonic inputs masked by default (`type="password"`) + eye toggle (HIGH)
- [[service worker]] POST cache filtered — signing/wallet endpoints excluded (MEDIUM)
- long-press protection on secrets — user-select: none (MEDIUM)
- `-webkit-text-security: disc` CSS fallback (MEDIUM)
- clipboard auto-clear 30s after copy (LOW)
- console.log near mnemonic removed (LOW)

[[studio]] responsive on mobile — single column, keywords horizontal, particle URL param for cyberlink from [[commander]].

your keys. your node. your browser.

commit `69209b78`
