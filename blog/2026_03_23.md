mobile web push — branch `mobile-web2`, [PR](https://github.com/cyberia-to/cyb/pull/new/mobile-web2)

what landed:

- all routes unblocked on mobile — removed `UseDesktopVersionBlock` gate, every page now renders on mobile (teleport/send, teleport/swap, particles, contracts, etc.)
- [[sigma]] page responsive — wrapper goes 100% width on mobile instead of 72%, balance details no longer merge numbers (liquid/frozen/growth rows were glued together)
- [[teleport]] sub-routes (send, bridge, swap) now accessible on mobile — wildcard added to `mobileAllowedRoutes`, then the whole check was removed
- [[teleport]] layout padding 16px on mobile
- null safety fix in `useGetLog` — `lastPage.data?.pagination` prevents crash "Cannot read properties of null (reading 'pagination')"
- Commander repositioned on mobile (fixed bottom)
- ActionBar z-index adjusted so send/bridge buttons aren't hidden behind Commander
- SigmaWrapper refactored from inline styles to SCSS module

still open:

- send action button may still be obscured by Commander on some pages — needs z-index tuning or Commander hide during tx flow
- IPFS content not resolving (Qm hashes show raw CID instead of content) — gateway/pinata integration needed
- teleport pages need more mobile polish (padding, responsive inputs)
