---
tags: cyber, launch, roadmap, article
alias: launch, cyber launch, launch phases, master plan, nox master plan, nox_master_plan, bootloader reborn, 2026-11-05
crystal-type: plan
crystal-domain: cyber
status: active
---
# cyber/launch

the launch of [[cyber]] is a sequence of phases, each a network that must hold before the next one exists. the phase with a date is the first: on 2026-11-05, five years after the [[bootloader/bostrom|bostrom]] genesis, bostrom and [[bootloader/tokens/$PUSSY|pussy]] come back to life in a new body, minutes apart, as two [[soft3]] chains with the full reward mechanics. [[$CYB]] launches later, after implementations in several languages and formal verification, and the two reborn chains are the bootloader and the canary that earn it.

version: 2026.09 · status: phase 1 in delivery · this page is the single tracker; every gate, decision and status change lands here.

## the phases

| phase | what | gate | when |
|---|---|---|---|
| 0 | foundations: field, hash, commitments, VM, proofs, state | complete | done |
| 1 | the bootloader reborn: bostrom and pussy on soft3 with settlement mining, fold, foculus consensus and privacy | the property registry below is green or explicitly deferred; two chains live from the burial snapshots | 2026-11-05 |
| 2 | canary: the two chains under real use and weekly red team | 90 days, all economic invariants hold, no critical bug under attack | 2026-11-05 → 2027-02 |
| 3 | $CYB mainnet | implementations in several languages agree on 10⁶ blocks; the formal spine (P4) green; the six mainnet questions answered | after phase 2 |

a milestone is code that passes its gate. the date exists for phase 1 only, and it is a gift, so what does not fit the date is cut from phase 1, never squeezed.

## where we are · 2026.09

the stack was built bottom-up. the cryptographic floor is implemented and tested; the frontier is running the pieces together as a network. counts are rust lines and `#[test]` functions in the checkout on 2026-09-18.

| component | role | lines | tests | phase 1 use | status |
|---|---|---|---|---|---|
| [[strata]] | five algebras, [[nebu]] Goldilocks | 16.1K | 453 | every field operation | complete |
| [[hemera]] | [[Poseidon2]] hash, [[particle]] identity | 11.4K | 313 | particles, transcripts, trees | complete, unaudited |
| [[lens]] | commitment backends, 7 crates | 5.6K | 128 | Brakedown for zheng, bbg openings | evaluation binding is `TensorMerkle`; recursive form blocked |
| [[soft3/nox|nox]] | proof-native VM, 18 patterns + jets | 8.9K | 175 | the protocol's own programs | complete |
| [[zheng]] | SuperSpartan + Brakedown + HyperNova folding | 12.6K | 211 | ticket proofs, fold, decide, privacy proofs | execution proofs bind computation to result; fold + decide exist; private execution incomplete |
| [[soft3/bbg|bbg]] | authenticated state, vendored fjall | 13.1K | 216 | state, openings, file store | core complete; durable storage gates open (P0) |
| [[radio]] | QUIC transport, gossip, blobs with cyber-bao | 194K | 559 | signal gossip, file bytes by particle | multi-node tested; a dependency of nothing in phase 1 yet |
| [[foculus]] | consensus by convergence, beacon, settlement, tickets, fold, domain finality | 15.7K | 207 | cores 2, 3, 5 | single machine, never a network; `net` feature on upstream iroh |
| [[tru]] | φ*, [[tri-kernel]], impulse, truth scoring | 10.2K | 86 | the value oracle, the serum | φ*, κ < 1, impulse; serum scored in 6 tests |
| [[tok]] | coin, card, conservation, mint, lock, burn | 0.6K | 8 | mint, stake, ICBS, $ν books, referral | published; no live economy |
| [[soft3/cybergraph\|cybergraph]] | signal processor, applications, receipts | 7.5K | 97 | admission, registration of books | durable applications in review |
| [[inf]] | query engine over authenticated state, 7 crates | 80.8K | 369 | what links this file; reads for cyb | live |
| [[mudra]] | identity, legacy bridge, spell, stealth, veil | 2.9K | 49 | claims, P1–P3, valence | keys, claims, bridge; stealth and veil specified |
| [neuron](../neuron/README.md) | the subject: id, model, engine, node | 9.8K | 50 | one identity across bbg, mudra, tok, inf | local |
| [[tade]] | framing | 1.2K | 26 | the wire | 0.1.0 published |
| [[file]] | `file = (particle, data)`, kinds, sniff | 0.2K | 4 | the one particle type; render kinds | two days old; used by spark and cyb only |
| spark | opener: file → surface | 0.1K | 3 | media and PDF render | text and image |
| [[soft3]] | node and CLI | 2.8K | 28 | the chain binary | one node live, HTTP ingress, one signal one block |
| [[cyb]] | the body: worlds, networks, relay, vault | 19.2K | 76 | the client; content visible | networks, relay, beacon, spell, fleet gate |
| [vault](../vault/README.md) | custody, spell, signing | 6.7K | 46 | keys and claims in cyb | local CLI, 46 tests |
| [[rune]] | cyb's page language, 6 crates | 8.3K | 193 | memory and graph worlds | 0.1.0 |
| [[prysm]] | paint: chunks → UI | 1.6K | 0 | every rendered row | live in cyb, no tests |
| [[mir]] | render: positions → world | 7.6K | 54 | the graph world | live |
| [[soma]] | kernel and agent | 4.3K | 24 | the mind's tasks in cyb | local |
| [[glia]] | model runtime, import | 30.5K | 159 | local inference in cyb | 9 tok/s |
| [[honeycrisp]] | acpu, aruminium, unimem | 134K | 366 | glia's accelerator | live |
| nu | nushell fork, 24 crates, cyb's console | 387K | 5228 | the terminal world | a local checkout without a git remote; a reproducibility gap for the release train |
| [[fs]] | edit, patch, sovereign sync | spec | — | none; bytes move over radio | target design only |
| [[bootloader/bostrom|bostrom]] | burial scripts, snapshot, claim checker | py/js | — | genesis pipeline, claim page | complete for the old chain |
| [[cybernode]] | cyberproxy, deimos, jupiter, io | ops | — | three nodes, file store, blockstore | io overloaded |

the dependency closure above is computed from the path dependencies of the phase-1 binaries (soft3 node, cyb, cy, true-cyber, foculus, neuron, tok, tru) on 2026-09-18, plus the non-code owners. radio and fs are listed because the cores need them, not because a binary depends on them yet.

the burial is complete and is the genesis material: bostrom halted at 25,120,712 with 61,675 accounts, 46,039 passport owners, 2,949,732 cyberlinks and 3,143,650 particles rebuilt bit-exact from block events, 97.63% of particle bytes present; space-pussy with 29,112 links recovered. every dataset is pinned with a CID and a sha256 at [snapshot.bostrom.network](https://snapshot.bostrom.network).

## phase 1 · the bootloader reborn · 2026-11-05

today · 2026-09-22 · 44 days to launch. gates: cyb fleet green (21/21), foculus · tru · mudra · tok · cybergraph green, zheng red in the owner's tree unchanged (example `legacy_wire_cost` does not compile: `TraceProof: serde::Serialize` not satisfied; lib tests pass), spacepussy-test idle at height 39,641 since yesterday: nobody cast. red: 33 launch pull requests open, still 0 merged; the workers ran 42 times overnight and returned NONE 37 times because every open row is taken, then took rows 34–36 within an hour of their creation. first numbers: one worker, one thread on an M4 Max samples 673 tickets/s at an ε-support of 8 nodes, 124/s at 64, 17/s at 512, 2.1/s at 4096, at 5–6 W, so 7 mJ to 2.4 J per sample; the falloff is linear in support size, no superlinear term (foculus #16). the canonical cluster partition exists as code with the radius still a parameter, not derived from ε (foculus #15). DAS decides availability against a withheld shard locally (foculus #14). decisions still open: particle definition (row 19), privacy at genesis vs the current wire, merge policy, referral share r, missing-bytes identity, 8.97T BOOT, the bond formula.

review · 2026-09-22 · all 33 worker pull requests reviewed by four review agents, each in an isolated worktree with the tests run: 30 to merge, 2 held as drafts (mudra #3, #4 until the mudra `cyber-nox` 0.3 pin lands), 1 new (spark #1, so that file #1 cannot break spark and cyb). real defects found and fixed on the branches before the verdicts: foculus #7's beacon binding let an attacker relabel the epoch or substitute claims_root on a finished VDF (b1cf1d5 fixes the binding and the tests now recompute the digest); tru #11 clawed back past annuity accrual on a falling target (clipped at zero per §2 and §12); tru #9's minority test passed by a 2-ULP accident (rewritten as a fixed-share minority with a non-collapsing margin); plumb #4 computed the referral share through f64 (now u128 fixed point); plumb #5's Sybil proof was a tautology (replaced by a real fee pool distributed pro rata to $ν holders); plumb #1's burn panicked on an unknown holder; foculus #15 listed cluster members in input order (now canonical); foculus #14 compared f64 confidences (now an integer threshold); zheng #21 was rewritten around the corrected P1 (author leak, not content). the eleven foculus PRs now carry a byte-identical Cargo pin fix equal to the owner's #5 and merge in any order with zero conflicts; the plumb chain is prefix-closed 1→5; tru 8→11 merges clean. merge order: foculus #12 → #13 → #6 → #7 → #8 → #9 → #10 → #11 → #14 → #15 → #16 → #5 · tru #8 → #9 → #10 → #11 · zheng #20, #21 · plumb #1 → #2 → #3 → #4 → #5 · mudra #5 · file #1, #2, spark #1 · radio #5 → #4 · bbg #9 (owner) → #10, #11 · cybergraph #4, #5, #3 (owner) · cyb #1392. every row stays partial after merge: no PR provides the multi-node gate, the VDF is the sequential-squaring placeholder, banded_target and partition_into_clusters are not yet called by a live epoch.

### the four cores

these are the point of launching. none of them is cut, none of them is faked.

1. mint by Shapley and settlement mining. a neuron links, [[tru]] measures the directed impulse Δφ⁺, the surprise gate ρ prices it, and the share is settled by the lottery of [[rewards|rewards]] §7: every hash attempt is a real Shapley sample, every winning ticket carries a [[zheng]] proof, and the swarm mean converges by Hoeffding. the subsidy is the same ticket (§8).
2. fold. winning tickets aggregate per cluster into one O(1) accumulator by HyperNova folding ([[foculus]] fold-mining): self-fold, cluster tree, decide, then [[tok]] mints under conservation. without this, settlement verification is a DoS surface.
3. consensus by convergence. [[foculus]] on independent nodes: a particle is final when φ*ᵢ > τ, forks resolve by φ*, the beacon comes from a VDF over finalized signals, equivocation is rejected, and nodes converge from different starting states.
4. hybrid economics: truth markets and staking. this is the game changer, and it is what makes the mint more than a payout. two mechanisms, one program set:
   - the truth market. every cyberlink carries an [[ICBS]] position (cost $C = \lambda\sqrt{s_Y^2+s_N^2}$, self-scaling liquidity, the spam cost) and a [[Bayesian Truth Serum]] meta-report; the serum is the oracle, the market the liquidity skin over it, and [[valence]] privacy the third leg that removes the coordination channel. [[strong truthfulness]] proves the fused mechanism extracts truth even in the perpetual market with no external resolver. surprise ρ, the gate of the mint, is read from it.
   - staking on two axes ([[rewards|rewards]] §9–13). any stake moves rank: weight in $A^{\text{eff}}$, hence φ* and [[cyberank]]. only correct risk under $v_\ell \neq 0$ moves reward; passive capital buys influence and earns nothing by category. the security budget splits PoW/PoS by the allocation curve $\theta^\alpha$; base emission goes to work and risk only. operations: mint, burn (eternal particles and cyberlinks), lock (stake on particles or cyberlinks). the pulse is the instant mint; the annuity is the yield stream that pays foundational links as the graph grows around them.
5. personal chains and the referral. every [[neuron]] roots its own home book ([[cyber/research/oikos|oikos]]): one non-fungible name, one token in the neuron's name, $ν. the book is where a personal mind is born: the neuron's links, its local φ*, its obligations, settled at home and registered into the [[cybergraph]] by name and state root; [[foculus]] gives each book domain finality. without personal chains no personal brains are born, so this ships in phase 1 with the two root chains.
   the referral rides on it. registration is a cyberlink: the referrer's neuron links the newcomer's name. at the birth of ν's book a fixed share r of $ν goes to the referrer, and holding $ν is the right to a pro-rata share of the fees ν's book collects for as long as the book lives. the referrer is paid in the referee's own token, from the referee's own economy: a fake account has a book with no fees and a token worth nothing, so referring it pays nothing, which keeps the referral Sybil-resistant by construction, in the spirit of paying on focus created and never per head.
6. privacy. three invariants, all three at genesis:
   - P1 · the edge is public, the author is private. the network sees that particle p links particle q, the axon weight, and the aggregates φ* is computed from; it does not see which neuron signed, who holds the position, or who owns the energy. corrected 2026-09-22: the earlier wording, content visible only to participants, would have hidden the edges themselves and left nothing for the collective φ* to run on.
   - P2 · balances and transfers are private by [[mudra]] stealth addresses and veil.
   - P3 · settling a marginal m(n) reveals nothing of the miner's ego-net beyond public aggregates.

learning is not a seventh item: it is what the first item measures. φ* is the state of the collective mind, and a link pays because it moved it.

### cut from phase 1

- programmability for users: nox programs on chain, [[joy]] for foreign programs, [[trident]] contracts. the chain runs exactly the programs the protocol needs: φ* and impulse, settlement and fold, the ICBS market and the serum, staking with lock, burn and the yield stream, conservation and the mint. programmability for anything else returns as the first canary upgrade.
- UniversalHash and the viewing economy: out of scope; a separate network later.
- the fee role as paid inference; book-level fees on personal chains are in, because the referral pays from them. sigma, eidos, wysm, kern, soma tasks, IKP/IBC, Neptune.
- [[$CYB]]: phase 3.

### the two chains

one token, one chain ([[cyber/research/oikos|oikos]]). bostrom carries [[bootloader/tokens/$BOOT|$BOOT]], pussy carries [[bootloader/tokens/$PUSSY|$PUSSY]]. same binary, same genesis pipeline, two genesis roots, launched minutes apart on 2026-11-05. bostrom is the bootloader; pussy is the canary that takes every rehearsal first. they are the two root books; every neuron's personal book hangs off them by registration, so the same binary that runs bostrom runs a neuron's own chain.

genesis rules:

- balances come from the burial snapshot, every denomination. every account claims by signature with its old key; the [[mudra]] legacy bridge turns a secp256k1 key into a hemera neuron. 8,390 wallets that never sent a transaction are still claimable. the 8.97T BOOT held by the passport contract is a decision still open on this page.
- stake at genesis is bonded uniformly from the snapshot, as the old conditions distributed it: everybody starts bonded, and whoever wants order rebalances. the bond accounts for milliampere and millivolt held at halt, because without the resource tokens the old links carried no weight and the migrated graph would pay nothing. the exact formula is a line in the registry below.
- the graph comes home as files and cyberlinks: 2.9M links and 3.1M particles for bostrom, 29K links for pussy, replayed root matched against the snapshot. karma carries as reputation.
- the missing 2.37% of particle bytes are listed; a black hole stays a black hole until someone sparks it.

### content, visible: cyb

the old network let people link and never let them see what they linked. this is the failure phase 1 fixes, and it weighs as much as the cores: a graph nobody can read is a graph nobody links.

- availability: every particle the chain references resolves in [[cyb]] from the network, the burial blockstore or a peer; the 97.63% baseline is the floor and the missing list is public.
- render: text and images today; phase 1 adds media (video, audio) and a PDF reader, because a large share of the bootloader's linked files are PDFs. the render lives in `spark`, the opener that turns a file into a surface.
- from any link to its two files in one tap, and from any file to what links it.

### critical dependencies found on the second scan · 2026-09-18

the first draft of this page missed six dependencies. two of them decide genesis.

1. one particle. three definitions are in play today: the [[file]] crate and [[cyber/file]] say `particle = hemera(data)`, 32 bytes, no prefix; the cybergraph particle spec describes a flat 64-byte namespace; [[cyber/particle]] names bbg's construction, hemera over the [[lens]] commitment to the data, domain-separated. every particle of the migrated graph is computed by whichever wins, so the definition freezes before the genesis pipeline runs, and `file::Particle` becomes the one type bbg, cybergraph, foculus and tru share instead of four private `[u8; 32]` aliases.
2. re-addressing the bootloader graph. the 3,143,650 particles of bostrom are IPFS CIDs; the new identity is a hemera hash of the bytes. the 97.63% with bytes present are re-hashed and carry their CID as a naming cyberlink; the 2.37% without bytes have a CID and no data, so their particle cannot be computed, and the rule for them is a genesis decision: a black hole addressed by a cyberlink from the CID label, sparked if the bytes ever surface.
3. transport. [[radio]] is a dependency of nothing in the phase-1 binaries; the node accepts signals over HTTP (`/v1/link`, `/v2/frame`, `/v3/action`) and foculus carries an optional `net` feature on upstream iroh for settle gossip only. signal gossip between nodes and blob transfer of file bytes by particle (iroh-blobs with cyber-bao) both hang on wiring radio into the node and into cyb. this is on the critical path of core 3 and of lane C at once.
4. serving the burial bytes. 3,069,134 files sit on io's IPFS flatfs under CIDs, on an overloaded machine. phase 1 needs a file store that answers by hemera particle, seeded from that blockstore, replicated to the three network machines, so that cyb resolves what the graph references. storage economics stays open; availability at genesis does not.
5. file kinds for render. media and PDF enter through `file::Kind` and `sniff`, then spark opens them; the crate is two days old and only cyb uses it.
6. the subject. `neuron-id` is the identity every core crate already shares (bbg, mudra, tok, inf, soma); claims by old key land on it. it stays in, and it is the model for what `file::Particle` must become for content.

out of scope and noted: five new warriors created on 2026-09-16 (gaw for Polkadot, tolya for Solana, vitalina for Ethereum, zenda for Zcash, pearla for Pearl inference) belong to the warriors program, not to phase 1.

### lanes

| lane | owns | first gate |
|---|---|---|
| A · core | settlement, fold, foculus network, truth market, staking, personal chains and referral, privacy | one node: impulse → m(n) → ticket → zheng proof; one link with an ICBS position and a serum report scored |
| B · body | genesis pipeline, nodes on three machines, claims, releases | genesis of pussy boots from real data |
| C · content | one particle type, file store by particle, radio blobs, media and PDF render in cyb | a bostrom PDF opens in cyb from its cyberlink, fetched by particle |

lane A is the critical path and yields to nothing. lanes B and C run on the proven conveyors: the burial scripts and the coordinated release train of 2026-09-16.

### property registry

every claim the launch stands on, with the kind of evidence it has. states: proven · simulated · measured · open. an open property has a date by which it is closed or explicitly deferred to phase 2 on this page.

| # | property | owner | state | evidence | close by |
|---|---|---|---|---|---|
| 1 | φ* exists, unique, converges with κ < 1 | tru | proven | convergence.md, bostrom measurement λ₂ ≈ 0.13 | closed |
| 2 | Σφ*ᵢ = 1 | tru | proven | tri-kernel spec | closed |
| 3 | ticket proof cost < ticket reward | foculus, zheng | open | measure on real m(n) with the folded proof; foculus/audit/ticket-proof-cost.md — O(1) shape holds (decide ~18.7ms, verify ~9.2ms flat n=8..512), reward-side pricing still blocked on #26/#27; PR open: [foculus#6](https://github.com/cyberia-to/foculus/pull/6) — measured: decide/verify 9–19 ms per ticket, flat in size; spec said 10–50 µs | 2026-09-25 |
| 4 | progress-freedom across clusters: difficulty schedule corrects per-cluster cost | foculus | open | settle-target banded linearly by cluster size (`tickets::banded_target`); two-point measurement n=4 vs n=32 shows flat target ratio ~12x collapsed to ~1.5x banded (launch #4, foculus/audit/progress-freedom-banding.md); network-scale million.rs simulation still open; PR open: [foculus#10](https://github.com/cyberia-to/foculus/pull/10) — banded target by cluster size | 2026-10-09 |
| 5 | fold is a commutative monoid, decide is O(1) | zheng, foculus | open | end-to-end on three nodes; PR open: [zheng#20](https://github.com/cyberia-to/zheng/pull/20) — fold either order, both decide() | 2026-10-09 |
| 6 | beacon unpredictable and unbiasable | foculus | open | VDF over finalized signals, attack_vectors.rs; PR open: [foculus#7](https://github.com/cyberia-to/foculus/pull/7) — four VDF-binding attacks rejected | 2026-10-16 |
| 7 | withholding bias bounded by compute share | foculus | open | analytic bound derived, bias(q) ≤ (mean−min)·q/(1−q); measured on a 400-ticket pool at q=0.05–0.5, within bound and monotone (launch #7, foculus/audit/withholding-bias.md); priced forfeit and role separation still open; PR open: [foculus#11](https://github.com/cyberia-to/foculus/pull/11) — withholding bias bounded by compute share | 2026-10-16 |
| 8 | no two nodes finalize conflicting state | foculus | open | network gate with conflicts and partition; PR open: [foculus#8](https://github.com/cyberia-to/foculus/pull/8) — MinHash agrees across n-way conflicts | 2026-10-16 |
| 9 | nodes converge from different starting states | foculus, cybergraph | open | network gate; PR open: [foculus#9](https://github.com/cyberia-to/foculus/pull/9) — convergence order-independent of arrival | 2026-10-02 |
| 10 | P1 signal privacy: edge public, author private | mudra, zheng | open | commitment + validity proof, leakage bounded — audited: cybergraph's signal spec broadcasts `ℓ⃗` in plaintext with a validity proof over disclosed content, the inverse of P1; no mudra spec defines a content-commitment interface; zheng's private-execution backend has no reviewed hiding protocol (launch #10, mudra/audit/p1-signal-content-commitment-gap.md); PR open: [zheng#21](https://github.com/cyberia-to/zheng/pull/21) [mudra#3](https://github.com/cyberia-to/mudra/pull/3) — audit: wire format leaks full link content; P1 needs zheng private execution | 2026-10-16 |
| 11 | P2 stealth and veil | mudra, tok | open | stealth.md, veil.md, transfer vectors — audited: tok's ledger holds every balance plain by `(NeuronId, TokenId) → u64`, no ciphertext type or stealth column; neither stealth nor veil has code in mudra/src, and stealth's dependency crate `genies` does not exist in this checkout (launch #11, mudra/audit/p2-stealth-veil-transfer-gap.md); PR open: [mudra#4](https://github.com/cyberia-to/mudra/pull/4) — audit: stealth/veil gap vs tok ledger | 2026-10-16 |
| 12 | P3 miner ego-net hidden beyond aggregates | foculus | open | interface invariant 4, test — audited: `SettlementTicket.marginals` and `replay_marginals` publish the full per-contributor vector by design, not an aggregate; closing needs zheng private execution (launch #12, foculus/audit/p3-ego-net-leak.md); PR open: [foculus#12](https://github.com/cyberia-to/foculus/pull/12) — audit: tickets publish the full marginal vector in the clear | 2026-10-16 |
| 13 | conservation under mint and transfer | tok | open | proof at every mutation — mint, burn, transfer tested (plumb#1); lock remains; PR open: [plumb#1](https://github.com/cyberia-to/plumb/pull/1) [plumb#5](https://github.com/cyberia-to/plumb/pull/5) — transfer added; referral payout conserved | 2026-10-09 |
| 14 | genesis is bijective with the snapshot | bostrom, cybergraph | open | replayed root matches, zero loss; PR open: [cybergraph#5](https://github.com/cyberia-to/cybergraph/pull/5) — genesis replay bijection contract | 2026-10-02 |
| 15 | claim by old key binds one neuron | mudra | measured | 13 bridge tests | closed |
| 16 | every referenced particle resolves in cyb | cyb | open | availability audit, missing list — audit module implemented and tested: resolves against local store + ASCII names, reports fraction and missing list (launch #16, cyb/audit/particle-availability.md); run over the real burial vocabulary still blocked on 22/23; PR open: [file#1](https://github.com/cyberia-to/file/pull/1) [cyb#1392](https://github.com/cyberia-to/cyb/pull/1392) — PDF/video/audio sniffed; availability audit module (draft) | 2026-10-23 |
| 17 | signed signal cannot be forged or replayed | mudra, cybergraph | measured | signal codec strict, consumer audit | closed |
| 18 | durable storage survives failure and restart | bbg | open | bbg P0 gates D1–D5 — D2 archival population and checkpoint boundary specified (launch #18, bbg specs/storage.md), population code still open; PR open: [bbg#10](https://github.com/cyberia-to/bbg/pull/10) — WARM archival population and checkpoint boundary | 2026-10-09 |
| 19 | one particle definition, one `file::Particle` type across bbg, cybergraph, foculus, tru | file, bbg, cybergraph | open | decision on this page, then the type shared — five current definitions and their contradictions surveyed (launch #19, cybergraph/audit/particle-alias-survey.md); PR open: [cybergraph#4](https://github.com/cyberia-to/cybergraph/pull/4) — survey of five particle definitions with file:line | 2026-09-25 |
| 20 | every bostrom particle re-addressed under hemera with its CID kept as a naming link; missing bytes have a defined identity | bostrom, cybergraph | open | `readdress(cid, bytes)` implemented and tested: bytes present → `Particle::hash(bytes)`, missing → black hole `Particle::hash(cid)`, sparks when bytes surface (launch #20, file crate); the run over the 3,143,650-CID burial blockstore with count-matches check still open; PR open: [file#2](https://github.com/cyberia-to/file/pull/2) — re-address CIDs to hemera particles, black hole for missing bytes; needs the decision in row 19 | 2026-10-02 |
| 21 | signals gossip between nodes over radio | radio, foculus, soft3 | open | fixed a pre-existing build break (iroh-docs pulled upstream iroh-blobs/iroh-gossip instead of this workspace's fork); proved multi-hop relay — a 4-node chain where the receiver only knows its immediate neighbor still gets the broadcast (launch #21, radio); real Signal payload, "before finality", and wiring radio as soft3's actual transport still open; PR open: [radio#5](https://github.com/cyberia-to/radio/pull/5) — gossip relays multi-hop; fork pins fixed | 2026-10-02 |
| 22 | file bytes fetch by particle over radio blobs with verified streaming | radio, cyb | open | cyb opens a file it never had from a peer; PR open: [radio#4](https://github.com/cyberia-to/radio/pull/4) — verified-streaming tests for the particle CLI | 2026-10-09 |
| 23 | a file store answers by particle on the three network machines, seeded from the burial | cybernode, bbg | open | 3,069,134 files served, availability audit; PR open: [bbg#11](https://github.com/cyberia-to/bbg/pull/11) — audit: network fetch has no self-authentication check | 2026-10-16 |
| 24 | the serum is strictly proper in the meta-report and selects truth over coordinated consensus | tru | proven | strong-truthfulness.md; truth_scoring.rs 7 tests, incl. scaling to 200-agent coordinated majority (launch #24, tru/audit/coordinated-consensus-scoring.md); PR open: [tru#9](https://github.com/cyberia-to/tru/pull/9) — informed minority beats coordinated majority at scale | implementation open, 2026-10-09 |
| 25 | ICBS market: cost function, inverse coupling, spam cost, per-link positions under conservation | tok, tru | open | cost function + prices implemented and tested (tru/rs/icbs.rs, launch #25); market program on chain, honesty_loop.rs extended still open; PR open: [tru#8](https://github.com/cyberia-to/tru/pull/8) — ICBS cost function and price derivatives | 2026-10-09 |
| 26 | two axes: passive stake moves rank and earns nothing; only $v_\ell \neq 0$ risk earns | tok, tru | open | reward equation §11 implemented, test with idle and Sybil capital; PR open: [plumb#2](https://github.com/cyberia-to/plumb/pull/2) — stake-yield pays active risk, zeros passive and Sybil | 2026-10-16 |
| 27 | allocation curve $\theta^\alpha$ splits the security budget; floor derived from attack economics | tok | open | split and floor formulas implemented over Fx, simulated across α∈{0.3,0.5,0.7} and c_sec∈{1x..5x} (launch #27, tru/audit/allocation-curve.md); c_sec/r_atk PID loop still open; PR open: [tru#10](https://github.com/cyberia-to/tru/pull/10) — PoW/PoS allocation split and security floor | 2026-10-16 |
| 28 | the yield annuity pays foundational links; the discovery leak (§12) bounded or accepted | tru, foculus | open | discrete per-epoch accrual primitive implemented and tested, falsified-link non-reversal verified (launch #28, tru/audit/annuity-accrual.md); wiring ω(t)/Δφ*_j(t) to real ICBS price and focusing output still open; the leak is an accepted open frontier for phase 1; PR open: [tru#11](https://github.com/cyberia-to/tru/pull/11) — per-epoch accrual of the annuity | 2026-10-23 |
| 29 | valence privacy hides individual positions and reports; only aggregates public | mudra, zheng | open | same proof profile as P1; PR open: [mudra#5](https://github.com/cyberia-to/mudra/pull/5) — audit: valence leaks on the wire | 2026-10-16 |
| 30 | any neuron roots a home book with its own token on the same binary; registration links name and state root into the graph | soft3, tok, cybergraph | open | a neuron on pussy-rc roots a book from cyb; the book settles at home — ledger half done: deterministic book token id, one-home-book-per-neuron invariant (launch #30, tok/oikos-book.md); cybergraph naming-link registration still open; PR open: [plumb#3](https://github.com/cyberia-to/plumb/pull/3) — one home-book token per neuron | 2026-10-09 |
| 31 | domain finality per book: a book settles locally, cross-book conditions wait on evidence | foculus | open | oikos foundation 2 specified and exercised on two books; PR open: [foculus#13](https://github.com/cyberia-to/foculus/pull/13) — book-level domain finality | 2026-10-16 |
| 32 | referral: a share r of $ν at book birth goes to the referrer who linked the name; $ν holders receive the book's fees pro rata under conservation | tok, cybergraph | open | birth allocation vector done — split committed atomically under conservation, self-referral rejected, r a caller-supplied parameter pending the open r decision (launch #32, tok/referral.md); fee split and transfer vectors still open; PR open: [plumb#4](https://github.com/cyberia-to/plumb/pull/4) — referral birth-mint split | 2026-10-16 |
| 33 | referring an inactive or Sybil account yields zero | tok, tru | open | simulation with fake books; PR open: [plumb#5](https://github.com/cyberia-to/plumb/pull/5) — zero for inactive and Sybil books | 2026-10-23 |
| 34 | the public graph is served by every full node and every partial node for its namespaces, replicated by radio, sampled by DAS; a light client verifies against the tip root; the burial file store is the seed, not the only copy | foculus, bbg, radio, cybernode | open | `decide_availability` implemented and tested: a withheld (non-responding) shard counts as a failed sample exactly like a tampered one, verdict compared against a confidence threshold (launch #34, foculus/src/das.rs); this closes the local, single-process half of "DAS sampling passes against a withheld chunk" only — three-machine replication, a fourth node joining, and light-client tip-root verification are unstarted; PR open: [foculus#14](https://github.com/cyberia-to/foculus/pull/14) — DAS availability decision treats a withheld shard as a failed sample | 2026-10-16 |
| 35 | clusters are canonical: an epoch's claims partition into connected components of overlapping ε-supports, derived from the graph and ε, no miner-drawn boundary; the settlement proof commits to the support | foculus, tru | open | `epsilon_support` (bounded-radius neighborhood) and `partition_into_clusters` (union-find, canonical order) implemented and tested (launch #35, foculus/src/cluster.rs); radius not yet derived from ε, wiring into epoch.rs and the settlement-proof boundary commitment still open; PR open: [foculus#15](https://github.com/cyberia-to/foculus/pull/15) — ε-support derivation and canonical cluster partition | 2026-10-09 |
| 36 | hardware profile of a ticket measured on the bostrom graph: samples per second and joules per sample on Apple Silicon, x86 desktop, a phone and a GPU; the per-cluster difficulty schedule equalizes pay per second of work | foculus, warriors | open | `hardware_profile` binary grinds real `try_settlement_ticket` samples with a streaming `powermetrics` sampler; one of four points measured — Apple M4 Max: 672.8 samples/s at ε-support 8 down to 2.1 samples/s at ε-support 4096, 5-6.4W, near-linear in support size (launch #36, foculus/audit/hardware-profile.md); x86, phone, GPU runs still open; PR open: [foculus#16](https://github.com/cyberia-to/foculus/pull/16) — samples/s and mJ/sample vs ε-support size on Apple Silicon | 2026-10-16 |
| 37 | fixed point everywhere on the settle-mint path: tok `conservation.rs` (`fx_to_tokens`, `fx_weight`) still converts through f64 on main | tok | open | found in review; violates arithmetic.md; a one-file fix | 2026-09-30 |
| 38 | mudra builds from a clean checkout: `cyber-nox` pin 0.1.2 → 0.3 (the owner's dirty tree has it) | mudra | open | pins bumped to match local nox 0.3.0/zheng 0.4.0, `cargo check --tests` and `cargo test` green (20 tests); `--features prove` still broken on a real API break (`Statement.bbg_root`), documented in mudra/audit/nox-pin-build-gate.md as the next slice; PR open: [mudra#6](https://github.com/cyberia-to/mudra/pull/6) — bump cyber-nox/zheng pins so mudra builds from a clean checkout | 2026-09-25 |
| 39 | soft3 node genesis loading is validated: default genesis creation, chain_id/engine/protocol rejection, `deny_unknown_fields`, and the 16 KiB/64 MiB size bounds all covered by tests | soft3 | open | found in review: the module had zero test coverage though it is the only real validation logic on the node's startup path; 10 tests added, `cargo test` green (14 total); the node still has no `genesis`/`claim` subcommand to load a real burial-snapshot genesis, only the hardcoded `spacepussy-test` default — that remains a separate, larger slice; PR open: [soft3#4](https://github.com/cyberia-to/soft3/pull/4) — cover node genesis load/validate with tests | 2026-10-02 |
| 40 | HTTP submission identity derivation (`/v1/link`, `/v1/pay`, `/v1/frame`, `/v2/frame`) is covered by tests: Idempotency-Key/request_id reconciliation, length/charset bounds, hex-key passthrough vs domain-separated hash | soft3 | open | found in review: `node/requests.rs`, `routes.rs`, `http.rs` and `node.rs` had zero test coverage despite being all of the node's current HTTP ingress (the transport before radio wiring, critical dependency #3); 13 tests added for `identity()`, `cargo test` green (17 total); `routes.rs`/`http.rs`/`node.rs` and `submit()` end-to-end still untested; PR open: [soft3#5](https://github.com/cyberia-to/soft3/pull/5) — cover HTTP request identity derivation with tests | 2026-10-02 |
| 41 | HTTP query-string integer parsing (`/log?from=`, `/blocks?limit=&before=`, `/v2/history?after=&limit=`) is covered by tests: duplicate-key, invalid/empty/negative-value rejection | soft3 | open | found in review: `routes.rs`'s `parameter()` had zero test coverage; 11 tests added, `cargo test` green (15 total); `route()`/`route_ready()` dispatch and `http.rs` request parsing still untested end-to-end, need a live Node/TCP fixture; PR open: [soft3#6](https://github.com/cyberia-to/soft3/pull/6) — cover query parameter parsing with tests | 2026-10-02 |
| 42 | mudra's `prove`-feature proving pipeline (milestone 2e) builds and its proof round-trips: `open_statement()` sets nox 0.3's `bbg_root` field | mudra | open | found in review, named as the next slice in mudra#6/audit/nox-pin-build-gate.md: `bbg_root: [0u8; 32]` matches zheng's own no-state-read sentinel, `cargo test --features prove` green (44 tests, incl. `madd_proof_verifies_end_to_end`); depends on mudra#6 (row 38) merging first; PR open: [mudra#7](https://github.com/cyberia-to/mudra/pull/7) — fix bbg_root sentinel so mudra builds with --features prove | 2026-09-30 |
| 43 | fold-mining's Hoeffding `k_min`/`meets_precision` (the cluster decide-precision gate underlying property 5) computed in fixed point, not `f64` | foculus | open | found in review, arithmetic.md doctrine sweep: `f64::ln` is not guaranteed bit-identical across platforms, a real non-determinism risk on a decide path once wired in; rewritten over `tru::Fx` (Goldilocks fixed point), cross-checked against the textbook `f64` formula within ±1 at four (ε,δ) pairs, `cargo test` green (209 across all binaries); no production caller yet, so pre-emptive; PR open: [foculus#17](https://github.com/cyberia-to/foculus/pull/17) — compute fold-mining k_min in fixed point, not f64 | 2026-10-02 |
| 44 | bbg builds from a clean checkout: `rs/Cargo.toml` lens/nox/zheng pins and `cli/Cargo.toml`'s stale `cyber-tape` path (renamed to `tade` 2026-09-16, bbg's cli crate was missed in that sweep) both match local versions | bbg | open | found in review, same class as row 38: `cargo check --tests` was broken two ways before compiling a single file; both fixed, `cargo test` green in `rs` (51 + 1, one test `#[ignore]`d with the real reason inline — zheng 0.4 retired the recursive look-opening gadget it needs, `CommitError::UnsupportedRecursiveOpening`, matching the component table's "recursive form blocked" note, not a build-gate issue) and in `cli` (3); PR open: [bbg#12](https://github.com/cyberia-to/bbg/pull/12) — bump lens/nox/zheng pins and tape->tade path so bbg builds clean | 2026-09-30 |
| 45 | soft3 node genesis validates the launch chain set (`spacepussy-test`, `pussy-rc`, `pussy`, `bostrom`), not only the hardcoded default | soft3 | open | found in review, named in row 39's Remains as the genesis/claim gap: validation hard-failed on any chain_id but `spacepussy-test`, which blocks loading a real bostrom or pussy genesis candidate outright; `ALLOWED_CHAINS` maps each chain to its own `soft3/<chain_id>/v1` protocol tag so one chain's genesis cannot validate under another's; `cargo test` green (11, 7 new); producing a real bostrom/pussy genesis file from the burial snapshot and a CLI subcommand to load one remains the larger slice; PR open: [soft3#7](https://github.com/cyberia-to/soft3/pull/7) — genesis validation accepts pussy-rc/pussy/bostrom alongside spacepussy-test | 2026-10-09 |
| 46 | zheng builds from a clean checkout: `cli/Cargo.toml`'s stale `cyber-tape` path (renamed to `tade` 2026-09-16) and `rs/Cargo.toml`'s `lens`/`nox` pins both match local versions | zheng | open | found in review, same class as rows 38/44: fixing the pins surfaces a real gap, not a rename — lens 0.2's `Brakedown::open` now returns `Opening::TensorMerkle`, the sound scheme replacing lens's own documented "known-unsound residual" `Opening::Tensor`; zheng's `ccs::transcript`/`ccs::verifier_steps` only match `Tensor` and silently return zero binding steps for anything else; 10 tests `#[ignore]`d with the reason inline, `cargo test` green (133 passed, 0 failed, 10 ignored); `zheng/audit/lens-tensormerkle-binding-gap.md`; PR open: [zheng#22](https://github.com/cyberia-to/zheng/pull/22) — zheng builds from a clean checkout; TensorMerkle gap found | 2026-10-09 |
| 47 | foculus builds from a clean checkout: `Cargo.toml`'s stale `cyber-tape` path and `bbg`/`zheng` pins both match local versions | foculus | open | found in review, same class as row 46: unlike zheng, no `lens::Opening` variant is touched directly, so the whole suite is green after the pin bump — `cargo test` green (193 across lib, doctests, and the three integration suites incl. the 79s `million.rs::chaos` test); PR open: [foculus#18](https://github.com/cyberia-to/foculus/pull/18) — bump tape->tade path and bbg/zheng pins so foculus builds clean | 2026-10-09 |
| 48 | cybergraph's manifest resolves against the checkouts on disk: `bbg`/`zheng` (`[dependencies]`) and `nox`/`lens` (`[dev-dependencies]`) pins all match local versions | cybergraph | open | found in review: the `[dependencies]` half is mechanical and green (`cargo check`, library only, is clean); the `[dev-dependencies]` half cannot ship green — nox's 0.1→0.3 identity rename (`Order<N>` arena struct → `pub type Order = u32`, `OrderId`/`Tag` gone, the settled reduction/order/particle/word vocab migration) breaks all nine `tests/stack_*.rs` files and `tests/common/mod.rs`, 121 compile errors (`E0599`×67, `E0107`×32, `E0432`×7, `E0063`×4, `E0609`×2, the last two a missing `bbg_root` on `zheng::Statement`/`LookOpening` as in row 42); full breakdown in `cybergraph/audit/nox-zheng-dev-dep-drift.md`; PR open (draft, `cargo test` red): [cybergraph#6](https://github.com/cyberia-to/cybergraph/pull/6) — bump bbg/zheng/nox/lens pins; nox test migration still needed |
| 49 | the soft3 node genesis pipeline has an explicit CLI step to adopt a real genesis candidate, not only the hardcoded spacepussy-test default created on first boot | soft3 | open | found in review, named as the next slice in row 39's Remains: `soft3 genesis install --file PATH [--home DIR]` validates an external genesis file against the same chain/engine/protocol rules as node startup (`validate()` extracted from `load()`) and writes it byte-for-byte to `$home/genesis.json`, refusing a home that already has a database or a genesis file; spec clause in `specs/native-node.md`; `cargo test` green (8, 4 new); still open: widening which chains validate is row 45, and producing a real bostrom/pussy genesis file from the burial snapshot is a separate, larger slice; PR open: [soft3#8](https://github.com/cyberia-to/soft3/pull/8) — genesis install subcommand adopts an external genesis file before first boot | 2026-10-09 |
| 50 | `file::Particle::from_hex` never panics on attacker-supplied input | file | open | found by a launch worker: byte-length check alone let a 64-byte string containing a multi-byte UTF-8 character reach a fixed 2-byte `&str` slice, panicking when the slice boundary lands mid-character; hex particle parsing is reachable from untrusted input (row 40's HTTP hex-key passthrough once wired, CLI, vault); fixed by requiring `s.is_ascii()` before slicing, regression test reproduces the exact panic shape, `cargo test` green (5); PR open: [file#3](https://github.com/cyberia-to/file/pull/3) — reject non-ASCII input before slicing | 2026-09-30 |
| 52 | true-cyber (the `cyber` binary, a phase-1 release-train build) builds from a clean checkout | true-cyber | open | found in review: `main`'s uncommitted-nowhere-else WIP commit b58589f (2026-09-16) references `cyb_core::{GraphSession, native_mirror, robot}`, none of which exist in `cyb-core` 0.1.0 or anywhere in the `cyb` repo's history; `cargo check --tests` red with 6 compile errors across `src/main.rs`, `src/http.rs`, `tests/native_client.rs` (true-cyber/audit/clean-checkout-build-gap.md); last known-green commit `6cd1d8e`; fix is either implementing the missing cyb-core API or reverting the WIP commit, both out of this audit's scope; row 175 takes the revert path; PR open (draft, red): [true-cyber#1](https://github.com/cyberia-to/true-cyber/pull/1) — audit: true-cyber does not build from a clean checkout | 2026-10-09 |
| 175 | true-cyber builds from a clean checkout again, by taking row 52's own named fast path | true-cyber | open | found by a launch worker, lane B, direct follow-up to row 52's own Remains ("fix is either implementing the missing cyb-core API or reverting the WIP commit ... the audit recommends the revert as the fast path if the rewrite doesn't land before the 2026-10-23 feature freeze"): six days later, `cyb-core` still exports only `cell`/`chroma`/`intent`/`money`/`sense`/`signal` (confirmed again), no `GraphSession`/`native_mirror`/`robot`, and `true-cyber` — one of the six phase-1 release-train binaries — was still silently red on every checkout; took the named fast path: `git revert --no-edit b58589f`, a new commit restoring the last-known-green shape at `6cd1d8e`, history intact, trivially undoable; `cargo check --tests` clean (was 3 compile errors), `cargo test` green (0 tests, this binary's own pre-existing baseline, unchanged); PR open: [true-cyber#2](https://github.com/cyberia-to/true-cyber/pull/2) — revert the broken cyb-core GraphSession WIP rewrite | 2026-10-09 |
| 53 | soft3 node.rs's `key32`/`hash32` identity derivation (the hex/label convention every caller-supplied particle key resolves through) is covered by tests | soft3 | open | found in review, same class as rows 39–41: zero test coverage despite `key32` carrying its own correctness-critical ASCII-hexdigit guard comment; 8 tests added (hex passthrough, 0x-strip, left-pad, case-insensitivity, non-hex/over-long/empty fallback), `cargo test` green (12); PR open: [soft3#9](https://github.com/cyberia-to/soft3/pull/9) — cover key32/hash32 with tests | 2026-10-02 |
| 54 | soft3 node/http.rs's `read_request` (the node's HTTP ingress, critical dependency #3) is covered by tests | soft3 | open | found in review, same class as rows 39–41: zero test coverage on the request-line/header/body parser, testable directly against `&[u8]` with no TCP fixture despite rows 40/41 naming that as the blocker; 16 tests — happy path, every 400 branch, the 413 and header-size limits; `cargo test` green (20); PR open: [soft3#10](https://github.com/cyberia-to/soft3/pull/10) — cover read_request with tests | 2026-10-02 |
| 55 | radio's `iroh/bench` workspace member builds from a clean checkout | radio | open | found by a launch worker, same class as rows 38/44/46/47: the crate rename to cyber-radio (aliased `iroh`) left `iroh/bench`'s own source on the stale `radio::` extern path at 8 call sites (`iroh.rs`, `lib.rs`, `bin/bulk.rs`), masking a second bug underneath — its `quinn` dependency pointed at the real crates.io `iroh-quinn 0.16.1` instead of this workspace's vendored `quinn/quinn` fork, two versions of `AckFrequencyConfig`/`PublicKey` in one binary; both fixed, `cargo check -p iroh-bench --tests` and `cargo test -p iroh-bench` green (0 tests, a bench harness); `iroh-docs` still fails `cargo check --workspace` on the same class of bug, unrelated crate, already owned by row 21; PR open: [radio#6](https://github.com/cyberia-to/radio/pull/6) — iroh-bench builds against this workspace's iroh/quinn fork | 2026-09-25 |
| 56 | mudra `claim::from_hex` (the migration-claim wire decoder) never panics on attacker-supplied input | mudra | open | found by a launch worker: same bug class as row 50 — the odd-length guard let a multi-byte UTF-8 character reach a `&s[i..i+2]` slice by raw byte index, panicking when the boundary lands mid-character; the decoder is `Claim::decode`, the parser for the wire line row 15's claim-by-old-key mechanism produces; fixed by requiring `s.is_ascii()` before slicing; regression test reproduces the exact panic shape on the pre-fix code, `cargo test` green (19; workspace-wide nox/zheng pin bump used locally only for isolation, not committed — depends on mudra#6/row 38 for a clean-checkout green run); PR open: [mudra#8](https://github.com/cyberia-to/mudra/pull/8) — reject non-ASCII input before slicing | 2026-10-02 |
| 57 | soft3 `Node::open`'s legacy-migration guard (log present, no bbg database yet) is covered by tests | soft3 | open | found in review, same class as rows 39–41/53/54: the module doc comment names this guard the exact invariant a three-machine rollout depends on to fail loud instead of silently booting an empty graph next to an unimported flat-file log, and it had zero coverage; 3 tests added — the guard firing, the guard correctly not firing once a `bbg` dir exists, a fresh home with neither opening cleanly end to end; `cargo test` green (7, 3 new); PR open: [soft3#11](https://github.com/cyberia-to/soft3/pull/11) — cover Node::open's legacy-migration guard | 2026-10-02 |
| 58 | foculus `settle_net::from_hex` (the settle-gossip radio daemon's `--topic` parser) never panics on attacker-supplied input | foculus | open | found by a launch worker: same bug class as rows 50/56 — the odd-length guard let a multi-byte UTF-8 character reach a `&s[i..i+2]` slice by raw byte index, panicking when the boundary lands mid-character; reached via `parse_topic`, the `SettleNet` daemon's `--topic` clap parser (rows 4/6-9/21's settle-gossip path once wired); fixed by requiring `s.is_ascii()` before slicing; the fix's own test could not be run green — the `net` feature (gating this whole module) fails to compile independent of this change, on a real `ed25519-dalek 3.0.0-pre.1`/iroh type error inside that vendored crate, not investigated further; default-feature `cargo test --lib` green (144, pins bumped locally for isolation only, row 47/foculus#18 not yet merged); PR open (draft): [foculus#19](https://github.com/cyberia-to/foculus/pull/19) — reject non-ASCII hex before slicing; net-feature build separately broken | 2026-10-02 |
| 59 | radio `particle` CLI's `hex_to_bytes` (the decode/verify root-hash parser) never panics on attacker-supplied input | radio | open | found by a launch worker: same bug class as rows 50/56/58 - the even-length guard let a multi-byte UTF-8 character reach a `&hex[i..i+2]` slice by raw byte index, panicking when the boundary lands mid-character; reached via `particle decode`/`particle verify`'s hash argument, the CLI phase-1 leans on for BAO verified-streaming particle checks (row 22); fixed by requiring `s.is_ascii()` before slicing, regression test reproduces the exact panic shape, `cargo test -p particle` green (4, all new - this binary had zero prior test coverage); PR open: [radio#7](https://github.com/cyberia-to/radio/pull/7) - reject non-ASCII input before slicing | 2026-10-02 |
| 60 | radio-cli's `hex_to_bytes` (a separate crate's CLI hash-argument parser) never panics on attacker-supplied input | radio | open | found by a launch worker, same class as row 59 in the same repo, different crate: fixed by requiring `s.is_ascii()` before slicing, `hex_to_bytes_tests` added - this file had zero prior test coverage - `cargo test -p radio-cli` green (4, all new); PR open: [radio#8](https://github.com/cyberia-to/radio/pull/8) - reject non-ASCII input before slicing | 2026-10-02 |
| 61 | cyb's local particle-hex parsers (`content.rs`'s `particles.jsonl` reader, memory world's `unhex32`) never panic on attacker-supplied or corrupted input | cyb | open | found by a launch worker, same bug class as rows 50/56/58/59/60/62: fixed byte-index slicing was guarded only by `len() != 64`, not `is_ascii()`; `unhex32` is reached live from a tapped memory-row's `target_ref` (`memory/mod.rs:497`); content.rs's inline check extracted into a standalone `parse_particle_hex`; fixed by requiring `is_ascii()` before slicing, regression tests reproduce the exact panic shape with a real 64-byte/32-char string (`"a" + '中' + 60 ASCII bytes`), `cargo test -p cyb --lib -- content:: memory::` green (6, all new), `make fleet` green (21 ok); PR open: [cyb#1393](https://github.com/cyberia-to/cyb/pull/1393) — reject non-ASCII input before slicing | 2026-10-06 |
| 62 | lens cli's `hex_to_bytes` (the field/digest/root hex decoder used across the `lens` CLI) never panics on attacker-supplied input | lens | open | found by a launch worker: same bug class as rows 50/56/58/59/60 — the odd-length guard let a multi-byte UTF-8 character reach a `&s[i..i+2]` slice by raw byte index, panicking when the boundary lands mid-character; fixed by requiring `s.is_ascii()` before slicing, regression test reproduces the exact panic shape, `cargo test -p lens-cli` green (10, 2 new — this file had zero prior test coverage); PR open: [lens#7](https://github.com/cyberia-to/lens/pull/7) — reject non-ASCII input before slicing | 2026-10-06 |
| 63 | hemera cli's `parse_hex_fixed` (the hash decoder backing `parse_hash` and every hex hash argument on `hemera verify`/`decode`/`sparse verify`) never panics on attacker-supplied input | hemera | open | found by a launch worker: same bug class as rows 50/56/58/59/60/62, but with a fixed-length guard (`hex.len() != N*2`) instead of an odd-length one — still bypassed by a multi-byte UTF-8 character that keeps total byte length unchanged while landing a `&hex[i*2..i*2+2]` slice mid-character; standalone repro confirmed the pre-fix panic (`end byte index 62 is not a char boundary; it is inside 'é'`); fixed by requiring `hex.is_ascii()` before the length check, `cargo test -p hemera-cli` green (2, both new — this binary had zero prior test coverage); PR open: [hemera#3](https://github.com/cyberia-to/hemera/pull/3) — reject non-ASCII input before slicing | 2026-10-06 |
| 64 | strata nebu-cli's `decode_hex` (the hex-argument decoder behind the `nebu` benchmark CLI) never panics on attacker-supplied input | strata | open | found by a launch worker: same bug class as rows 50/56/58/59/60/62/63, reported via `eprintln!`+`process::exit` rather than `Result`, so the fallible core was extracted into `try_decode_hex` to make it testable without exiting the test process; fixed by requiring `s.is_ascii()` before the length check, regression test reproduces the exact panic shape, `cargo test -p nebu-cli` green (2, both new — this binary had zero prior test coverage); PR open: [strata#4](https://github.com/cyberia-to/strata/pull/4) — reject non-ASCII input before slicing | 2026-10-06 |
| 65 | glia's `hex_decode` (decodes a `.model` file's hex-encoded graph section, `run/format.rs`) never panics on attacker-supplied input | glia | open | found by a launch worker: same bug class as rows 50/56/58/59/60/61/62/63/64, and genuinely reachable from foreign file content, not a self-generated string — checked even byte length but not `is_ascii()` before `&s[2*i..2*i+2]`; standalone repro confirmed the pre-fix panic (`end byte index 2 is not a char boundary; it is inside '中'`); fixed by requiring `is_ascii()` before the length check, `cargo test -p run --lib -- hex_decode` green (3, all new); two pre-existing unrelated `TransformerConfig` integration-test breaks noted, not caused by this change; PR open: [glia#2](https://github.com/cyberia-to/glia/pull/2) — reject non-ASCII input before slicing | 2026-10-06 |
| 66 | `crates/cyb`'s wire `hex32` (the repl's `follow <64-hex neuron>` command parser) never panics on attacker-supplied input | cyb | open | found by a launch worker: same bug class as rows 50/56/58/59/60/61/62/63/64/65 — checked exact byte length but not `is_ascii()` before `&s[i*2..i*2+2]`; `crates/cyb` is the standalone `cy` wire crate (follow/antenna/socket as cyberlinks, published separately to crates.io, excluded from the main workspace, last touched 2026-09-01, not legacy); fixed by requiring `is_ascii()` before the length check, `cargo test --lib -- hex32` green (3, all new) and the full suite green (27), `make fleet` green (21 ok); PR open: [cyb#1394](https://github.com/cyberia-to/cyb/pull/1394) — reject non-ASCII input before slicing | 2026-10-06 |
| 67 | lens cli's `hex_number_le` (`cli/src/field.rs`, the hex-literal decoder behind `F2_128::parse` and `Fq::parse`) never panics on attacker-supplied input | lens | open | found by a launch worker: same bug class as rows 50/56/58/59/60/61/62/63/64/65/66, in a second decoder `lens#7` (row 62) did not touch — the odd-length padding guard let a multi-byte UTF-8 character keep the padded byte length even while landing a `&padded[i..i+2]` slice mid-character; standalone repro confirmed the pre-fix panic (`end byte index 2 is not a char boundary; it is inside 'é'`); fixed by requiring `s.is_ascii()` before the length/slicing logic, `cargo test -p lens-cli` green (10, 2 new); PR open: [lens#8](https://github.com/cyberia-to/lens/pull/8) — reject non-ASCII input before slicing | 2026-10-06 |
| 68 | foculus `vdisk::hex_to_hash` (the shard-hash decoder used by `get_file` and disk rebalancing) never panics on attacker-supplied input | foculus | open | found by a launch worker: same bug class as rows 50/56/58/59/60/61/62/63/64/65/66/67 — a fixed-length guard (`hex.len() != 64`, byte length) does not catch a multi-byte UTF-8 character landing a `&hex[i*2..i*2+2]` slice boundary mid-character; `shard_hashes` are self-generated via `Hash::to_hex()` today, so not yet network-reachable, but sit on the registry row 23/bbg#11 plans to make network-authoritative; standalone repro confirmed the pre-fix panic; fixed by requiring `hex.is_ascii()` alongside the length check; this repo's default checkout is independently red on the stale `cyber-tape` path/pins (row 47/foculus#18) — bumped those three lines locally only to verify, then reverted before committing, so this PR touches only `src/vdisk.rs`, `cargo test --lib` green (145, 1 new); PR open: [foculus#20](https://github.com/cyberia-to/foculus/pull/20) — reject non-ASCII input before slicing | 2026-10-06 |
| 69 | `cy`'s REPL command parser (`split_args`, the quote-aware tokenizer every typed line goes through before dispatch) is covered by tests | cyb | open | found by a launch worker, same class as soft3 rows 39–41/53/54/57: `cli/src/main.rs` (the live `cy` binary's REPL, distinct from row 66's separately-published `crates/cyb` wire crate) is 1089 lines with zero test coverage; surveyed the established hex-panic bug class (rows 50/56/58–68) across the remaining un-swept repos first — bbg, cybergraph, neuron and soft3's own `parse_id`/`key32` all already guard with `is_ascii()`/`is_ascii_hexdigit()` before slicing, so no new instance of that bug turned up; 10 tests added for `split_args`/`is_builtin` — quote grouping, a quote type held literal inside the other, an empty quoted token, unterminated quotes, non-ASCII content, the builtin-verb set — no bug found in `split_args` itself, so this is a pure coverage slice; `cargo test` (cli) green (10, all new), `cargo check --tests` clean, `make fleet` green (21 ok) on commit; PR open: [cyb#1395](https://github.com/cyberia-to/cyb/pull/1395) — cover split_args/is_builtin in the cy REPL parser | 2026-10-06 |
| 70 | soft3 CLI's `--network`/`-n` flag extraction (`bin/soft3.rs`, the same binary that runs bostrom, pussy and every personal book) is covered by tests | soft3 | open | found by a launch worker, same class as rows 39–41/53/54/57 (this repo's `node/` module) but in the CLI entry point instead: the argv split lived inline in an untestable `fn main` with zero coverage; extracted to a pure `extract_network_flags`, preserving exact behavior (every flag validated in argv order via the already-tested `parse_net`/`Network::parse`, last one wins, an empty string on a dangling flag); 5 tests added — no flag, `--network` and `-n` forms, a dangling flag, repeated flags in order; `cargo test --bin soft3` green (5, all new), `cargo check --tests` and full `cargo test` clean (9 total); PR open: [soft3#12](https://github.com/cyberia-to/soft3/pull/12) — cover --network/-n flag extraction in the soft3 CLI | 2026-10-06 |
| 71 | bbg-cli's `dim_from`, `h32` and `height_key` (the operator's only hand-tool over bbg state) are covered by tests | bbg | open | found by a launch worker, same class as rows 39–41/53/54/57: zero test coverage on three pure parsers; `h32` already guards with `.chars().all(is_ascii_hexdigit())` before slicing, so it does not have the non-ASCII slice-panic bug rows 50/56/58–68 found elsewhere — confirmed with a dedicated non-panicking regression test rather than assumed; 11 tests added; this repo's default checkout does not build clean independent of this change (`cli/Cargo.toml`'s stale `cyber-tape` path, `rs/Cargo.toml`'s stale `lens`/`nox`/`zheng` pins — exactly row 44/bbg#12's gap), so the pins were bumped locally only to run `cargo test`, confirmed green (11 new + 3 pre-existing `frame::tests`), then reverted before committing — this PR touches only `cli/src/main.rs` and depends on bbg#12 (row 44) merging first for a clean-checkout CI run; `dim_from` has no `"balances"` arm though `bbg::Dim::Balances` exists — flagged, not changed, may be deliberate (balances are private state, not one of the 9 public NMT dimensions); PR open: [bbg#13](https://github.com/cyberia-to/bbg/pull/13) — cover dim_from, h32 and height_key in bbg-cli | 2026-10-06 |
| 72 | the CSR sparse-matrix primitive behind φ*/tri-kernel (`tru::focusing::csr`) is covered by direct unit tests, not only indirect focusing-level coverage | tru | open | found by a launch worker: `CsrBuilder::build`/`CsrMatrix::{spmv,spmv_add}` had zero direct tests despite docstring claims of dedup-summing and insertion-order independence; 9 tests added — dedup, insertion-order independence, row-boundary layout, `spmv` overwrite vs `spmv_add` accumulate, multi-column row dot product; `cargo test -p cyber-tru --lib` green (93, 9 new); `CsrBuilder::add`'s bounds check is a `debug_assert!` only, compiled out in release, flagged not fixed — no production caller reaches it yet; PR open: [tru#12](https://github.com/cyberia-to/tru/pull/12) — cover CsrMatrix/CsrBuilder with direct unit tests | 2026-09-30 |
| 73 | radio-cli's blob store persists a blob across the separate `add`/`get`/`list` processes a person or `cyb`/spark integration actually invokes, not only within one process | radio | open | found by a launch worker while surveying lane C: `radio blob add/get/list` each opened a fresh `iroh_blobs::store::mem::MemStore::new()`, so `add` in one process left nothing for `list` or a later `get` to find — `list` was structurally guaranteed to print `(no blobs stored)` even right after an `add`; fixed with a `--dir` flag opening one on-disk `FsStore` shared across the three subcommands; regression test opens a second store handle over the same directory (the same shape as two CLI invocations) and asserts the blob and its tag are visible, `cargo test -p radio-cli` green (1 new); PR open: [radio#9](https://github.com/cyberia-to/radio/pull/9) — persist radio-cli's blob store across invocations | 2026-10-02 |
| 74 | `tru::graph::record`'s `Cyberlink::decode` and `CyberlinkIter` (the fixed 128-byte reader the burial graph mmap — 2.9M bostrom links, 29K pussy links — is read through, row 106) are covered by direct unit tests, not only one happy-path smoke test | tru | open | found by a launch worker: only `rs/tests/smoke.rs` touched this path, one record, exact-length file; 7 tests added — every field at its documented offset, `valence` round-trips the full signed `i8` range (the doc comment promises only -1/0/+1), padding bytes `[125..128]` provably unread, empty input, a short trailing chunk dropped instead of panicking, an exact multiple yields every record in order, `ExactSizeIterator::len()` tracks correctly as the iterator advances; `cargo test -p cyber-tru --lib` green (91, 7 new); sibling gap flagged not fixed: `graph::frontmatter::index_sections` computes `start + sz as usize` from an attacker/file-supplied `size` with no overflow check and has no direct tests of its own; PR open (retitled from #73 to avoid a row collision with radio#9): [tru#13](https://github.com/cyberia-to/tru/pull/13) — cover Cyberlink::decode and CyberlinkIter with direct unit tests | 2026-09-30 |
| 75 | bbg's `verify_particle` light-client query-proof verifier binds its proof to the claimed root and the claimed particle, not just to its own internal Brakedown consistency | bbg | open | found by a launch worker: `proof::verify_particle(proof, _root, _particle)` discards both arguments — it only re-checks the Brakedown opening's internal consistency, never that `proof.commitment` is bound into `_root` or that the opened entry's key is `_particle`; `rs/tests/serde_wire.rs:50` already calls it with an all-zero dummy root/particle and gets `true` by accident; a direct regression added (`verify_particle_accepts_wrong_root_and_wrong_particle`), `cargo test` green (52, lib suite; pins locally bumped for isolation only per row 44, reverted before commit); closing needs a structural extension to `QueryProof` (dimension tag + root-inclusion proof), not a small fix — not yet reachable from production code, only from light clients once wired (rows 16, 34); `query::verify_opening` not yet reviewed for the same shape (launch #75, bbg/audit/verify-particle-unbound.md); PR open (retitled from #72 to avoid a row collision with tru#12): [bbg#14](https://github.com/cyberia-to/bbg/pull/14) — audit: verify_particle ignores its root and particle arguments | 2026-10-16 |
| 76 | `tru::graph::frontmatter::index_sections` (the `.cyb`-container section indexer every `.graph`/`.vocab`/`.model` reader shares, directly in front of row 74's cyberlink decoder) rejects an overflowing section size instead of panicking or wrapping it into a bogus range | tru | open | found by a launch worker: `start + sz as usize` was computed from a file-supplied `FileEntry.size: Option<u64>` with no overflow check — a size near `u64::MAX` panics in a debug build and wraps silently to a small `end` in a release build, which can then pass the `end > bytes.len()` bounds check with an attacker-chosen range instead of being rejected; fixed with `start.checked_add(sz as usize)`, erroring instead; the module had zero direct tests before this, 7 added — `split`'s delimiter-found/no-delimiter/non-utf8 paths, `index_sections`'s sized+unsized happy path, header mismatch, size past EOF, and the overflow regression itself; `cargo test -p cyber-tru --lib` green (91, 7 new); PR open (retitled from #75 to avoid a row collision with bbg#14): [tru#14](https://github.com/cyberia-to/tru/pull/14) — reject overflowing section sizes in frontmatter index_sections | 2026-09-30 |
| 77 | `tru::focusing::csr::CsrBuilder::add` (the bounds check in front of φ*/tri-kernel's CSR sparse matrix, directly behind row 72's coverage) enforces its row/col check in release builds, not only debug | tru | open | found by a launch worker, flagged but not fixed in row 72's audit: `debug_assert!` compiles out in release, so an out-of-range col was accepted silently at insertion and could later read the wrong slot in `spmv` instead of panicking near the bug, corrupting the tri-kernel result with no error; promoted to `assert!` with a message naming the offending index and matrix size; 2 regression tests added, one confirmed panicking in `--release`, `cargo test` green (86 lib, 88 total); no production caller reaches this with untrusted indices yet, so this closes the gap pre-emptively; PR open: [tru#15](https://github.com/cyberia-to/tru/pull/15) — enforce CsrBuilder bounds check in release builds | 2026-09-30 |
| 78 | rune (cyb's page language, feeding memory and graph worlds) builds from a clean checkout; `rune-prysm::noun_to_chunk` — the seam every rune-rendered row in cyb decodes through — has a test for every tag arm and every malformed-noun path | rune | open | found by a launch worker while surveying lane C: `rune` had received zero launch PRs; same class as rows 38/44/46/47 — the crate rename to `tade` (2026-09-16) left the root `Cargo.toml`'s `[workspace.dependencies]` and `rs/prysm/Cargo.toml` on the stale `cyber-tape`/`../tape/impl/rust` path, so `cargo check` could not load the workspace manifest on a fresh clone and neither `cli` nor `rs/prysm` ever built; fixed the two path/package lines and the four `tape::`/`Vec<tape::Chunk>` references that followed (`rs/prysm/lib.rs`, `rs/ast/lib.rs` doc comment, `cli/render.rs`, `cli/repl.rs`); once green, `noun_to_chunk` had tests for only 3 of 5 tag arms (ANNO, LOG untested) and none of its four `None`-returning malformed-noun paths, and `noun_to_chunks`'s own doc-claimed single-element and unrecognised-noun behaviors were untested — 8 tests added, no behavior changed; `cargo check --workspace --tests` and `cargo test --workspace` green (190 tests, 0 failed); PR open: [rune#1](https://github.com/cyberia-to/rune/pull/1) — rune builds clean; prysm chunk-noun decode fully covered | 2026-09-30 |
| 80 | `zheng::phi::spmv::SparseGraph::add` (the adjacency-graph builder behind the CCS instance a settlement ticket's tri-kernel recompute is proved against) enforces its row/col check in release builds, not only debug | zheng | open | found by a launch worker, same class and same run as row 77 (tru's `CsrBuilder::add`, identical shape): `debug_assert!` compiles out in release, so an out-of-range row/col was accepted silently at insertion instead of failing loudly at the point of the bug; promoted to `assert!` with a message naming the offending index and graph size; 2 regression tests added, one confirmed panicking in `--release`; `cargo test -p zheng --lib -- phi::spmv` green (6, 2 new); full-suite verification needs row 46/zheng#22's pin bump (not yet merged) — with that applied locally for isolation only and reverted before commit, 135 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap zheng#22 already tracks, none in `phi::spmv`, none new; no production caller reaches this with untrusted indices yet, so this closes the gap pre-emptively; PR open: [zheng#23](https://github.com/cyberia-to/zheng/pull/23) — enforce SparseGraph bounds check in release builds | 2026-09-30 |
| 81 | `mudra::proof::field::normalize` (the secp256k1 base-field limb carry-propagation the legacy-key claim bridge's field emulation, row 15, is built on) enforces its final-carry invariant in release builds, not only debug | mudra | open | found by a launch worker, same class and same run as rows 77 and 80: `debug_assert_eq!` compiles out in release, so a genuine violation of `Fe::add`/`Fe::sub`'s "no final carry" promise silently truncated the field element to a wrong value instead of failing at the point of the broken invariant; promoted to `assert_eq!`, 1 regression test added forcing a real unrepresentable carry, confirmed panicking in `--release`; `cargo test --lib --features prove -- field::` green (7, 1 new); full-suite verification needs row 38/mudra#6's pin bump and row 42/mudra#7's `bbg_root` sentinel fix (neither merged) — with both applied locally for isolation only and reverted before commit, `cargo test --lib --features prove` green (45) and default-feature `cargo test` green (2); the existing 4000-iteration randomized differential test already exercises `normalize` under normal operation without tripping the assert, so this closes the release/debug enforcement gap pre-emptively; PR open: [mudra#9](https://github.com/cyberia-to/mudra/pull/9) — enforce normalize's carry invariant in release builds | 2026-09-30 |
| 83 | `rune-lex::Lexer::tokenize` (the tokenizer both the classic and pure registers share, feeding rune-parse/rune-parse-pure and everything cyb renders through rune) never drops or panics on a character adjacent to a comment | rune | open | found by a launch worker surveying lane C: `tokenize`'s fallback `pos += 1` ran on every `None` from `next_token`, not only the genuinely-unknown-char case — it silently ate the newline right after a `::` line comment (no `Token::Newline` ever produced, though the crate's own doc comment calls newlines significant in the pure register's tall form) and the first character right after a `/* */` block comment (`/* c */fn` tokenized as `Ident("n")`); for a real unknown character it advanced by one byte, not one char, landing mid-character and panicking on the next slice for any non-ASCII content (same bug class as rows 50/56/58–68, found in the lexer instead of a hex decoder); fixed by having the unknown-char arm advance past the character itself and dropping the redundant fallback increment; 25-test module added (zero direct tests existed before), regressions reproduce both the pre-fix comment bugs and the multi-byte panic; `cargo test -p rune-lex` green (25, all new), `cargo test --workspace` green, no regressions; PR open: [rune#2](https://github.com/cyberia-to/rune/pull/2) — fix rune-lex double-skip bug; cover tokenizer with direct tests | 2026-10-06 |
| 84 | glia's `run` crate builds from a clean checkout (same class as rows 38/44/46/47/78) | glia | open | flagged, not fixed, in row 65/glia#2's audit as a pre-existing unrelated break: `run/tests/tier4_ir_exec.rs`/`tier5_graph_ir.rs` build `TransformerConfig` literals predating the `has_attn_bias` field, `E0063` on a clean checkout; tier5's synthetic fixture gets `false` matching the type's own `Default`, tier4 plumbs the real loaded model's `config.has_attn_bias` through exactly as it already does for `has_qk_norm`; `cargo check -p run --tests` clean, `cargo test -p run` green (full crate, 0 failed); PR open: [glia#3](https://github.com/cyberia-to/glia/pull/3) — fix TransformerConfig clean-checkout test build break | 2026-09-25 |
| 87 | `bbg::TieredStore::fetch_content` rejects network-tier content that doesn't hash to the requested particle | bbg | open | lands the code half of row 23/bbg#11's documentation-only audit: fetched bytes are hashed with `hemera::hash` (already used elsewhere in the module) and a mismatch rejected as unreachable, no `NetworkStore` trait change; `MockNetworkStore` + 3 tests added (accept matching, reject tampered, `None` with no network tier); `cargo test --lib storage::tiered` green (12, 3 new), full-crate `cargo test --lib` green (54); default checkout still can't build independent of row 44/bbg#12's lens 0.1.3→0.2.0 pin bump (not yet merged) — applied locally only to verify, reverted before commit, this PR touches only `rs/src/storage/tiered.rs`; retitled from #86 to avoid a row collision with zheng#24; PR open: [bbg#15](https://github.com/cyberia-to/bbg/pull/15) — fetch_content verifies fetched bytes hash to the particle | 2026-10-16 |
| 94 | inf's fixed rule `PageRank(edges, theta, iters)` validates its query-supplied `theta`/`iters` against the bounds specs/algorithms.md documents, instead of feeding them unchecked into fixed-point arithmetic and an iteration loop | inf | open | found by a launch worker, first launch PR on inf (0 prior): `theta` is read straight from a query-term literal (`fixed.rs::run`, `param("theta").unwrap_or(85)`) and multiplied into `theta * PR_SCALE`/`theta * rank[u]` with no range check, though the spec documents it as "damping as an integer percent" — an out-of-range theta overflows `i64` (confirmed by a discarded repro: `theta = 999_999_999_999_999` panics `attempt to multiply with overflow` at `fixed.rs:348` in a debug/test build; wraps silently to a nonsense rank distribution in release); `iters` drove an unbounded loop with no static cap at all, violating trident constraint 1's "fixed iteration count, never data-dependent unbounded" and the spec's "cost is static" promise — a real hang/DoS surface; fixed by validating `theta_pct ∈ 0..=100` and `iters ∈ 0..=MAX_PR_ITERS` (10,000, a conservative constant, not yet derived from the committed `diameter_bound` cost model) before any arithmetic runs, returning `Err` instead; 2 tests added, `cargo test -p inf-eval` green (26, 2 new), full `cargo test --workspace` green; retitled from #88 to avoid a row collision with soft3#13; PR open: [inf#1](https://github.com/cyberia-to/inf/pull/1) — bound PageRank theta and iters against query input | 2026-10-06 |
| 95 | inf's fixed rules `LabelPropagation`, `RandomWalk` and `KShortestPathYen` validate their query-supplied `iters`/`steps`/`times`/`k` loop counts, same class as row 94 | inf | open | found by a launch worker auditing the rest of `fixed_more.rs` for row 94's pattern: `for _ in 0..iters.max(0)` (LabelPropagation), nested `for _ in 0..times.max(0) { for _ in 0..steps.max(0) { ... } }` (RandomWalk), and `while accepted.len() < k` (KShortestPathYen, `k` floored at 1 but never capped above) all drive a loop from an unbounded query-supplied `i64`, violating the same trident-constraint-1 "fixed iteration count" promise; fixed with a shared `bounded(name, v)` check at the `fixed_more::run` dispatch boundary, `0..=MAX_ITERS` (10,000), before any of the three algorithms run; 1 test added covering all four parameters, `cargo test -p inf-eval` green (25, 1 new), full `cargo test --workspace` green; independent of row 94/inf#1, merges in either order; PR open: [inf#2](https://github.com/cyberia-to/inf/pull/2) — bound LabelPropagation/RandomWalk/Yen loop counts | 2026-10-06 |
| 88 | soft3's `network::probe` status-JSON parsing (`enrich_from_status_json`, `json_u64` — the node-to-node health check lane B's three-machine rollout reads chain_id/height/sync-state through) is covered by direct unit tests | soft3 | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70 in this repo: the module already had a `Network::parse` test suite but zero tests on its JSON-parsing internals; 10 tests added — full status payload, engine-vs-protocol precedence, missing `result` wrapper, empty object, malformed JSON (no panic); `cargo test --lib` green (13, 10 new); `probe()`'s own HTTP round-trip stays untested — `Network::rpc()` is a hardcoded product URL with no injectable base for a local test fixture; PR open: [soft3#13](https://github.com/cyberia-to/soft3/pull/13) — cover network probe status-JSON parsing with tests | 2026-10-06 |
| 93 | zheng's `SumcheckProver::final_claim`/`OuterSumcheckProver::matrix_evals` (sumcheck bookkeeping behind ticket proofs and fold, rows 5/85) enforce their post-fold length invariant in release builds, not only debug | zheng | open | found by a launch worker, same file and class as row 85/zheng#25 which fixed this file's two constructors but left these two post-condition checks; called early (before folding completes) each silently returned an unfolded table's first element instead of failing, in release; promoted both to `assert!`/`assert_eq!`, 2 regression tests added, both confirmed panicking in `--release`; `cargo test -p zheng --lib -- sumcheck::prover::` green (4, 2 new); full-suite `cargo test -p zheng --lib` 135 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none new (pins locally bumped for isolation only, reverted before commit); `ccs/mod.rs:299`'s replay-divergence debug_assert (soundness-adjacent, like row 86) and `ccs/universal.rs:196`'s gadget-collision guard flagged, not fixed; PR open: [zheng#27](https://github.com/cyberia-to/zheng/pull/27) — enforce sumcheck prover post-fold invariants in release | 2026-09-30 |
| 82 | `foculus::finality::Domain::from_focus` (the constructor domain-local finality, protocol.md step 6, builds its ε-support domain from) enforces the particles/focus parallel-vector length invariant in release builds, not only debug | foculus | open | found by a launch worker, same class and same run as rows 77/80/81: `debug_assert_eq!` compiled out in release, so a mismatched (particles, focus) pair constructed silently — `mean()`/`variance()` divide by `focus.len()` as if it were the domain size, and `focus_of()` indexes `focus` by a position found in `particles`, so a longer `particles` panics later at an unrelated call site instead of here; promoted to `assert_eq!` with a named message, 1 regression test added, confirmed panicking in `--release`; `cargo test --lib finality::` and `--release` both green (7, 1 new), full-suite `cargo test --lib` green (145; pins locally bumped to bbg 0.3/zheng 0.4/tade per row 47/foculus#18, not yet merged, for isolation only, reverted before commit); no production caller currently passes mismatched vectors, so this closes the gap pre-emptively; PR open: [foculus#21](https://github.com/cyberia-to/foculus/pull/21) — enforce Domain::from_focus length invariant in release builds | 2026-09-30 |
| 85 | `zheng::sumcheck::prover::{SumcheckProver, OuterSumcheckProver}::new` (the bilinear and multi-multilinear sumcheck bookkeeping constructors behind ticket proofs and fold) enforce their table-length invariants in release builds, not only debug | zheng | open | found by a launch worker, same run as row 86, same class as rows 77/80/81/82: both constructors checked length invariants with `debug_assert!`/`debug_assert_eq!`, compiled out in release — `SumcheckProver::new`'s mismatch is not caught anywhere downstream, `zip` silently truncates and `claimed_sum` is computed over fewer terms than `num_vars` implies; `OuterSumcheckProver::new`'s mismatched `f_table` panics later inside `compute_sum` (an unrelated call site) instead of at construction, or silently drops its tail if longer; both promoted to `assert!`/`assert_eq!` with named messages, 3 regression tests added, all confirmed panicking in `--release`; `cargo test -p zheng --lib -- sumcheck::prover::` and `--release` both green (5, 3 new); full-suite `cargo test -p zheng --lib` 136 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none new, none in `sumcheck::prover` (pins locally bumped to nox 0.3/lens 0.2/tade for isolation only, not yet merged, reverted before commit); both constructors are prover-side, driven by zheng's own pipeline rather than directly by untrusted data, so this closes the gap pre-emptively; PR open: [zheng#25](https://github.com/cyberia-to/zheng/pull/25) — enforce sumcheck prover length invariants in release builds | 2026-09-30 |
| 86 | `zheng::multilinear::evaluate_multilinear` (the evals/point folding evaluator `SpartanVerifier::verify_using` calls twice on the proof-verification path) enforces `evals.len() == 1 << point.len()` in release builds, not only debug | zheng | open | found by a launch worker, same class as rows 77/80/81/82 but on the verifier path: `debug_assert_eq!` compiled out in release, and it is not redundant with `fold_inplace`'s own power-of-two check — when `evals.len()` is already a power of two but larger than `1 << point.len()`, folding stops at a partial table and `table[0]` silently returns the wrong evaluation instead of panicking; called from `spartan/verifier.rs:92,137` where lengths trace back to proof data, so a silent wrong value here is soundness-adjacent, not only an internal bug; promoted to `assert_eq!` with a named message, 1 regression test added, confirmed panicking in `--release`; `cargo test -p zheng --lib -- multilinear::` and `--release` both green (7, 1 new); full-suite `cargo test -p zheng --lib` 134 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none new, none in `multilinear` (pins locally bumped to nox 0.3/lens 0.2/tade for isolation only, not yet merged, reverted before commit); no confirmed forged-proof exploit built, so this closes the release/debug enforcement gap pre-emptively; retitled from #84 to avoid a row collision with glia#3; PR open: [zheng#24](https://github.com/cyberia-to/zheng/pull/24) — enforce evaluate_multilinear length invariant in release builds | 2026-09-30 |
| 89 | `zheng::ccs::build_look_steps_from_trace`'s root replay (the BBG root a look opening binds into the trace, computed both natively by `root_from_leaves` and by the traced `build_root_steps` that emits the Layer-1 witness rows) rejects a divergence between the two instead of trusting a debug-only check | zheng | open | found by a launch worker: `debug_assert_eq!` compiled out in release; worse than the other release/debug gaps (rows 77/80/81/82/85/86), because the eq steps bind the native `root` into the trace while the pushed witness rows come from the traced `computed` — a release-build divergence would silently emit a witness for a different root than the one asserted, with the caller getting `Ok(..)`; fixed by returning `CommitError::LookBinding` instead, matching that error's own doc comment ("a ... root constraint is unsatisfied"); `cargo test -p zheng --lib` 133 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none in `ccs::mod`/`ccs::root`; `root_from_leaves` and `build_root_steps` are deterministic pure functions of the same input so no known caller can force the divergence branch itself — closes the gap pre-emptively; PR open: [zheng#26](https://github.com/cyberia-to/zheng/pull/26) — reject a look-opening root replay that diverges from the native fold | 2026-10-06 |
| 90 | `tru::Fx::to_i64_scaled` (the `.model` tensor storage encoding every trained weight is written through) enforces its `frac_bits ≤ FRAC_BITS` bound in release builds, not only debug | tru | open | found by a launch worker, same class as rows 77/80/81/82/85/86: `debug_assert!` compiled out in release, and a violation does not panic downstream either — the u32 subtraction underflows and Rust's shift operators mask rather than panic outside debug, so the function would silently return a wrong scaled value, the exact silent-corruption failure mode arithmetic.md exists to rule out; promoted to `assert!` with a named message, 1 regression test added, confirmed panicking in `--release`; `cargo test -p cyber-tru --lib` green (85, full crate); the one production caller (`model/writer.rs`) only ever passes a fixed 8 or 16, so this closes the gap pre-emptively; PR open: [tru#16](https://github.com/cyberia-to/tru/pull/16) — enforce Fx::to_i64_scaled's frac_bits bound in release builds | 2026-09-30 |
| 96 | `soft3 node`'s flag parser (`cmd_node`, the entry point every operator uses to run a node on the three network machines) is covered by tests and never panics on a mistyped invocation | soft3 | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/88: a missing value for `--home`/`--bind`/`--moniker` reached `.expect(...)`, panicking with an unwind instead of the clean `eprintln!` + `process::exit(2)` the same function already uses one branch down for an unknown flag; extracted into a pure `parse_node_args` returning `Result`, `hostname_fallback`'s env lookup made injectable; 11 tests added, `cargo test --bin soft3` and full `cargo test` green (11); PR open (retitled from #95, row collision with inf#2): [soft3#14](https://github.com/cyberia-to/soft3/pull/14) — cover node arg parsing; stop panicking on missing values | 2026-10-06 |
| 98 | `mudra`'s CLI argv split (`main`'s `--bostrom` flag strip and command/mnemonic split, the entry point every operator uses to claim a legacy account) is covered by tests | mudra | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/88/96: `bin/mudra.rs` had zero test coverage; extracted into a pure `parse_cli_args`, behavior-preserving, plus direct coverage of `hex()`; 9 tests added; this repo's default checkout does not build independent of row 38/mudra#6's pin bump (not yet merged) — applied locally only to verify, reverted before commit, this PR touches only `src/bin/mudra.rs`, `cargo test --bin mudra` green (9) and full `cargo test` green (2, `tests/vectors.rs`) with the pin bump applied; PR open (retitled from #97, row collision): [mudra#10](https://github.com/cyberia-to/mudra/pull/10) — cover CLI argv split | 2026-10-06 |
| 97 | tade's `Reader::next_chunk` (the wire-framing codec every phase-1 binary reads frames through — soft3 HTTP ingress, radio transport, cyb) never panics on an attacker-supplied frame length | tade | open | found by a launch worker, first launch PR on tade (0 prior): `payload_start + payload_len as usize` computed directly from an untrusted LEB128 varint with no overflow check, same bug class as row 76 (tru's `start + sz as usize`) — a length near `u64::MAX` overflows `usize`, panicking in debug (`attempt to add with overflow`, confirmed by temporarily reverting the fix and re-running the regression test) and wrapping to a bogus `start > end` range that panics on the following slice in release; fixed with `payload_start.checked_add(...)`, folding the unsatisfiable case into the existing `Pending` path; 1 test added, `cargo test` green (25 lib + 2 integration, 1 new), `cargo check --tests` clean; PR open: [tade#2](https://github.com/cyberia-to/tade/pull/2) — reject overflowing varint payload length before slicing | 2026-10-06 |
| 99 | `zheng::ccs::universal::Builder::put` (the placement primitive behind the universal CCS gadget circuit's static `build()`, the fixed constraint layout every ticket proof and fold step is checked against) enforces its slot/row collision guard in release builds, not only debug | zheng | open | found by a launch worker following up on row 93/zheng#27, which fixed two sibling debug_assert-in-release gaps in the same file/class but explicitly flagged this one, not fixed (row 89/zheng#26's `ccs/mod.rs:299` was confirmed already fixed by reading its diff, not a second gap); `SparseMatrix::set` pushes rather than overwrites, so an uncaught collision does not panic downstream either — it silently mixes two gadgets' linear forms into one wrong constraint; promoted to `assert!`, 2 regression tests added, one confirmed panicking in `--release`; `cargo test` green (135 passed, 10 pre-existing failures unrelated — the row 46/zheng#22 lens 0.2 TensorMerkle gap, none new, none in `ccs::universal`; pins locally bumped for isolation only, reverted before commit); no production caller reaches this with a dynamic (slot, row) pair, `build()`'s call sites are all static, so this closes the gap pre-emptively like rows 77/80/81/82; PR open: [zheng#28](https://github.com/cyberia-to/zheng/pull/28) — enforce Builder::put's collision guard in release builds | 2026-10-06 |
| 100 | `cyber-bao::io::decode::decode` (the verified-streaming decoder `particle decode`/`radio-cli blob decode` call, the primitive row 22's cyb-fetches-from-a-peer path depends on) rejects an oversized declared_size before it drives allocation, instead of trusting an unverified 8-byte header | radio | open | found by a launch worker surveying lane C: the header's declared_size fed `BaoTree::pre_order_chunks()` and `Vec::with_capacity(declared_size as usize)` before a single hash was verified — a peer sending a short buffer with declared_size near u64::MAX crashed the decoder (capacity-overflow panic) instead of `DecodeError::Truncated`; fixed by rejecting declared_size greater than encoded.len() minus 8 before any size-proportional work, a correctness check since a valid encoding can never need more payload than that; 2 tests added, `cargo test -p cyber-bao` green (69, 2 new); PR open: [radio#10](https://github.com/cyberia-to/radio/pull/10) — reject oversized declared_size before allocation in bao decode | 2026-10-06 |
| 101 | `cyber-bao::io::slice::decode_slice` (the slice-proof verifier for a requested byte range, sibling to row 100's full-body decode) caps the declared block count before it drives allocation, instead of trusting an unverified header | radio | open | found by a launch worker, same run as row 100, same file family: `declared_size` fed `pre_order_chunks()` before verification here too, but a slice legitimately proves a small range of a large file, so row 100's `declared_size <= buffer.len()` bound is wrong for this function; capped `tree.blocks()` directly at a conservative constant (`1 << 24`, not derived from a committed cost model, same class as row 94) before the tree is built; 1 test added, `cargo test -p cyber-bao` green (68, 1 new); PR open: [radio#11](https://github.com/cyberia-to/radio/pull/11) — cap declared block count before allocation in bao slice decode | 2026-10-06 |
| 102 | `cybergraph-cli` (the operator's only hand-tool over the cybergraph processor) builds on a clean checkout, independent of row 48's pin drift; its `hex`/`h32`/`fmt_value` helpers are covered by direct unit tests | cybergraph | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/71/88/96, but with its own build gate: `cargo check`/`cargo test` could not run from `cli/` at all, on any checkout — the parent `Cargo.toml`'s empty `[workspace]` table claims it as a workspace root `cli` isn't a listed member of; an empty `[workspace]` table in `cli/Cargo.toml` opts it out, cargo's own suggested fix; `h32` already guards with `is_ascii_hexdigit()` before slicing, confirmed non-panicking rather than assumed; 16 tests added, `cargo test` green (16, all new; row 48's separate bbg/zheng/nox/lens pin drift bumped locally only to verify, reverted before commit); PR open: [cybergraph#7](https://github.com/cyberia-to/cybergraph/pull/7) — cybergraph-cli opts out of parent workspace; cover h32/hex/fmt_value | 2026-10-06 |
| 104 | the `inf` CLI's `parse_plan` gate and `fmt_value` printer (every `check`/`plan`/`cost`/`run` subcommand goes through the first, every printed row through the second) are covered by direct unit tests | inf | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/71/88/96/102, first launch PR on `inf`'s CLI crate specifically (row 94/95 touched `inf-eval`'s fixed rules, not the CLI): zero prior coverage; `fmt_value`'s `Value::Hash` truncates to its first four bytes and `Value::Bytes` prints quoted only when valid UTF-8, else hex — neither branch was exercised; 13 tests added, reusing `inf-parse`'s own `vertical_slice`/`bounded_recursion` query strings so they match the demo graph's schema; relation existence turned out to be an eval-time concern, not a `plan()`-time one — learned by writing the test, not assumed; `cargo test -p inf-cli` green (13, all new), full `cargo test --workspace` green (12 suites, 0 failed); PR open (retitled from #103, row collision with foculus#22): [inf#3](https://github.com/cyberia-to/inf/pull/3) — cover parse_plan/cmd_*/fmt_value in the inf CLI | 2026-10-06 |
| 103 | `foculus::frames::deserialize_signal` (the radio ingress decoder behind `decode_signal_frame`/`decode_signals`/`decode_events`, "Radio carries these frames") does not preallocate from an unverified wire-supplied link count | foculus | open | found by a launch worker: `count` is an attacker-controlled `u32` read before a single link byte is checked to exist, then fed straight into `Vec::with_capacity(count)` — an 84-byte payload declaring `u32::MAX` links drives an allocation-abort DoS on the sync/radio path, not the graceful `None` every other short read in this decoder already returns; `box_moves` and `delta_pi` two field groups later already grow on demand instead of preallocating from an untrusted count, `links` now matches them; 1 regression test added reproducing the exact pre-fix shape, `cargo test --lib -- frames::` green (8, 1 new), full `cargo test --lib` green (145; row 47's separate cyber-tape→tade path and bbg/zheng pin drift bumped locally only to verify, reverted before commit); no MAX_LINKS-style cap exists in this legacy frame format, a large-but-real signal still walks a long loop bounded only by actual buffer bytes — flagged as a separate, larger slice; PR open: [foculus#22](https://github.com/cyberia-to/foculus/pull/22) — bound deserialize_signal's link-count allocation | 2026-10-06 |
| 105 | `tru::vocab::parse_particles` (the decoder behind `Vocab::from_bytes`/`Vocab::read`, the public loader for `.vocab`/`.model` container files) does not preallocate from an unverified file-supplied entry count | tru | open | found by a launch worker, same class as rows 76/100/101/103: `n` is a raw file-supplied `u32` read before a single entry byte is checked to exist, then fed straight into `Vec::with_capacity(n)` — a 4-byte `particles` section declaring `n = u32::MAX` drives an allocation-abort DoS on any `.vocab`/`.model` load rather than the `InvalidGraph` truncation error the per-entry loop already returns one line later; capped the preallocation at `b.len().saturating_sub(4) / 40` (the minimum bytes a single entry needs), the existing per-entry error handling unchanged; 1 regression test added, `cargo test --lib -- vocab::` green (6, 1 new), full `cargo test --lib` green (85); row 76's `frontmatter::index_sections` fix already guards a section's own offset, this closes the sibling gap at a section's self-declared entry count; PR open: [tru#17](https://github.com/cyberia-to/tru/pull/17) — bound parse_particles's entry-count allocation | 2026-10-06 |
| 106 | [[prysm]] (cyb's paint layer, "every rendered row") builds from a clean checkout, same class as rows 38/44/46/47/78 | prysm | open | found by a launch worker, first launch PR on prysm (0 prior, matching the component table's 0-test baseline): the `tape`→`tade` rename (2026-09-16) was missed here entirely — `Cargo.toml`'s `tape = { path = "../tape/impl/rust" }` and every `use tape::`/`pub use tape` across all six crates (`rs`, `atoms`, `molecules`, `system`, `examples`) left a clean checkout unable to resolve the path at all, `cargo check` failing before compiling a single file; fixed by renaming the dependency to `tade` and every reference to match (API unchanged, package rename only), one prose string in the demo example left untouched; `cargo check --tests` and `cargo test` green (0 passed, 0 failed — the crate's own zero-test baseline, unchanged); PR open (retitled from #104, row collision with inf#3): [prysm#4](https://github.com/cyberia-to/prysm/pull/4) — bump tape->tade path so prysm builds clean | 2026-10-06 |
| 107 | hemera-cli's `fmt_size` (every subcommand's byte-count output) and `parse_backend_flag` (the `--gpu`/`--cpu` override every hashing/proving subcommand reads) are covered by direct unit tests | hemera | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/71/88/96/102/104, on a file row 63 already touched for a different function (`parse_hex_fixed`'s non-ASCII panic) but left these two untested — 0 `#[test]` in the whole file before this; 9 tests added: every `fmt_size` unit boundary (B/KB/MB/GB, incl. the just-under-the-next-unit rounding edge — `1024*1024 - 1` bytes rounds to `1024.0 KB`), `parse_backend_flag`'s default/`--gpu`/`--cpu`/last-flag-wins/flag-stripped-from-any-position/empty-input behavior; `cargo test -p hemera-cli` green (9, all new), full `cargo test` green (7 suites, 0 failed); PR open: [hemera#4](https://github.com/cyberia-to/hemera/pull/4) — cover fmt_size/parse_backend_flag in hemera-cli | 2026-10-06 |
| 108 | [[mir]]'s `EdgePass::update_flow_uvs` (the graph world's per-frame flow-UV animation, "every rendered row" of the visible graph) enforces its offset/weight length invariant in release builds, not only debug | mir | open | found by a launch worker, first launch PR on mir (0 prior), same class as rows 77/80/81/82/85/86/90/93/99: `debug_assert_eq!` compiled out in release, and the mismatch was not caught downstream either — the update loop's `Iterator::zip` silently truncates to the shorter of `flow_offsets`/`edge_weights` instead of panicking, so a release-build length mismatch would silently stop updating flow offsets partway through the visible edge set with no error; promoted to `assert_eq!`; the only call site (`bevy/world.rs::animate_edges`) always derives both lengths from `flow_offsets.len()` itself, so this closes the gap pre-emptively, no known live bug; 3 tests added (this file had zero prior coverage) — wrap-and-advance, resize grow/shrink, and the panic regression confirmed under `--release` where the old `debug_assert!` would not have fired; `cargo test --lib --features bevy-plugin -- frame::edges::` green (3, all new) and full default-feature `cargo test --lib` green (41); PR open: [mir#1](https://github.com/cyberia-to/mir/pull/1) — enforce EdgePass::update_flow_uvs length invariant in release | 2026-10-06 |
| 109 | `foculus::wire::decode_settle_msg`'s `decode_claim_body`/`decode_acc_body` (the settle-gossip radio and iroh ALPN settle-protocol decoders) do not preallocate from an unverified wire-supplied entry count | foculus | open | found by a launch worker, same class as rows 76/97/100/101/103/105, in a file (`wire.rs`) none of those touched: `n` is a raw wire-supplied `u32` read before a single link/Fx byte is checked to exist, then fed straight into `Vec::with_capacity(n)` for both `links` and `sum_m` — a short or corrupted `ClaimAnnounce`/`SelfAcc` message declaring `n = u32::MAX` drives an allocation-abort DoS on `decode_settle_msg` itself; capped the preallocation at what the remaining buffer could actually hold (121 bytes/link, 8 bytes/Fx), the per-entry decode loop's existing truncation handling unchanged; 2 regression tests added, `cargo test --lib -- wire::` green (5, 2 new), full `cargo test --lib` green (146; pins locally bumped to bbg 0.3/zheng 0.4/tade per row 47/foculus#18, not yet merged, for isolation only, reverted before commit); `decode_acc_body`'s `seen` count drives an unbounded loop but not a preallocation (`BTreeSet::new()`), fails fast on the first missing byte instead — not the same bug, not touched; PR open (retitled from #108, row collision with mir#1): [foculus#23](https://github.com/cyberia-to/foculus/pull/23) — bound decode_settle_msg's link/table preallocation | 2026-10-06 |
| 110 | `tok::ledger::MintLedger`'s burn/mint edge paths (conservation core, row 13) are covered by direct unit tests, not only one happy-path mint and one happy-path burn | tok | open | found by a launch worker, same coverage-gap class as rows 39–41/53/54/57/70/71/88/96/102/104/107: `ledger.rs` had exactly 2 tests before this, none touching a rejected insufficient burn, a burn from an untouched neuron, zero-amount legs inside `mint_batch`, an all-zero or empty batch, per-token balance isolation, or a neuron repeated within one batch; 8 tests added, no production code changed; `cargo test --lib` green (16, 8 new); `mint_batch`'s `BurnExceedsMint` rollback branch looks structurally unreachable — mint always grows `minted` and a balance by the same delta, so `check_token` cannot fail right after a mint if it held before — flagged, not exercised; PR open: [plumb#7](https://github.com/cyberia-to/plumb/pull/7) — cover MintLedger burn/mint edge cases | 2026-10-06 |
| 111 | [[honeycrisp]] (glia's accelerator: unimem/acpu/aruminium/rane) builds from a clean checkout; `unimem::Tape::take`'s alignment invariant enforces in release builds, not only debug | honeycrisp | open | found by a launch worker, first launch PR on honeycrisp (0 prior): the clean-checkout build was broken independent of this change — root `Cargo.toml`'s `nebu` path dependency pinned `package = "cyb-nebu"`, but strata renamed that crate to `strata-nebu` (confirmed on strata's own `main`), so `cargo check -p unimem` failed before compiling a single file, same drift class as rows 38/44/46/47/78/106; fixed the pin. Once resolved, `Tape::take`'s alignment check was `debug_assert!(align.is_power_of_two(), ...)`, compiled out in release — a non-power-of-two align silently computed a wrong mask and returned a bogus offset from the lock-free bump allocator instead of failing, same class as rows 77/80/81/82/85/86/90/93/99; promoted to `assert!`. 2 regression tests added (file had zero prior direct unit tests, only `tests/roundtrip.rs` integration coverage); `cargo test -p unimem` and `cargo test -p unimem --release` both green (2 new lib + 36 pre-existing integration, 0 failed); PR open: [honeycrisp#1](https://github.com/cyberia-to/honeycrisp/pull/1) — honeycrisp builds clean; Tape::take alignment invariant enforced in release | 2026-10-06 |
| 112 | soft3 node's `route`/`route_ready` HTTP dispatch table (`crate/src/node/routes.rs`, the switchboard every GET/POST on the node's HTTP ingress passes through) is covered by direct unit tests | soft3 | open | found by a launch worker, closing a gap rows 40 and 41 both flagged in their own Remains as needing "a live Node/TCP fixture" — unnecessary: `route_ready(&mut Node, Request)` is a pure function once a `Node` exists, no socket required; 18 tests added covering every GET route, the routes.rs-local clamp/range checks in front of `parameter()` (`/blocks` limit clamp, `/v2/history` limit range), trailing-slash normalization, POST dispatch into `requests::submit` vs `/v1/finalize`'s 410 Gone vs the unmatched-path 404, and the outer `route()` lock wrapper; added a `#[cfg(test)]`-gated `Response::parts()` accessor since `Response`'s fields were private even to sibling module `routes.rs`, the only production-code change; `cargo test --lib -- node::routes::` green (18, all new), full `cargo test` green (22 lib); PR open: [soft3#15](https://github.com/cyberia-to/soft3/pull/15) — cover routes::route/route_ready HTTP dispatch table | 2026-10-06 |
| 113 | [[spark]] (cyb's opener, lane C: "media and PDF render") has direct test coverage for `open()`'s dispatch, not only `resolve()`'s classification | spark | open | found by a launch worker surveying lane C: spark is a named phase-1 dependency (component table, calendar weeks 2–3) that had received zero launch PRs; `resolve()` had one test (PNG), `open()`'s `Surface::Image` branch — the bytes+kind actually handed to the shell — was untested, JPEG/GIF/WEBP untested in either function; 4 tests added, one per image kind, asserting `open()`'s returned surface matches the input; also documented (comment only) why `open_text`'s UTF-8-error arm is unreachable through the public API — `File::kind()` always freshly sniffs the actual bytes, and `Kind::Text` is only assigned once `file::sniff` has confirmed the whole buffer is valid UTF-8; `cargo test` green (7, 4 new); PR open: [spark#2](https://github.com/cyberia-to/spark/pull/2) — cover open()'s image dispatch across every sniffed image kind | 2026-10-02 |
| 114 | [[glia]]'s CPU matmul backend (`run/backend/cpu/matmul.rs::simd_dot_f32`, the reference dot product every naive-CPU inference call in glia goes through, glia's local inference is a lane C dependency of cyb's `soma`) enforces its operand-length invariant in release builds, not only debug | glia | open | found by a launch worker surveying glia's `debug_assert` usages (rows 65/84 only touched hex-decode panics and a build break, none of the module's five `debug_assert`s); same class as rows 77/80/81/82/85/86/90/93/99/108/111: `k = a.len()` drove every index into both slices, so a longer `b` was silently truncated to `a`'s length (a wrong partial dot product) instead of failing anywhere, in release; promoted to `assert!` with a named message; 2 tests added (zero direct unit tests existed, only indirect coverage via `matmul_f32`'s SIMD-vs-scalar comparison) — a scalar-tail correctness check (k=11) and the length-mismatch panic, confirmed under `--release`; `cargo test -p run --lib -- backend::cpu::matmul::` green (5, 2 new) and full `cargo test -p run --lib` green (91); `cargo check -p run --tests` independently red on the pre-existing row 84/glia#3 `TransformerConfig` break, unrelated, not yet merged; `mrope.rs`/`gated_delta.rs`/`quant_matmul.rs`'s remaining four `debug_assert`s in the same file family not yet audited; PR open: [glia#4](https://github.com/cyberia-to/glia/pull/4) — enforce simd_dot_f32's operand-length invariant in release | 2026-09-30 |
| 115 | [[glia]]'s quantized matmul backend (`run/backend/cpu/quant_matmul.rs`'s `read_f16`/`nibbles_f32x8`/`i8_bytes_f32x8`, the Q4/Q8/Q4_K/Q6_K dequant-fusion path behind glia's local inference) enforces its byte-length invariants in release builds, not only debug | glia | open | found by a launch worker on the same debug_assert survey as row 114/glia#4, which flagged these three as not yet audited: all three checked a fixed-length contract (2 or 8 bytes) with `debug_assert_eq!`, compiled out in release; every call site already slices the exact expected length, so no known live bug — closes the gap pre-emptively, same class as rows 77/80/81/82/85/86/90/93/99/108/111/114; promoted to `assert!` with named messages; 6 direct unit tests added (zero existed before, only indirect coverage via the full dequant/matmul fusion tests) — known-bit-pattern decode for each function plus a length-mismatch panic regression per function, confirmed under `--release`; `cargo test -p run --lib -- backend::cpu::quant_matmul::` green (13, 6 new) and full `cargo test -p run --lib` green (95); a broader glia debug_assert survey found two more instances (`mrope.rs`, `gated_delta.rs`) that exist only on the unmerged `feat/glia-cli` branch, out of scope for a PR against main; PR open: [glia#5](https://github.com/cyberia-to/glia/pull/5) — enforce quant_matmul's byte-length invariants in release | 2026-09-30 |
| 116 | [[foculus]]'s `erasure::decode` (the Reed-Solomon reconstruction behind property 34's DAS availability sampling) rejects a duplicate or out-of-range shard index instead of silently interpolating through it | foculus | open | found by a launch worker, first launch PR touching `erasure.rs`: the general (partial-shard) decode path took the first `k` shards passed to it and built Lagrange evaluation points from their `index` fields with no check that those indices were distinct or `< n`; a duplicate index makes the interpolation matrix singular, and `Goldilocks::inv(0) == 0` (Fermat exponentiation, not a panic on zero) means the singular system does not crash — it silently folds that shard's contribution to zero and returns corrupted data, the exact silent-corruption class `arithmetic.md` exists to rule out; the all-n-shards fast path was already safe (`is_complete` rejects duplicates/range before it runs); no production caller currently supplies a duplicate (both callers gather shards by `enumerate()` over a fixed list), so this closes the gap pre-emptively, same framing as rows 77/80/81/82/85/86/90/93/99; `decode` now returns `Result<Vec<u8>, String>` (also turning its two pre-existing `assert!` panics — too few shards, non-power-of-two n — into errors) and both callers, already in `Result`-returning functions, needed only to propagate; `cargo test --lib -- erasure::` green (9, 3 new), full `cargo test --lib` green (147), `cargo test --test structural_sync --test stress --test million` green (32, incl. the 82s chaos test), full `cargo test` green; PR open: [foculus#24](https://github.com/cyberia-to/foculus/pull/24) — reject duplicate/out-of-range shard index in erasure decode | 2026-10-06 |
| 117 | [[honeycrisp]]'s `acpu::sparse::{csr_matvec, csr_matvec_set}` (the CSR sparse matvec [[mir]]'s `backend::csr_matvec_set` forwards into on Apple targets, called every Lanczos iteration behind the graph world's spectral layout) enforces its `y`/`col_idx`/`values` length invariants in release builds, not only debug | honeycrisp | open | found by a launch worker surveying lane C: same class as rows 77/80/81/82/85/86/90/93/99/108/111/114/115, but worse than the usual silent-corruption shape — the aarch64 inner loop writes through `y.get_unchecked_mut(i)` and reads through `x.get_unchecked(cols[j] as usize)`, both bounds-check-free by construction, so a release-build length mismatch is out-of-bounds memory access, not a wrong value; promoted both checks to `assert_eq!` with named messages ahead of the unsafe block; 2 regression tests added, confirmed panicking under `--release`; `cargo test -p acpu --lib -- sparse::` green (8, 2 new) and full `cargo test -p acpu --lib` green (128, 1 ignored); this repo's default checkout does not build independent of row 111/honeycrisp#1's pin fix (`cyb-nebu`→`strata-nebu`, not yet merged) — bumped locally only to verify, reverted before commit; `x` itself and `col_idx`'s value range against `x.len()` remain unchecked, flagged not fixed; PR open: [honeycrisp#2](https://github.com/cyberia-to/honeycrisp/pull/2) — enforce csr_matvec length invariants in release builds | 2026-10-06 |
| 118 | [[mir]]'s `epoch::eigensolver::laplacian_matvec` (the normalized-Laplacian matvec every Lanczos iteration of the graph world's spectral layout calls) enforces its `x`/`y` length invariants in release builds, not only debug | mir | open | found by a launch worker, same run and class as row 108/mir#1 (a different file, `frame/edges.rs`, in the same repo) and row 117/honeycrisp#2 (the unsafe CSR write this function's `y` eventually reaches via `crate::backend::csr_matvec_set` on Apple targets); promoted both `debug_assert_eq!` to `assert_eq!` with named messages as defense-in-depth ahead of the unsafe downstream write; 2 regression tests added, confirmed panicking under `--release`; `cargo test --lib -- epoch::eigensolver` green (4, 2 new) and full `cargo test --lib` green (40); `cargo check --tests` independently needs `--features bevy-plugin` for three integration tests, same pre-existing condition row 108/mir#1 already scoped around; `backend.rs`'s own sibling `debug_assert_eq!` pair (the non-Apple portable path, same unsafe-write class) untouched, flagged not fixed; PR open: [mir#2](https://github.com/cyberia-to/mir/pull/2) — enforce laplacian_matvec length invariants in release builds | 2026-10-06 |
| 119 | [[vault]] builds from a clean checkout; `codec::{Encoder, Decoder}` (the wire-framing primitive every stored record and packet encodes/decodes through) is covered by direct unit tests | vault | open | found by a launch worker, first launch PR on vault (0 prior): mudra renamed its `seed` module to `spell` (`seed()`/`signing_key()` → `spell::derive()`/`spell::signing_key()`, same signatures); vault's `main` still called the old `mudra::seed` path in `src/record.rs`, `src/use_secret.rs` and `tests/otp_identity.rs`, so `cargo check --tests` failed before compiling a single file on a fresh checkout, same drift class as rows 38/44/46/47/78/106/111; fixed the three call sites, no behavior change. `codec.rs` (`pub(crate)`, so its tests live in the module) had zero direct tests, only indirect coverage through the integration suites; 11 tests added — scalar and bytes/text round trips, `Decoder::new`'s `MAX_PACKET_BYTES` bound, `take`/`array` rejecting truncated input as `Corrupt` instead of panicking, `text` rejecting invalid UTF-8, `finish` rejecting trailing bytes, `Encoder::bytes`' growth bound; `cargo check --tests` clean, `cargo test --lib` green (18, 11 new), full `cargo test` (all integration suites) green; PR open: [vault#2](https://github.com/cyberia-to/vault/pull/2) — vault builds clean; cover codec's Encoder/Decoder | 2026-10-06 |
| 120 | [[honeycrisp]]'s `acpu::sparse::chebyshev::chebyshev_matvec` (the graph heat-kernel diffusion step, same file family as row 117) enforces its `y`/`t0`/`t1` scratch-length invariants in release builds, not only debug | honeycrisp | open | found by a launch worker as the direct follow-up row 117/honeycrisp#2 flagged not-yet-audited in the same file family; `t1` reaches row 117's now-hardened `csr_matvec_set` as its `y` argument, so this closes the gap at the layer with the clearest names for the mismatch, independent of merge order with row 117; promoted all three `debug_assert_eq!` to `assert_eq!`; 2 regression tests added, confirmed panicking under `--release`; `cargo test -p acpu --lib -- sparse::chebyshev` green (5, 2 new) and full `cargo test -p acpu --lib` green (128, 1 ignored); same row 111/honeycrisp#1 clean-checkout pin caveat as row 117, bumped locally only to verify, reverted before commit; `d_inv_sqrt` unchecked (panics via ordinary safe indexing if short, lower priority), `gemm/{gevm_kern,matvec}.rs`'s own asserts unaudited; PR open: [honeycrisp#3](https://github.com/cyberia-to/honeycrisp/pull/3) — enforce chebyshev_matvec scratch-length invariants in release builds | 2026-10-06 |
| 121 | [[tru]]'s `vocab::parse_particles` (the decoder behind `Vocab::from_bytes`/`Vocab::read`, sibling to row 105's fix in the same function) rejects an overflowing file-supplied entry length instead of trusting `c + len` not to overflow | tru | open | found by a launch worker: row 105/tru#17, still open, already fixed this function's unbounded `Vec::with_capacity(n)` preallocation but left a second, independent bug untouched — `c + len` (`len` a raw file-supplied `u64`) was computed unchecked before the `> b.len()` bound check, the identical failure shape already fixed in this repo's sibling decoder `graph::frontmatter::index_sections` (row 76, `start + sz as usize`): a length near `usize::MAX` panics on overflow in debug, wraps to a bogus range in release; fixed with `checked_add`, rejecting the entry instead; 1 regression test added, `cargo test -p cyber-tru --lib -- vocab::` green (6, 1 new) in both debug and `--release`, full `cargo test -p cyber-tru --lib` green (85); PR open: [tru#18](https://github.com/cyberia-to/tru/pull/18) — reject overflowing entry length in vocab::parse_particles | 2026-10-06 |
| 122 | [[tru]]'s `Model::from_bytes` tensor-range check (the `.model` checkpoint loader every glia inference run reads through, third instance of the same bug class in this repo) rejects an overflowing file-supplied tensor offset+size instead of trusting `o + sz` not to overflow | tru | open | found by a launch worker, same run as row 121/tru#18: `o + sz` (`o`/`sz` = `t.offset`/`t.size`, raw `u64` fields parsed from the `tensors` TOML section) was computed unchecked before comparing against `weights.len()`, identical failure shape to row 76's `start + sz as usize` and row 121's `c + len`, now closed in this crate's third file-section-length decoder; fixed with `checked_add`, rejecting the tensor instead; 1 regression test added building a hand-crafted `.model` container with an offset/size that overflows `usize`, `cargo test -p cyber-tru --lib -- model::` green (5, 1 new) in both debug and `--release`, full `cargo test -p cyber-tru --lib` green (85); retitled from #119 to avoid a row collision with vault#2; PR open: [tru#19](https://github.com/cyberia-to/tru/pull/19) — reject overflowing tensor offset+size in Model::from_bytes | 2026-10-06 |
| 123 | [[nox]] (the proof-native VM, "the protocol's own programs") builds from a clean checkout, same class as rows 38/44/46/47/78/106/111 | nox | open | found by a launch worker, first launch PR on nox (0 prior): `rs/Cargo.toml`'s optional `cyber-lens-brakedown` path dependency was still pinned `"0.1"` while lens's brakedown crate bumped independently to `0.2.0` (the sound `TensorMerkle` opening, same drift row 46/zheng#22 already tracks) — cargo resolves every optional path dependency's version bound during workspace unification even with the feature off, so `cargo check --tests` failed before compiling a single file; fixed the one version bound; `cargo test` green (161) and `cargo test -p cyber-nox --features brakedown` green (164); PR open: [nox#6](https://github.com/cyberia-to/nox/pull/6) — bump cyber-lens-brakedown pin so nox builds clean | 2026-10-13 |
| 124 | nox's `jets::ntt::ntt_jet` and `jets::poly_eval::poly_eval_jet` (portable and honeycrisp backends) reject an out-of-range `n`/`k` before shifting, instead of trusting `1usize << n`/`1usize << k` on a value straight from the calling program's atom | nox | open | found by a launch worker surveying lane A's proof-native VM (0 prior launch PRs touching nox's jets; row 123/nox#6 found the same clean-checkout pin gap independently, in this same file family, retitled from #123 to avoid the collision): `n`/`k` come from `reduction.atom_value` with no bound check before `1usize << n`/`k`; a value ≥ 64 panics in debug ("attempt to shift left with overflow"); `poly_eval_jet` is directly exploitable in `--release` too — the masked `expected = 1usize << k` only has to match a small crafted evals tree to pass the `evals.len() != expected` guard, after which `Vec::with_capacity(k)` still uses the full, unmasked `k`; confirmed pre-fix with `k` near the Goldilocks prime: an unconditional "capacity overflow" panic, not an OS-memory-dependent allocation failure, reproduced deterministically; fixed by rejecting `n`/`k >= usize::BITS` before either jet touches it, portable and honeycrisp backends; `cargo test -p cyber-nox` green (165, 4 new) and `--release` green on the touched tests; bundled the same row-123-class pin bump (`cyber-lens-brakedown` 0.1→0.2) as a prerequisite, independently found before seeing nox#6; PR open: [nox#7](https://github.com/cyberia-to/nox/pull/7) — reject an out-of-range n/k before shifting in ntt/poly_eval jets | 2026-10-13 |
| 125 | [[honeycrisp]]'s `acpu::sparse::{csr_matvec, csr_matvec_set}` reject an out-of-range `col_idx` entry before the aarch64 gather, instead of trusting `x.get_unchecked(cols[j])` on a value never checked against `x.len()` | honeycrisp | open | found by a launch worker: row 117/honeycrisp#2 already promoted this file's `y`/`col_idx`/`values` length `debug_assert`s to `assert!` in release but explicitly flagged this value-range gap as not fixed — the aarch64 `inner` reads `x[col_idx[j]]` through `get_unchecked` with no check that the index is in range for `x`; out-of-range is real undefined behavior (an out-of-bounds read) on Apple Silicon, the project's primary runtime target, not a panic — the non-aarch64 fallback already panics safely via ordinary indexing on the same input; fixed with a new `assert_col_idx_in_range` check called once at both public entry points before the unsafe loop runs, in every build; cost stated plainly in the PR: O(nnz), roughly doubling the call's cost on a documented hot path — a cheaper alternative (validate once at CSR-construction time) is a possible follow-up; `cargo test -p acpu --lib -- sparse::` green (9, 4 new), full `cargo test -p acpu --lib` green (129, 1 pre-existing ignore); same row-111-class clean-checkout gap (`nebu` package name) bumped locally only to verify, reverted before commit; `chebyshev::chebyshev_matvec`'s `d_inv_sqrt` and `gemm/{gevm_kern,matvec}.rs`'s own asserts remain unaudited, same as row 120/honeycrisp#3 already flagged; PR open: [honeycrisp#4](https://github.com/cyberia-to/honeycrisp/pull/4) — reject an out-of-range col_idx before the unsafe CSR gather | 2026-10-06 |
| 126 | [[honeycrisp]]'s `acpu::sparse::chebyshev::chebyshev_matvec` enforces its `d_inv_sqrt` length invariant, same as the `y`/`t0`/`t1` siblings row 117/118 already enforce | honeycrisp | open | found by a launch worker, direct follow-up to row 125/honeycrisp#4's Remains and row 120's own flagged gap: the doc comment lists `d_inv_sqrt` as length n like `y`/`t0`/`t1`, but only those three had a boundary length check; every `d_inv_sqrt[i]` access is ordinary safe indexing, not `get_unchecked`, so this is a coverage/consistency fix rather than a memory-safety one — a mismatch already panicked, just deep inside the recurrence with a generic message instead of at the boundary; fixed with one `assert_eq!` alongside the existing three; `cargo test -p acpu --lib -- sparse::` green (7, 1 new), full `cargo test -p acpu --lib` green (127, 1 pre-existing ignore); same row-111-class clean-checkout gap bumped locally only to verify, reverted before commit; `gemm/{gevm_kern,matvec}.rs`'s own asserts remain unaudited; PR open: [honeycrisp#5](https://github.com/cyberia-to/honeycrisp/pull/5) — enforce chebyshev_matvec's d_inv_sqrt length invariant | 2026-10-06 |
| 127 | [[neuron]] (the subject: id, model, engine, node) builds from a clean checkout, and unblocks bbg/tok/mudra's own clean-checkout builds, same class as rows 38/44/46/47/78/106/111/123 | neuron, bbg, tok, mudra | open | found by a launch worker, first launch PR on neuron (0 prior): `bbg/rs/Cargo.toml`, `tok/rs/Cargo.toml` and `mudra/Cargo.toml` all depend on a path crate `neuron/id` that had never been committed to this repo — not on `main`, not in any prior commit, not a workspace member — it existed only uncommitted in the owner's tree as one small piece of a much larger in-progress `cell`→`neuron` package rename; a genuine clean checkout of neuron next to bbg/tok/mudra failed before compiling a single file, and every prior "builds clean" verification of those three had unknowingly run against the owner's live tree through the mirror's symlink, never actually exercising this gap; fixed by adding the crate exactly as it already existed locally (`#![no_std]` `pub type NeuronId = [u8; 32]`, zero dependencies) and registering it as a workspace member, independent of the rest of the in-progress rename; `cargo check --workspace --tests` and `cargo test --workspace` green (15 tests), cargo's own resolution during that check confirmed bbg/cyber-tok/foculus now resolve `neuron-id` through the cell-node/cell-cli dependency chain (launch #127, neuron/audit/clean-checkout-missing-id-crate.md); PR open: [neuron#1](https://github.com/cyberia-to/neuron/pull/1) — add missing neuron-id crate so bbg/tok/mudra build clean | 2026-09-30 |
| 128 | [[honeycrisp]]'s `acpu::gemm::{gevm_kern::gemv_asm, matvec::matvec_f32}` (the AMX/NEON matrix-vector kernels glia's inference and mir's spectral layout call through) enforce their `a`/`b`/`c` slice-length invariants in release builds, not only debug | honeycrisp | open | found by a launch worker, same class as rows 77/80/81/82/85/86/90/93/99/108/111/117/118/120/126, worse than the typical silent-wrong-value shape: the checked lengths feed straight into unsafe raw-pointer AMX/NEON stores with no bounds checking of their own — `gemv_asm`'s `core::ptr::copy_nonoverlapping` into `pc.add(tile*16)` and `vst1q_f32(pc.add(j), ...)`, `matvec_f32`'s tiled/broadcast NEON stores through `pc.add(j)` — so a release-build length mismatch is a real out-of-bounds write, the same severity row 117/125 already closed for this crate's CSR path; promoted both functions' three `debug_assert_eq!` to `assert_eq!` with named messages; 2 regression tests added, both confirmed panicking under `--release`; `cargo test -p acpu --lib -- gemm::` green (16, 2 new) and full `cargo test -p acpu --lib` green (128, 1 pre-existing ignore); same row-111-class clean-checkout gap (`nebu` package pin) bumped locally only to verify, reverted before commit; `sme/gemm.rs`'s M4+ cache-blocked matmul not yet audited for the same class; PR open: [honeycrisp#6](https://github.com/cyberia-to/honeycrisp/pull/6) — enforce gemv_asm/matvec_f32 length invariants in release | 2026-10-06 |
| 129 | `bbg-cli`'s `frame::deserialize_signal` (the store-log signal decoder every log replay reads through) does not preallocate from an unverified wire-supplied link/box-move count | bbg | open | found by a launch worker, same class as rows 76/97/100/101/103/105/109: `n_links`/`n_moves` are raw wire `u32`s read before a single entry byte is checked to exist, then fed straight into `Vec::with_capacity` — a corrupted or crafted log entry declaring a count near `u32::MAX` drives an allocation-abort DoS instead of the `None` every other short read in this decoder already returns; fixed by capping each preallocation at what the remaining buffer bytes could actually hold (105 bytes/link, 33 bytes/move); 2 regression tests added, `cargo test` green (5, 2 new; `cli`/`rs` pin bump from row 44/bbg#12, unmerged, applied locally only to verify, reverted before commit — this PR touches only `cli/src/frame.rs`); PR open: [bbg#16](https://github.com/cyberia-to/bbg/pull/16) — bound signal link/move preallocation to remaining buffer | 2026-10-06 |
| 130 | [[mir]]'s `backend::csr_matvec_set`'s non-Apple portable path enforces its `y`/`col_idx`/`values` length invariants in release builds, not only debug | mir | open | found by a launch worker, flagged not-fixed in row 118/mir#2's own note ("backend.rs's own sibling debug_assert_eq! pair ... untouched"), same class as rows 77/80/81/82/85/86/90/93/99/108/117/118/120/126/128: a longer `col_idx`/`values` pair silently truncated the row's dot product instead of catching the mismatch, and a wrong `y` length just stopped filling partway through, in release; promoted both to `assert_eq!`; the portable loop sits behind `#[cfg(not(target_vendor = "apple"))]`, dead code on this Apple Silicon dev machine, so it was pulled into its own `csr_matvec_portable` function (no cfg on the function itself) to make the new asserts directly testable everywhere; 3 tests added (1 correctness, 2 panic regressions), `cargo test --lib` green (41, 3 new) and `cargo test --features bevy-plugin` green (the three integration suites row 108/mir#1 already scoped around); PR open: [mir#3](https://github.com/cyberia-to/mir/pull/3) — enforce csr_matvec_set's portable-path length invariants | 2026-10-06 |
| 131 | [[honeycrisp]]'s `acpu::field::{ntt::bit_reverse_permute, poly::multilinear_eval}` (Goldilocks NTT bit-reversal and multilinear evaluation) enforce their power-of-two length invariants in release builds, not only debug | honeycrisp | open | found by a launch worker surveying lane C, same class as rows 77/80/81/82/85/86/90/93/99/108/111/114/115/117/118/120/125/126/128/130 in a file family (`field/`) none of those touched: `bit_reverse_permute` is `pub`, reachable directly (not only through `ntt_forward`, which already carries its own `assert!`), and a release-build call with a non-power-of-two length silently scrambles into the wrong slots instead of failing at the boundary; `multilinear_eval`'s recursive `evals`/`point` split desyncs the same way, either returning a silently wrong evaluation or panicking deep inside on an unrelated index; both promoted to `assert!` with named messages; 2 regression tests added, both confirmed panicking under `--release`; `cargo test -p acpu --lib -- field::` green (29, 2 new) in debug and `--release`, full `cargo test -p acpu --lib` green (128, 1 pre-existing ignore); this repo's default checkout does not build independent of row 111/honeycrisp#1's `cyb-nebu`→`strata-nebu` pin fix (not yet merged) — bumped locally only to verify, reverted before commit, this PR touches only the two `field/` source files; PR open: [honeycrisp#8](https://github.com/cyberia-to/honeycrisp/pull/8) — enforce power-of-two invariants in field ntt/poly in release | 2026-10-07 |
| 132 | soft3's `node/requests.rs::submit` (the HTTP ingress dispatcher every `/v1/link`, `/v1/pay`, `/v1/frame`, `/v2/frame` POST goes through, critical dependency #3) is covered by direct unit tests, not only its `identity()` helper in isolation | soft3 | open | found by a launch worker, closing the gap rows 40/41/54/112 (soft3#5/#6/#10/#15, all still open) each named in their own Remains as untested: `requests.rs` had zero `#[test]`s on `main`; 15 tests added calling `submit()` directly against a fresh temp-dir `Node` — a Link happy path that reaches real `NativeNode::accept` and returns a receipt, `Link`'s `deny_unknown_fields`/default-token/default-amount fill-in, `Pay`'s zero-amount and insufficient-funds rejections, `/v1/frame`'s empty-batch and unparseable-bytes rejections, `/v2/frame`'s unparseable-bytes rejection, the unsupported-route arm, and two Idempotency-Key→`request_id` wiring checks through `identity()`; added the same test-only `Response::json()`/`Debug` scaffolding row 112/soft3#15 independently adds to `http.rs`, may need a trivial merge resolution against it; `cargo check --tests` clean, `cargo test --lib` green (15, 11 new), full `cargo test` green; PR open (retitled from #131, row collision with honeycrisp#8): [soft3#16](https://github.com/cyberia-to/soft3/pull/16) — cover submit()'s Link/Pay/frame dispatch end-to-end | 2026-10-07 |
| 133 | soft3's `network.rs` (the network-identity module every genesis, sync and CLI path resolves a chain name through) is covered by direct unit tests for its full alias sets, not one happy-path case each | soft3 | open | found by a launch worker: `Network::parse`'s 8 spacepussy-test synonyms and 6 bootloader-name synonyms were untested except two names each, the shared `.trim().to_ascii_lowercase()` normalization untested, and the nine chain-identity accessors (`chain_id`/`bech32_prefix`/`denom`/`rpc`/`lcd`/`index`/`websocket`/`local_bind`/`role`) plus `Display` had no direct assertions; 8 tests added — every valid alias, case-insensitivity and whitespace-trimming for both `parse` and `is_bootloader_name`, unknown-name rejection, every bootloader name rejected by `parse`, all nine accessors, `Display`; no production code changed; `cargo check --tests` clean, `cargo test --lib` green (11, 8 new), full `cargo test` green; PR open: [soft3#17](https://github.com/cyberia-to/soft3/pull/17) — cover Network::parse/is_bootloader_name aliases and accessors | 2026-10-07 |
| 134 | bbg's `dim.rs` (the dimension-commitment helpers every authenticated-state dimension — signals, particles, axons, neurons — serializes and commits through) is covered by direct unit tests | bbg | open | found by a launch worker: `goldilocks_from_u64`, `goldilocks_from_bytes32` and `commit_dim` had zero `#[test]`s on `main`; 7 tests added — `u64` round-trip across a value spread, the 32-byte-to-four-little-endian-limb split (hand-built bytes, plus all-zero), `commit_dim` determinism, key-order and value sensitivity, and the empty-dimension sentinel's stability and distinctness from any non-empty commitment; this repo's default checkout does not build independent of row 44/bbg#12's lens 0.1.3→0.2.0 pin bump (not yet merged, and `dim.rs`'s own doc comment already assumes lens/0.2) — applied locally only to verify, reverted before commit, this PR touches only `rs/src/dim.rs`; `cargo test --lib -- dim::` green (7, all new) and full `cargo test --lib` green (58) with the pins applied; flagged not fixed: `commit_dim`'s own "sorted map" doc contract is unenforced, nothing rejects an out-of-order or duplicate-key `entries` slice; PR open: [bbg#17](https://github.com/cyberia-to/bbg/pull/17) — cover goldilocks_from_u64/bytes32 and commit_dim | 2026-10-07 |
| 135 | `foculus::frames::deserialize_signal` (the legacy ZAP-sigil signal tape decoder behind radio/durable-log ingress, rows 10/21) does not preallocate from or loop on an unverified wire-supplied collection count | foculus | open | found by a launch worker, same class as rows 76/97/100/101/103/105/109/129, flagged in row 103's own text as a remaining gap ("no MAX_LINKS-style cap exists in this legacy frame format ... a separate, larger slice") and independently in row 109's wire.rs sibling fix; `take_count` now rejects a links/box_moves/delta_pi count the remaining buffer cannot hold (145/33/40 bytes per entry), closing the gap for all three, not only `links`; `cargo test --lib -- frames::` green (14, 9 new), full `cargo test --lib` green (151; this checkout's stale `cyber-tape` path and bbg 0.2/zheng 0.3 pins, rows 44/47, bumped locally only to verify, reverted before commit); PR open: [foculus#25](https://github.com/cyberia-to/foculus/pull/25) — bound legacy signal frame collection counts | 2026-10-07 |
| 136 | [[bostrom]]'s `snapshot/graph_rebuild.py` (the tool that replayed the 2.9M-link bostrom cyberlink graph bit-exact, and its own reuse target for any re-verification or a pussy-side rebuild) never crashes on a malformed cyberlink event | bostrom | open | found by a launch worker, first launch PR on bostrom (0 prior): the whole replay ran as top-level module code, so it could never be imported for testing; the per-tx event parser did `attrs["particleTo"]` unconditionally once `particleFrom` was seen, so an event missing that attribute raised `KeyError` and would abort an hours-long 25M-block rebuild instead of skipping the one malformed pair; extracted the parser into a pure `parse_tx_events(events, height, ts, txh)`, guarded the replay driver behind `main()`, fixed the crash (a missing `particleFrom` or `particleTo` is now dropped, not raised on); 8 stdlib-`unittest` tests added (no new dependency — this repo has no prior test convention); `python3 -m unittest test_graph_rebuild -v` green (8, all new); PR open: [bostrom#1](https://github.com/cyberia-to/bostrom/pull/1) — graph_rebuild's event parser never crashes on a malformed cyberlink event | 2026-10-07 |
| 137 | [[bostrom]]'s `snapshot/holdings.py` (the source of every account's genesis balance: liquid + delegated + undelegating + per-pool decomposition, and the pool-derived price feed) is importable and its denom-label/pool-rate logic is covered by direct unit tests | bostrom | open | found by a launch worker, same run as row 136: `holdings.py` ran its whole pipeline as top-level module code, same shape as row 136's `graph_rebuild.py`; guarded it behind `main()`, extracted `clean`, `denom_label` (parameterized on its `IBC` map instead of a module global), `pool_rate` and a new `compose_rates(pools)` as pure functions, no behavior change; the LP-token pro-rata decomposition loop was left inline in `main()`, flagged as the next slice; 16 tests added, incl. one pinning existing (not new) behavior a first read could mistake for a bug — a dust pool's rate folds to `0.0` via `pool_rate(...) or 0.0`, not "unquoted"; `python3 -m unittest test_holdings -v` green (16, all new); PR open: [bostrom#2](https://github.com/cyberia-to/bostrom/pull/2) — holdings.py importable; cover denom label and pool-rate logic | 2026-10-07 |
| 138 | [[bostrom]]'s `snapshot/graph_scan.py` (upstream of row 136's `graph_rebuild.py`: produces the tx-block height list it replays) tiles every height from 1 to 25,120,712 exactly once, with no gap and no overlap, and this partition is covered by direct unit tests | bostrom | open | found by a launch worker, same run as rows 136/137: the descending fixed-window height partition (`mn = max(lo, h - 19)`) ran as top-level module code with zero coverage; extracted into a pure `height_ranges(lo, hi, window=20)` generator, guarded the RPC-driving loop behind `main()`; a silent gap here would mean missed tx-blocks in the same "bit-exact" burial graph rows 14/20 depend on; 7 tests added — full-coverage-no-gaps-no-overlap for an exact-multiple and a non-multiple range, a non-1 `lo`, a single-height range, an empty range, `window=1`, and a hand-checked exact chunk sequence; progress-print cadence preserved exactly, verified by hand against the original's post-decrement `h`; `python3 -m unittest test_graph_scan -v` green (7, all new); PR open: [bostrom#3](https://github.com/cyberia-to/bostrom/pull/3) — extract graph_scan's height-window tiling; cover it | 2026-10-07 |
| 139 | [[honeycrisp]]'s `acpu::gemm::gemv_amx_asm::gemv_pure_asm` (the pure-assembly AMX GEMV path, sibling to row 128's `gemv_asm`/`matvec_f32`) enforces its `a`/`b`/`c` slice-length and `k` invariants before the raw AMX call, in release builds too | honeycrisp | open | found by a launch worker surveying lane C's glia-accelerator dependency, direct follow-up to row 128's own file family: unlike its two siblings (row 128, already promoted to `assert_eq!`), this function had only a `debug_assert!(k.is_multiple_of(8), ...)` and no check at all — not even debug-only — that `a.len() == k`, `b.len() == k * n`, or `c.len() == n`, before handing raw pointers to an `unsafe extern "C"` hand-written aarch64 AMX kernel with no bounds checking of its own; promoted the `k%8` check to `assert!` and added the three missing `assert_eq!` checks, mirroring `gemv_kern.rs`'s existing pattern; 5 tests added (file had zero prior tests) — a correctness check against a naive GEMV plus one panic regression per invariant, all confirmed panicking under `--release`; `cargo test -p acpu --lib -- gemm::gemv_amx_asm::` green (5, all new) and full `cargo test -p acpu --lib` green (131, 1 pre-existing ignore); same row-111-class clean-checkout gap (`nebu` package pin) bumped locally only to verify, reverted before commit; not called from any production site yet, closes the gap pre-emptively; PR open (retitled from #137, row collision with bostrom#3): [honeycrisp#9](https://github.com/cyberia-to/honeycrisp/pull/9) — check gemv_pure_asm's length/k invariants before AMX asm | 2026-10-07 |
| 140 | `foculus::frames::deserialize_signal`'s box-move commitment tag byte accepts only 0/1, not any non-zero value as "commitment present is false" | foculus | open | found by a launch worker, direct follow-up to row 135/foculus#25 (same file, flagged there as the next slice): the tag branch was `if flag == 1 { Some(..) } else { None }`, so a corrupted or malicious frame with a garbage tag byte silently decoded as "no commitment" instead of being rejected — wrong data accepted as valid, not a decode error; changed to a `match` accepting only `0`/`1`, anything else fails the decode; 3 tests added (commitment round-trip, no-commitment round-trip, invalid-tag rejection); `cargo test --lib -- frames::` green (10, 3 new), full `cargo test --lib` green (147; same pre-existing rows 44/47 pin-drift bumped locally only to verify, reverted before commit); PR open (retitled from #139, row collision with honeycrisp#9): [foculus#26](https://github.com/cyberia-to/foculus/pull/26) — reject invalid box-move commitment tag in frame decode | 2026-10-07 |
| 141 | [[bbg]]'s `dim::commit_dim` (the serializer every one of the 13 dimension commits in `state/commits.rs` funnels through, folded into `BBG_root`) rejects an unsorted or duplicate-key `entries` slice instead of trusting its own "sorted map" doc contract | bbg | open | found by a launch worker, direct follow-up to row 134/bbg#17's own flagged-not-fixed note ("commit_dim's own 'sorted map' doc contract is unenforced, nothing rejects an out-of-order or duplicate-key entries slice"): both the function's and the module's doc comments claim entries arrive as a sorted map, but nothing checked it — an unsorted or duplicate-key slice would fold silently into a non-canonical commitment instead of failing, breaking the one-canonical-commitment-per-state invariant `compute_root` depends on; every current caller builds `entries` from a `BTreeMap<Particle, _>::iter()`, already sorted and duplicate-free by construction, so this closes the gap pre-emptively, same framing as rows 77/80/81/82/85/86/90/93/99/108/111/117/118/120/125/126/128/130/131/139; fixed with one `assert!(entries.windows(2).all(|w| w[0].0 < w[1].0), ...)` right after the empty-dimension early return; 4 tests added (file had zero prior tests) — sorted-unique commits and is deterministic, a single entry commits, out-of-order panics, duplicate keys panic; `cargo test --lib -- dim::` green (4, all new) and full `cargo test --lib` green (55); this repo's default checkout does not build independent of row 44/bbg#12's lens/nox/zheng pin bump (not yet merged) — bumped `rs/Cargo.toml` locally only to verify, reverted before commit; PR open: [bbg#18](https://github.com/cyberia-to/bbg/pull/18) — commit_dim rejects unsorted or duplicate-key entries | 2026-10-07 |
| 142 | bbg's `query::verify_opening` light-client look-opening verifier binds its proof to the opening's own `commitment`/`point`/`value`, not to the `leaves`/`namespace` it also carries | bbg | open | found by a launch worker, direct follow-up to row 75/bbg#14's own Remains ("review query::verify_opening for the same shape"): `verify_opening` re-checks the Brakedown opening's internal consistency but never recomputes `zheng::root_from_leaves(&lo.leaves)` against an expected root, nor checks `lo.leaves.dims[lo.namespace as usize]` against `lo.commitment` — a real opening still verifies after `leaves`/`namespace` are swapped for fabricated values, regression `verify_opening_ignores_leaves_and_namespace` confirms it; same shape as row 75's `verify_particle` gap, but unlike `QueryProof`, `LookOpening` already carries the fields (`leaves`, `namespace`) a fix would bind — `BbgState::root_leaves()`'s commitment→leaf-limb packing is the exact convention a fix would reproduce, sketched but not implemented: getting the limb convention subtly wrong would look like it verifies the binding while remaining unsound, worse than leaving it documented (launch #142, bbg/audit/verify-opening-unbound.md); `cargo test --lib -- query::tests::verify_opening` green (1 new) and full `cargo test --lib` green (52); this repo's default checkout does not build independent of row 44/bbg#12's lens/nox/zheng pin bump (not yet merged) — bumped locally only to verify, reverted before commit; PR open: [bbg#19](https://github.com/cyberia-to/bbg/pull/19) — audit: verify_opening ignores its leaves and namespace | 2026-10-16 |
| 143 | bostrom's `snapshot/extract.py` (the fourth snapshot script, sibling to graph_scan.py/graph_rebuild.py/holdings.py) has its pure logic importable and covered, and its `pubkeys.csv` identity resolver — feeding row 15's legacy-key claim bridge — does not crash on a null `base_vesting_account` | bostrom | open | found by a launch worker, lane B: extract.py was the one of the four snapshot scripts never swept for the coverage gap rows 136-138 already closed on its siblings; extracted `is_extra_denom`, `pool_price`, `resolve_base_account` as pure functions; `resolve_base_account` had a real bug — `a.get("base_vesting_account", {})` only supplies its `{}` default when the key is absent, not when present with a JSON null, so a null-valued key crashed the whole pubkeys export with `AttributeError` instead of falling back to the account record; confirmed the pre-fix crash directly, `python3 -m unittest test_extract.py -v` green (10, all new); no live-LCD smoke test, `get`/`paged`'s retry logic still untested; PR open: [bostrom#4](https://github.com/cyberia-to/bostrom/pull/4) — extract.py pure functions importable and covered | 2026-10-09 |
| 144 | bootloader's `snapshot/extract.py` — byte-identical to row 143's bostrom copy, confirmed by direct diff — carries the same coverage gap and the same `resolve_base_account` null-crash | bootloader | open | found by a launch worker while closing row 143: the two repos hold an identical, unlinked copy of the same script; applied the identical fix (`is_extra_denom`, `pool_price`, `resolve_base_account` extracted, the null-`base_vesting_account` crash fixed); `python3 -m unittest test_extract.py -v` green (10, all new); which repo should own `snapshot/` and how the other absorbs it (subtree/submodule) is unresolved, flagged so the duplication does not silently drift the two copies again; PR open: [bootloader#1](https://github.com/cyberia-to/bootloader/pull/1) — extract.py pure functions importable and covered | 2026-10-09 |
| 145 | [[tok]]'s `mint::execute_settle_mints` (the settle→mint conservation path rows 13/25/26/32 mint through) has direct test coverage for its error-propagation branches and `intent_hash`'s order-independence, not only one happy-path clip test | tok | open | found by a launch worker, lane A: `mint.rs` had exactly one test before this; 5 added — `ConserveError::{Empty,BudgetZero,NoPositiveShare}` each propagate unchanged through `MintError::Conserve`; `intent_hash` is order-independent of input share order; `conserve_and_allocate`'s sub-scale dust path (`emission_scale = 0`) always mints exactly one 1-token leg; `MintError::Ledger`/`EmptyLegs` flagged, not exercised — the dust path guarantees `legs` is never empty on the `Ok` path, and `MintLedger::mint_batch` cannot fail right after a mint that grows `minted` and a balance together, the same structurally-unreachable shape row 110 already flagged for `MintLedger` itself; `cargo test -p cyber-tok` green (13, 5 new); PR open (retitled from #142, row collision with bbg#19): [plumb#8](https://github.com/cyberia-to/plumb/pull/8) — cover execute_settle_mints error paths and intent_hash ordering | 2026-10-09 |
| 146 | [[strata]]'s `kuro-cli`'s `cmd_encode` (`kuro encode <hex_bytes>`) never panics on attacker-supplied input | strata | open | found by a launch worker, same bug class as rows 50/56/58-68 but a sibling crate (`kuro`, not `nebu`) the row-64/strata#4 sweep of this repo never reached: the even-length guard (`input.len() % 2 != 0`) let a multi-byte UTF-8 character keep the total byte length even while landing a `&input[i..i+2]` slice boundary mid-character; confirmed the pre-fix panic standalone (`"aébbb"`, 6 bytes, panics at byte index 2 inside 'é'); fixed by extracting `try_encode_hex`, mirroring strata#4's own `try_decode_hex` shape, with an `is_ascii()` guard before the length check; `cmd_decode`'s `parse_u128` audited too — it parses through `u128::from_str_radix` on the whole string, no manual slicing, not affected; `cargo test -p kuro-cli` green (4, all new — this file had zero prior tests); PR open: [strata#5](https://github.com/cyberia-to/strata/pull/5) — reject non-ASCII input before slicing in kuro encode | 2026-10-09 |
| 147 | [[tru]]'s `graph::reader::Graph` (the `.graph`/`.vocab`/`.model` container opener behind rows 74/76's mmap readers) has direct coverage on `Graph::open` itself, and its `section()` accessor names the section it actually failed to find | tru | open | found by a launch worker, lane A: `reader.rs` had zero direct tests, only one happy-path fixture in `tests/smoke.rs`; while adding coverage, found `section(&self, name: &str)` always built `McError::MissingSection("section")` — the literal word, not the requested name — so a `.graph`/`.vocab`/`.model` file missing one of several declared sections always reported the same useless message; every other caller of this variant (vocab.rs, model/writer.rs) already passes its own correct static name, only this generic pass-through had the bug; fixed by widening `MissingSection` from `&'static str` to `String` and forwarding the real `name` (4 call sites updated); 6 tests added — minimal valid open, rejects a non-"graph" container, `Io` on a missing file, the missing-section regression (variant payload and `Display` text both asserted), non-UTF-8 section rejection, `config_raw` round-trip; `cargo test` green (90 lib + 2 integration + doctests); PR open: [tru#20](https://github.com/cyberia-to/tru/pull/20) — reader.rs names its actual missing section; Graph::open covered | 2026-10-09 |
| 149 | [[cyb]]'s oracle world `short_hex` (the truncator every displayed block/neuron hash in the oracle world goes through) never panics on attacker-supplied input | cyb | open | found by a launch worker, lane C, same bug class as rows 50/56/58–68 but in a display truncator rather than a hex decoder: `&hex[..8]`/`&hex[hex.len() - 4..]` byte-index slicing panicked when a multi-byte UTF-8 character landed on either boundary; `d.neuron` (`oracle/mod.rs:505`) is parsed straight out of an untrusted hub HTTP response body (`field(body, "neuron:")`), so a hub response can crash the oracle world; fixed by truncating on char count instead of byte offset (byte-identical output for ASCII input, never panics on anything else); 4 tests added (module had zero prior tests), `cargo test -p cyb --lib -- oracle::` green (4, all new), `cargo check --tests -p cyb` clean, `make fleet` green (21 ok); PR open: [cyb#1396](https://github.com/cyberia-to/cyb/pull/1396) — oracle's short_hex rejects multi-byte UTF-8 boundary panic | 2026-10-07 |
| 148 | bostrom's `snapshot/holdings.py`'s per-address LP-token pro-rata decomposition (liquid pool-coin → reserve-denom legs, plus delegated/undelegating boot) is a pure, directly tested function, not inline in `main()` | bostrom | open | found by a launch worker, lane B, direct follow-up to row 137's own flagged next slice ("the LP-token pro-rata decomposition loop was left inline in main(), flagged as the next slice"); extracted `compose_account_holdings(lq, pools, delegated, undel, label)`, same shape and no behavior change, `main()` now calls it once per address; 7 tests added — a plain liquid leg, a pool-coin decomposing pro rata into both reserve denoms, a zero-supply pool contributing nothing, delegated+undelegating joining the boot leg, a pool leg and delegated boot in the same bucket, a zero-total denom dropped, empty input; `python3 -m unittest test_holdings -v` green (23, 7 new); this PR targets row 137/bostrom#2's own unmerged branch, not main, because it needs that PR's `main()` import guard to make `holdings.py` importable at all for a test — rebases onto main cleanly once row 137 merges; PR open: [bostrom#5](https://github.com/cyberia-to/bostrom/pull/5) — extract holdings.py's LP-token pro-rata decomposition into a pure function | 2026-10-09 |
| 149 | bostrom's `snapshot/extract.py`'s `get` (five-attempt LCD retry with backoff) and `paged` (`pagination.next_key` cursor loop) are covered by direct unit tests | bostrom | open | found by a launch worker, lane B, direct follow-up to row 143's own flagged Remains ("no live-LCD smoke test, get/paged's retry logic still untested"); `urllib.request.urlopen`/`time.sleep` mocked for `get`, the module's own `get` mocked for `paged`, no production code changed; while writing the next_key-quoting test, confirmed (not assumed) that `urllib.parse.quote`'s default `safe="/"` leaves a base64 next_key's `/` literal and only escapes its `+` — harmless, pinned rather than guessed; 10 tests added — get's first-try success, retry-then-succeed, re-raise after five attempts, 2/4/6/8s backoff with no sleep after the last attempt; paged's single page, multi-page next_key follow, no-pagination-block stop, missing-key empty yield, extra query string, next_key quoting; `python3 -m unittest test_extract -v` green (20, 10 new); this PR targets row 143/bostrom#4's own unmerged branch, not main, for the same import-guard reason as row 148; PR open: [bostrom#6](https://github.com/cyberia-to/bostrom/pull/6) — cover extract.py's get retry backoff and paged pagination | 2026-10-09 |
| 150 | bostrom's `snapshot/graph_rebuild.py`'s `work(h)` (the per-height dispatcher deciding whether a block needs a second fetch at all, upstream of row 136's `parse_tx_events`) is covered by direct unit tests | bostrom | open | found by a launch worker, lane B, direct follow-up to row 136/bostrom#1 which covered `parse_tx_events` but left `work` itself untested; `graph_rebuild.get` mocked, no production code changed; 6 tests added — no-cyberlink-events short-circuits to OK without a second fetch, `block_results` failure reports `FAIL-BR`, a real cyberlink followed by a failed block fetch reports `FAIL-B`, a failed tx (`code != 0`) carrying a cyberlink-shaped event is skipped and never triggers the block fetch, a successful tx yields the correct sha256 hash and normalized timestamp, a tx index past `data.txs`'s length falls back to an empty hash instead of an index error; `python3 -m unittest test_graph_rebuild -v` green (14, 6 new); this PR targets row 136/bostrom#1's own unmerged branch, not main, for the same import-guard reason as rows 148/149; PR open: [bostrom#7](https://github.com/cyberia-to/bostrom/pull/7) — cover graph_rebuild's work() block-fetch dispatch | 2026-10-09 |
| 151 | [[mir]]'s `wgpu_arm::Buffer::read_f32`/`write_f32` (the non-Apple GPU backend's byte-to-`[f32]` reinterpret behind the graph world's render path) enforce their mapped-range alignment invariant in release builds, not only debug | mir | open | found by a launch worker, lane C, same class as rows 77/80/81/82/85/86/90/93/99/108/118/127 but one step worse: `debug_assert_eq!` guarded the alignment precondition of an unsafe `std::slice::from_raw_parts(_mut)` pointer cast, so an under-aligned mapped range in a release build was undefined behavior, not a caught bug or a silently wrong value; `wgpu_arm` only compiles `#[cfg(not(target_vendor = "apple"))]`, so the check was extracted into a free `assert_f32_aligned(addr: usize)` outside that cfg gate and promoted to `assert_eq!`, staying unit-testable on this Apple dev machine even though the unsafe call site itself does not compile here; `bind_buffer`'s sibling `debug_assert_eq!(offset, 0, ...)` in the same file is a lower-priority API-contract check, not a pointer-cast safety precondition — flagged, not fixed; `cargo test --features bevy-plugin --lib -- gpu::` green (2, both new), `cargo check --tests --features bevy-plugin` clean, default-feature `cargo test --lib` green (40); PR open: [mir#4](https://github.com/cyberia-to/mir/pull/4) — enforce f32-reinterpret alignment invariant in release | 2026-10-07 |
| 152 | `foculus::epoch_cert::verify_epoch_cert`'s settle-inputs spot-check rejects any mismatch between a receipt's claimed `directed_total` and the impulse recomputed from public claims, not only the one case where the recompute is positive and the claim is non-positive | foculus | open | found by a launch worker, lane A: `directed_total` gates the settle-mint ceiling (`min(v★, Δφ⁺)` in `tok::clip_shares`) but is bound into neither `receipt_hash` nor `cert_hash`, so this spot-check was the only thing standing between a forged, inflated mint ceiling and a verifying certificate; the actual guard only caught one narrow case, letting every other mismatch — including a zero recompute against a large claimed total — pass silently; Fx is deterministic fixed-point (arithmetic.md), so an honest receipt matches the recompute exactly, replaced the broken conditional with strict equality; `cargo test --lib` green (145; this checkout's stale `cyber-tape` path and bbg/zheng pins, rows 44/47, bumped locally only to verify, reverted before commit); PR open (retitled from #148, row collision with bostrom#5): [foculus#27](https://github.com/cyberia-to/foculus/pull/27) — reject a mismatched directed_total in verify_epoch_cert | 2026-10-07 |
| 153 | [[tok]]'s `clip_shares` conservation ceiling `min(v★, Δφ⁺)` clips to zero when Δφ⁺ is measured as non-positive, not the uncapped lottery sum v★ | tok | open | found by a launch worker, lane A: `rewards.rs`'s own doc for this clip step states the contract — "if directed_total is 0, no mint" — but the guard `directed_total > Fx::ZERO && ...` left `ceiling` at the uncapped `sum_pos` whenever `directed_total` was zero or negative, so a genuinely-measured zero Δφ⁺ epoch still minted the full lottery-estimated amount; the stale inline comment justified this as protecting a hypothetical zero-sentinel caller, but both real call sites (`foculus::rewards::settle_epoch`/`settle_epoch_tickets`) always pass a real measured impulse; fixed by clipping to zero whenever `directed_total <= Fx::ZERO`; `cargo test -p cyber-tok` green (11, 3 new); PR open (retitled from #149, row collision with cyb#1396/bostrom#6): [plumb#9](https://github.com/cyberia-to/plumb/pull/9) — clip_shares clips to zero on non-positive directed_total | 2026-10-07 |
| 154 | [[zheng]]'s `verify_phi_star` binds `proof.statement.graph_commit` to the `(transition, sym)` graphs it is actually called with, not an unchecked prover-supplied label | zheng | open | found by a launch worker, lane A: `graph_commit` is what an outer/recursive verifier reads to know which graph a φ* proof is about (`PhiStatement::to_zheng` folds it into `Statement.output_hash`), but `verify_phi_star` never checked it — the single current caller (`foculus::verify_phi_on_cert`) happens to be safe because it always supplies the real graphs and a genuine mismatch would diverge the recomputed `phi`, but the label itself was otherwise free for a prover to set to any value, a gap for any future consumer trusting the exported `Statement` without re-running the raw graphs; extracted the shared commitment formula into `graph_commit()` and check it at verification entry; `cargo test -p zheng --lib -- phi::trikernel::` green (3, 1 new), full-suite `cargo test -p zheng --lib` 134 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none in `phi::`, none new (pins locally bumped for isolation only, reverted before commit); PR open (retitled from #150, row collision with bostrom#7): [zheng#29](https://github.com/cyberia-to/zheng/pull/29) — verify_phi_star binds graph_commit to the real graphs | 2026-10-07 |
| 156 | [[mir]]'s `backend::csr_matvec_set` (the sparse CSR matvec every Lanczos iteration of the graph world's spectral layout calls, both the apple-forwarding and portable paths) enforces its `y`/`col_idx`/`values` length invariants in release builds, not only debug | mir | open | found by a launch worker, closing the gap row 118/mir#2 named explicitly in its own Remains ("backend.rs's own sibling debug_assert_eq! pair ... untouched, flagged not fixed"), same class as rows 77/80/81/82/85/86/90/93/99/108/111/114/115/117/118; hoisted both checks above the `#[cfg(target_vendor = "apple")]` split into one unconditional `assert_eq!` ahead of either branch — closes the gap on the apple path too (which forwards into honeycrisp #117's now-hardened but still separately-unsafe NEON loop) and makes the invariant testable without cross-compiling; 3 tests added (file had zero direct coverage of this function before), confirmed panicking under `--release`; `cargo test --lib` green (41, 3 new); `cargo check --tests` independently needs `--features bevy-plugin` for three integration tests, pre-existing per rows 108/118; `mir/gpu.rs`'s own debug_assert not yet audited; PR open: [mir#5](https://github.com/cyberia-to/mir/pull/5) — enforce csr_matvec_set length invariants in release builds | 2026-09-30 |
| 155 | [[prysm]]'s `molecules::table::spawn` (cyb's rendered-table widget, "every rendered row") has direct test coverage for its row/cell parsing, not zero as row 106's own Remains flagged | prysm | open | found by a launch worker, lane C, direct follow-up to row 106/prysm#4's own Remains ("prysm itself has 0 tests ... a coverage slice over its pure logic is a natural follow-up"); extracted the header/data-row parsing loop out of `spawn()` (mixed with untestable `Commands`-driven spawning) into a pure `parse_table(rows: &[Chunk]) -> (Vec<String>, Vec<Vec<String>>)`, no behavior change; 7 tests added — empty table, headers-only, headers+data rows, a hand-built payload with data rows but no schema row, ragged rows preserved as-is, non-ASCII text round-trips, an unrelated chunk interleaved at the row level silently skipped; `cargo check --tests` and `cargo test` green (7, all new; prysm had 0 tests before, matching the component table baseline); this PR targets row 106/prysm#4's own unmerged branch, not main, since the workspace does not build clean independent of that PR's tape→tade path fix; PR open: [prysm#5](https://github.com/cyberia-to/prysm/pull/5) — cover table.rs's row/cell parsing with a pure function | 2026-10-09 |
| 158 | [[rune]]'s `sub_field` (Goldilocks field subtraction behind nox opcode 6, the default tree-walking `eval`/`eval_with_host` path every `.rune` script runs through) never underflows on an unreduced operand | rune | open | found by a launch worker, lane C: `atom_u64()` hands opcode 6 a raw u64 straight out of the noun with no guarantee it is `< GOLDILOCKS_PRIME`, and the prime sits only `2^32-2` below `u64::MAX`, so `a as u128 + PRIME as u128 - b as u128` underflowed for any `b > a + PRIME` — a real panic in debug (confirmed by temporarily reverting the fix and rerunning the regression tests, both fail with "attempt to subtract with overflow") and silent field corruption in release (this workspace has no overflow-checks override), violating the field-arithmetic doctrine (arithmetic.md: "field arithmetic, NEVER float"); `add_field`/`mul_field` don't share this shape since u128 add/mul can't underflow; fixed by reducing both operands mod `GOLDILOCKS_PRIME` before the subtraction, one canonical fix point; 2 regression tests added next to the existing `sub_wraps_around`, both confirmed panicking pre-fix and green post-fix in debug and `--release`; `cargo test -p rune-interp` green (61 lib + 18 integration; this repo's stale `cyber-tape`→`tade` path, row 78/rune#1, bumped locally only to unblock the workspace manifest, reverted before commit); PR open (retitled from #156, row collision with mir#5): [rune#3](https://github.com/cyberia-to/rune/pull/3) — fix sub_field u128 underflow on unreduced operands | 2026-10-09 |
| 157 | [[foculus]]'s `capacity_weighted_placement` (the DAS/erasure layer's shard-to-device assignment behind `put_file`, rows 23/34's replication floor) weights self and a declared-unlimited peer consistently, not as either an unbounded winner or a starved loser | foculus | open | found by a launch worker, lane A: self was weighted a literal `u64::MAX`, so against any real peer byte-capacity (added via `foculus node add-peer <id> <capacity>`, an ordinary byte count) self's share rounds to essentially every shard and peers get ~0 — shards are not actually replicated off this node despite the function's name, undermining the availability properties this placement exists to serve; separately, a peer explicitly declared with capacity `0` (`format_bytes`'s and `parse_capacity`'s own convention for "unlimited") was weighted `.max(1)`, the smallest possible share, the opposite of "unlimited"; both now resolve to the largest known finite capacity among the other devices — generous, not unbounded or starved; extracted into a new `placement` module outside the `net` feature gate so it is directly testable (`node.rs` itself does not currently compile under `--features net`, an unrelated ed25519-dalek/iroh break, row 58/foculus#19); 8 tests added (file had none), `cargo check --tests` and `cargo test --lib` green (152, 8 new; this checkout's stale bbg/zheng/tape→tade pins, row 47/foculus#18, bumped locally only to verify, reverted before commit); PR open (retitled from #155, row collision with prysm#5): [foculus#28](https://github.com/cyberia-to/foculus/pull/28) — capacity-weighted placement no longer starves peers | 2026-10-09 |
| 159 | [[foculus]]'s `compute_params` and `format_bytes` (`node`'s deterministic (k,n) erasure shape and the byte-capacity display formatter row 157's "0 = unlimited" convention depends on) are covered by direct unit tests | foculus | open | found by a launch worker, lane A, same coverage-gap class as rows 39-41/53/54/57/70/71/88/96/102/104/107/112: both pure, zero prior tests, inside `node.rs` which does not currently compile under `--features net` independent of this change (row 58/foculus#19); extracted into a new `node_util` module outside the `net` gate, `SyncNode::compute_params` keeps its exact public signature, now forwarding; pinned (not fixed) one existing rounding-boundary quirk by test rather than guessing at it: `format_bytes(999_999_999)` prints "1000.0 MB", not "1.0 GB"; 5 tests added, `cargo check --tests` and `cargo test --lib` green (149, 5 new; this checkout's stale bbg/zheng/tape->tade pins, row 47/foculus#18, bumped locally only to verify, reverted before commit); PR open (retitled from #158, row collision with rune#3): [foculus#29](https://github.com/cyberia-to/foculus/pull/29) — cover compute_params/format_bytes | 2026-10-09 |
| 161 | [[tade]]'s CLI binary (`impl/rust/src/bin/tade.rs`, "the debugger for the wire" every durable log and gossiped message in the stack runs through) is covered by direct unit tests, same coverage-gap class as rows 39-41/53/54/57/70/71/88/96/102/104/107/112 | tade | open | found by a launch worker, lane C, first launch PR on tade's CLI specifically (row 97/tade#2 touched the library's `Reader::next_chunk`, not this binary): `sigil_byte`/`render_byte` (the `tade make` argument resolvers) and `preview` (the payload preview `tade inspect` prints) had zero direct tests; audited both for the non-ASCII byte-index slicing bug rows 50/56/58-68 etc. keep finding elsewhere — `s.len() == 1` is a byte-length guard and a single ASCII byte is always exactly one UTF-8 byte, so `s.as_bytes()[0]` cannot land mid-character here, confirmed not a bug rather than assumed; 13 tests added, no production code changed; `cargo test` green (39 total: 24 lib + 13 bin, 13 new + 2 nested_roundtrip.rs); PR open: [tade#3](https://github.com/cyberia-to/tade/pull/3) — cover sigil_byte/render_byte/preview in the tade CLI | 2026-10-07 |
| 162 | [[prysm]]'s `log` molecule (`molecules/rs/log.rs`, "every rendered row" of a structured log line in cyb) is covered by direct unit tests, same coverage-gap class as row 155's `table.rs` | prysm | open | found by a launch worker, lane C, direct follow-up to row 106/prysm#4's own Remains ("a coverage slice over its pure logic is a natural follow-up") on a different molecule than row 155; `level_style` and `kv_str` had zero direct tests; extracted the inline message assembly in `spawn()` into a pure `format_line(prefix, source, message)`, same shape as row 155's `parse_table` extraction, no behavior change; 8 tests added — both message-format branches, every level synonym, the info default including case-sensitivity, a present/missing key, a non-UTF-8 payload through `from_utf8_lossy` without panicking; `cargo test --lib` green (8, all new); this PR targets row 106/prysm#4's own unmerged branch, not main, for the same reason rows 148-150 targeted bostrom's unmerged branches — prysm does not build clean independent of that PR's tape→tade fix; PR open: [prysm#6](https://github.com/cyberia-to/prysm/pull/6) — cover log molecule's format_line/level_style/kv_str | 2026-10-07 |
| 163 | [[prysm]]'s `progress` molecule (`molecules/rs/progress.rs`, the live progress bar a phase-1 op streams `current`/`total` updates through) is covered by direct unit tests, same coverage-gap class as rows 155/162 | prysm | open | found by a launch worker, lane C, same run and class as row 162, a sibling molecule; `kv_u64` had zero direct tests, and the fill-fraction computation lived inline in `spawn()` mixed with Bevy `Commands` spawning; extracted into a pure `progress_percent(current, total) -> f32` — zero total returns 0 instead of dividing by zero, current > total clamps at 100 instead of overshooting the bar (the existing `.clamp` moved in, not changed); 10 tests added — zero total, zero current, midpoint, exact completion, the over-100% clamp, kv_str/kv_u64 present/missing, kv_u64 falling back to 0 on an unparseable payload; `cargo test --lib` green (10, all new); targets row 106/prysm#4's own unmerged branch, not main, same reason as row 162; independent of row 162/prysm#6, a sibling file on the same base branch; PR open: [prysm#7](https://github.com/cyberia-to/prysm/pull/7) — cover progress molecule's progress_percent/kv_str/kv_u64 | 2026-10-07 |
| 160 | [[tok]]'s `MintLedger::burn` (the conservation ledger's burn path, sibling to row 110's mint/burn edge-case sweep) never panics on a zero-amount burn from an untouched neuron/token pair | tok | open | found by a launch worker, lane A: `burn` reads a default balance of 0 via `.get(&key).copied().unwrap_or(0)` for a `(neuron, token)` pair never inserted into `balances`, then unconditionally calls `self.balances.get_mut(&key).unwrap()` one line later — for `amount == 0` the `have < amount` early-return guard (`0 < 0`) is false, so the `None` key reaches the unwrap and panics instead of completing as the no-op `mint_batch` already treats zero-amount legs as; row 110/plumb#7 added targeted burn edge-case tests (insufficient balance, untouched neuron with a *nonzero* amount) but not this zero-amount case, so the gap survived that sweep; reproduced the exact pre-fix panic (`ledger.rs:74:38, called Option::unwrap() on a None value`) against a throwaway test on origin's unfixed file before restoring the fix; fixed with an early `if amount == 0 { return Ok(()); }` guard matching `mint_batch`'s existing zero-skip semantics; `cargo test` green (9, 1 new); PR open: [plumb#11](https://github.com/cyberia-to/plumb/pull/11) — burn(0) on an untouched neuron/token pair no longer panics | 2026-10-07 |
| 164 | [[tru]]'s three tri-kernel operators (`focusing/operators.rs`'s `diffusion_step`, `springs_step`, `heat_step`, `normalize_l1` — the code that runs properties 1/2, φ* existence/uniqueness/convergence and Σφ*=1) are covered by direct unit tests, not only indirect exercise through `focusing.rs`'s integration test | tru | open | found by a launch worker, lane A: zero direct tests on the file that runs the value oracle every other core reads from; 6 tests added, two as exact fixed-point identities rather than approximate checks — `diffusion_step` at `alpha=1` must equal the teleport vector exactly regardless of the graph (the `(1-alpha)` term zeroes out entirely), and `heat_step` at `tau=0` must be the identity, hand-derived from the Chebyshev/Bessel coefficient formula (`s=0` collapses `cheb_coeffs` to `c_0=1`, `c_{k>0}=0`, so `H_0·φ=φ`) — a real correctness check on the Bessel-series and Chebyshev-recurrence code together, not just "it runs"; plus `heat_step`'s documented `lambda_max=0` no-op branch, `springs_step` on an isolated node, and `normalize_l1`'s sum-to-one and zero-vector branches; no production code changed; `cargo test` green (90 lib, 6 new); PR open: [tru#21](https://github.com/cyberia-to/tru/pull/21) — cover the three tri-kernel operators directly | 2026-10-07 |
| 165 | [[neuron]] model's `Content::validate` non-canonical pair-limb guard (the sole defense against a forged 64-byte pair node hashing identically to its canonical form under `nox::encode::particle_of`'s silent limb canonicalization) is covered by direct unit tests | neuron | open | found by a launch worker, lane B: `particle_of`'s SIZE_PAIR branch (`nox/rs/encode.rs`) canonicalizes each 32-byte half's four Goldilocks limbs (`digest_from_bytes`) before hashing, so an out-of-range limb (raw >= p) would otherwise be a second, malleable encoding of the same particle; `data.rs`'s `Content::validate` guards this ahead of the id check itself, but the branch had zero test coverage in either direction; 2 tests added — a non-canonical limb rejected regardless of claimed id, and the boundary value p-1 validating cleanly against its real id; no production code changed; this repo's default checkout does not build independent of row 127/[neuron#1](https://github.com/cyberia-to/neuron/pull/1)'s missing `neuron-id` crate — added it locally only to unblock resolution, reverted before commit; `cargo test -p cell-model` green (4, 2 new); PR open (retitled from #160, row collision with plumb#11): [neuron#2](https://github.com/cyberia-to/neuron/pull/2) — cover Content::validate's non-canonical pair-limb rejection | 2026-10-09 |
| 166 | [[nox]]'s `fri_fold_jet` (the FRI polynomial-folding jet behind the protocol's own proof-native programs) rejects an out-of-range fold-depth `k` before shifting, instead of trusting `1usize << k` on a value straight from the calling program's atom | nox | open | found by a launch worker, lane B/A boundary, same bug class as row 124/[nox#7](https://github.com/cyberia-to/nox/pull/7) (`ntt_jet`/`poly_eval_jet`) but in a third jet that PR didn't touch; `k` comes from `reduction.atom_value(k_id)` with no bound check before `1usize << k` — `k >= 64` panics unconditionally in debug ("attempt to shift left with overflow"), reproduced deterministically pre-fix; `fri_fold` has one CPU implementation shared by both the portable and honeycrisp jet registries, so one fix covers both; fixed by rejecting `k >= usize::BITS` before the shift, the identical guard row 124 established; 2 tests added — the exact pre-fix panic shape now rejects cleanly, and a large-but-in-range k still falls through to the existing tree-length check; `cargo test -p cyber-nox` green (163) and `--features brakedown` green (166); this checkout's default `cyber-lens-brakedown` 0.1 pin (row 123/[nox#6](https://github.com/cyberia-to/nox/pull/6), not yet merged) bumped locally only to verify, reverted before commit; PR open: [nox#8](https://github.com/cyberia-to/nox/pull/8) — reject fri_fold's k before it can shift out of range | 2026-10-09 |
| 167 | [[honeycrisp]]'s `aruminium::ffi::{retain_nonnull, release_nonnull}` (the ObjC retain/release calls every GPU command-encoder/buffer `Drop` in aruminium goes through, glia's accelerator, a lane C dependency of cyb's soma) enforce their non-null invariant in release builds, not only debug | honeycrisp | open | found by a launch worker, lane C, same class as rows 77/80/81/82/85/86/90/93/99/108/111/114/115/117/118/120/125/126/128/130/131/139 but sitting directly in front of an ObjC runtime call: `debug_assert!` compiled out in release, so a violated "known-non-null" precondition calls `objc_retain`/`objc_release` on a null/garbage pointer instead of failing at the boundary — reached from three `Drop` impls (`encoder.rs`, `command.rs`, `render/encoder.rs`) releasing GPU objects, exactly the state a `Drop` path can reach if an earlier bug leaves `self.raw` null; promoted both to `assert!` with named messages; 2 regression tests added, confirmed panicking under `--release`; `cargo test --lib -- ffi::tests::` and `--release` both green (2 new), full `cargo test --lib -p aruminium` green (52); this checkout's `cyb-nebu`→`strata-nebu` pin (row 111/[honeycrisp#1](https://github.com/cyberia-to/honeycrisp/pull/1), not yet merged) bumped locally only to verify, reverted before commit; no production caller currently passes null, closes the gap pre-emptively; PR open: [honeycrisp#10](https://github.com/cyberia-to/honeycrisp/pull/10) — enforce retain_nonnull/release_nonnull's null check in release | 2026-10-09 |
| 171 | [[prysm]]'s `error` molecule (`molecules/rs/error.rs`, "every rendered row" of an error/warning toast in cyb) is covered by direct unit tests, same coverage-gap class as rows 106/155/162/163 | prysm | open | found by a launch worker, lane C: `error.rs` is a sibling of row 162's `log.rs`/row 163's `progress.rs` in the same molecules directory, same shape — pure prefix/color dispatch and a `kv_str` payload reader mixed inline with untestable `Commands`-driven spawning, zero prior tests; extracted the two separate inline `match level.as_str()` blocks computing the same prefix/color partition into one pure `error_style(level: &str) -> (&'static str, Color)`, matching `log.rs`'s existing `level_style` shape, no behavior change; 6 tests added — the default/unknown-level fallback to the "✕" error style, both `"warn"`/`"warning"` synonyms mapping to the "⚠" style, `kv_str` reading a present key, falling back on a missing key, never panicking on a non-UTF-8 payload; targets row 106/[prysm#4](https://github.com/cyberia-to/prysm/pull/4)'s own unmerged branch, not main, same reason as rows 155/162/163 — the workspace does not build clean independent of that PR's tape→tade path fix; `cargo test --lib -- error::` and full `cargo test --lib` both green (6, all new; 0 before, matching the component-table baseline); PR open (retitled from #168, row collision with strata#6): [prysm#8](https://github.com/cyberia-to/prysm/pull/8) — cover error molecule's error_style/kv_str | 2026-10-09 |
| 177 | [[prysm]]'s `status` molecule (`molecules/rs/status.rs`, a sibling of rows 106/155/162/163/171's other molecules) is covered by direct unit tests | prysm | open | found by a launch worker, lane C: `status.rs` had zero prior tests, same class as `log.rs`/`progress.rs`/`error.rs`; extracted `status_code`/`status_color` out of `spawn()`, no behavior change; 6 tests added; targets row 106/[prysm#4](https://github.com/cyberia-to/prysm/pull/4)'s own unmerged branch, not main, same reason as rows 155/162/163/171; `cargo test` green (6, all new; 0 before); PR open: [prysm#9](https://github.com/cyberia-to/prysm/pull/9) — cover status molecule's status_code/status_color | 2026-10-09 |
| 176 | radio's `cyber-bao::io::mixed::traverse_ranges_validated` (the server-side streaming encode path behind property 22) is covered by direct unit tests, not only its client-side sibling `io::fsm` | radio | open | found by a launch worker, lane C: `io/mixed.rs` had zero direct tests despite every other `io/` module in the crate having 5-14; 2 tests added — the happy path (Size announced first, leaves reassemble the original bytes, Done last) and a root-only corruption case confirming a hash mismatch surfaces as an `EncodedItem::Error` sent to the peer, not a `Result::Err` from the function itself, previously unexercised; `cargo test -p cyber-bao` green (69, 2 new); test-only, no production code changed; PR open: [radio#12](https://github.com/cyberia-to/radio/pull/12) — cover mixed.rs's server-side traverse_ranges_validated | 2026-10-09 |
| 173 | [[spark]] (cyb's opener, lane C) resolves and opens the PDF/video/audio `Kind`s file#1 (row 16) adds, not only text/image | spark, file | open | found by a launch worker, lane C: file#1 (row 16, open) adds `Kind::Pdf`/`Kind::VideoMp4`/`Kind::VideoWebm`/`Kind::AudioMp3`/`Kind::AudioWav`/`Kind::AudioOgg`/`Kind::AudioFlac` to the sniffer but spark's `resolve()` match was exhaustive over the old `Kind` set only, so a merge of file#1 would break spark's build; added `SparkId::Document`/`SparkId::Media` and `Surface::Document`/`Surface::Media` (kind + raw bytes, same shape as the existing `Image` branch, no decoding — that's the calendar's separate PDF-reader/media-render slice); built and tested against file#1's own branch as the sibling `../file`, `cargo test` green (10, 7 new); spark's own `cargo check` against file's current `main` stays red until file#1 merges, same cross-repo ordering as row 127; PR open: [spark#3](https://github.com/cyberia-to/spark/pull/3) — spark opens PDF/video/audio as Document/Media surfaces | 2026-10-09 |
| 172 | [[foculus]]'s `rewards::fx_weight` (the proportional-split weight the settle-mint budget allocator divides `budget` by, core 1) computes in fixed point, not `f64` | foculus | open | found by a launch worker, lane A, arithmetic.md doctrine sweep, same class as row 37 (tok `conservation.rs`'s `fx_to_tokens`/`fx_weight`) but a separate, previously unflagged occurrence: `fx_weight` fed `Fx` through `to_f64()` then `* 1_000_000_000_000.0`, a computation step `Fx::to_f64`'s own doc comment says it must never be ("display and offline analysis ONLY"); rewritten to read `x.raw().as_u64()` — the canonical scaled field element `round(x · 2^32)` `Fx` already carries — directly, no float round-trip; 2 tests added (`fx_weight_matches_raw_scaled_integer`, `allocate_budget_splits_by_raw_fixed_point_weight`), `cargo test --lib` green (146, 2 new); this checkout's tape→tade/bbg/zheng pin drift (row 47/[foculus#18](https://github.com/cyberia-to/foculus/pull/18), not yet merged) bumped locally only to verify, reverted before commit; PR open: [foculus#30](https://github.com/cyberia-to/foculus/pull/30) — compute fx_weight from raw fixed point, not f64 | 2026-10-09 |
| 174 | [[mir]]'s `wgpu_arm::Encoder::bind_buffer` (the graph world's non-Apple GPU backend bind-group entry point) enforces its whole-buffer-offset invariant in release builds, not only debug | mir | open | found by a launch worker, lane C (picked up after lane B/A registry rows were all claimed by open PRs): `bind_buffer` never actually honors `offset` — `dispatch()` always calls `buffer.as_entire_binding()` — so the `debug_assert_eq!(offset, 0, ...)` was the only guard against a caller passing a nonzero offset expecting a partial binding; compiled out in release, a nonzero offset would silently bind the whole buffer instead, a silent wrong GPU-compute result, same class as rows 77/80/81/82/85/86/90/93/99/108/118/120/125/126/128/130/131/139/151; flagged but explicitly left unfixed by row 151/mir#4 ("a lower-priority API-contract check ... flagged, not fixed") and independently flagged again by row 156/mir#5 ("mir/gpu.rs's own debug_assert not yet audited") — this closes that dangling flag; same extraction shape as mir#4's `assert_f32_aligned`: a free `assert_whole_buffer_offset(offset)` outside the `#[cfg(not(target_vendor = "apple"))]` gate, unit-testable on Apple hardware; every current call site (`frame/paint.rs`, `frame/cull.rs`) already passes 0, closes the gap pre-emptively; `cargo test --lib` green (40, 2 new) and `--release` green; PR open: [mir#6](https://github.com/cyberia-to/mir/pull/6) — enforce bind_buffer's whole-buffer-offset invariant in release | 2026-10-09 |
| 178 | [[foculus]]'s `net` feature (the settle-gossip radio daemon, lane A's own "foculus network") builds from a clean checkout | foculus | open | found by a launch worker, lane A: row 58/foculus#19 hit a `net`-feature compile break and left it "not investigated further"; root cause pinned down — `iroh`/`iroh-base` 0.96.1 (upstream, crates.io) both pin `ed25519-dalek = "=3.0.0-pre.1"` exactly, a pre-release with a real bug in its own `pkcs8` feature (`signing.rs:714/717`); `ed25519-dalek` 3.0.0 stable fixes it but the exact upstream pin blocks both `cargo update --precise` and `[patch.crates-io]` (patches need a different source than the one patched) from reaching it; confirmed `iroh` 1.2.0 resolves `ed25519-dalek` to the fixed 3.0.0, but 1.x removed/renamed the `address-lookup-mdns` feature `net` depends on for `SettleRadio`/`SyncNode` — a real dependency-major-version migration, not a pin bump, named as the next slice; audit-only, no source or manifest change; PR open: [foculus#31](https://github.com/cyberia-to/foculus/pull/31) — audit: net-feature build break is iroh 0.96's ed25519-dalek pin | 2026-10-23 |
| 179 | [[glia]]'s GGUF loader (`import/loader/gguf.rs`, the model importer behind soma's local inference, lane C) rejects an oversized `tensor_count`/`n_dims` instead of allocating from it | glia | open | found by a launch worker, lane C: `parse_gguf_header` read `tensor_count` (u64, file-header-controlled) and each tensor's `n_dims` (u32) straight into `Vec::with_capacity` with no bound, the one gap in a file that already caps every other untrusted length before allocating — `read_gguf_string` rejects over 64 MiB, the array value reader rejects `arr_len` over 100M and caps its `with_capacity` at 1M; a malformed `.gguf` file panicked on capacity overflow instead of returning the normal parse `Result::Err`; fixed by rejecting `tensor_count > 10_000_000` and `n_dims > 8` (ggml's own `GGML_MAX_DIMS` is 4) before allocating, same reject-then-cap shape as the sibling readers; 3 tests added — oversized tensor_count, oversized n_dims, a well-formed entry still parses — this module had zero prior tests; `cargo test -p import --lib -- gguf::` green (3, all new), full `cargo test -p import` green (6 lib + 15 integration); PR open: [glia#6](https://github.com/cyberia-to/glia/pull/6) — bound GGUF tensor_count/n_dims before allocating | 2026-10-09 |
| 180 | [[bbg]]-cli's `deserialize_signal` (`cli/src/frame.rs`, the store-log decoder for the wire format signals gossip over radio will carry, row 21) rejects an oversized `n_links`/`n_moves` instead of allocating from it | bbg | open | found by a launch worker, lane C, same bug class as row 179: `n_links`/`n_moves` (each `u32`, wire-controlled) fed `Vec::with_capacity` with no bound in `decode_events`'s signal-frame path — a truncated or adversarial frame declaring a huge count triggers a multi-hundred-GB allocation attempt, an allocator-failure abort rather than the `None` every other malformed-frame case in the file already returns; fixed by bounding each count against how many records the remaining payload bytes can actually hold (`LINK_BYTES = 105`, `MOVE_MIN_BYTES = 33`, both derived from the frame catalog's own documented wire shapes) before allocating; 4 tests added — oversized n_links, n_links exceeding the records remaining, oversized n_moves, a well-formed signal with a link and a box move round-trips; `cargo test --tests -- frame::` (cli) green (7, 4 new), full `cargo test` (cli) green (7); this repo's default checkout does not build independent of row 44/bbg#12's tape→tade and lens/nox/zheng pin gap — bumped locally only to verify, reverted before commit, this PR touches only `cli/src/frame.rs`; PR open: [bbg#20](https://github.com/cyberia-to/bbg/pull/20) — bound deserialize_signal's n_links/n_moves before allocating | 2026-10-09 |
| 181 | [[lens]] CLI's opening-artifact reader (`cli/src/artifact.rs::decode`, the wire format `open`/`verify` exchange between machines and CI, lane B releases) rejects an oversized count instead of allocating from it | lens | open | found by a launch worker, lane B, same bug class as rows 179/180: `n_points`, `round_commitments`, `query_responses` and `merkle_paths`/path-hash counts (each a wire/file-controlled `u32`) fed `Vec::with_capacity` with no bound before a single element was read — a corrupted `.artifact` file declaring a count near `u32::MAX` drives an allocation-abort DoS instead of the `"artifact truncated"` error the per-element read already returns one line later; fixed by adding `Reader::remaining()` and capping every `with_capacity` at `n.min(r.remaining())`, safe because every encoded element consumes at least one byte; 2 tests added — an oversized `n_points` and an oversized commitments count inside a Tensor opening, both hand-built minimal malformed artifacts; `cargo test -p lens-cli` green (10, 2 new); full-workspace `cargo test` has one pre-existing, unrelated failure (`cyber-lens-porphyry::tests::batch_roundtrip`, a `genies` field-width panic), confirmed present with this PR's two files reverted; PR open: [lens#9](https://github.com/cyberia-to/lens/pull/9) — cap artifact reader's untrusted count preallocation | 2026-10-09 |
| 182 | [[glia]]'s `LlamaConfig::parse` (`run/arch/decoder/config.rs`, the model-loader config gate, lane C) rejects an oversized `num_hidden_layers` instead of allocating from it | glia | open | found by a launch worker, lane C, same bug class as rows 179/180/181, a second occurrence in the same repo as row 179: `num_hidden_layers` (parsed from `config.toml`'s `[architecture]` section as a raw `i64 as usize`) was only checked nonzero — the parser itself then builds `vec![LayerKind::Sliding; num_hidden_layers]` a few lines later when `layer_types` isn't set explicitly (the common case), and every per-layer `Vec::with_capacity` downstream in the loader repeats the same trust; a corrupted or adversarial `config.toml` with a huge declared layer count triggers a multi-terabyte allocation attempt inside `parse()` itself, before any tensor is touched, and aborts the process instead of returning `FormatError::Invalid`; fixed by rejecting `num_hidden_layers > 4096` (no real model comes close) next to the existing `== 0` check, closing the gap where the untrusted value enters the type rather than patching each downstream site; 3 tests added — a well-formed config still parses, an oversized count is rejected, the pre-existing zero rejection still holds — this module had zero prior tests; `cargo test -p run --lib -- arch::decoder::config::` green (3, all new), full `cargo test -p run --lib` green (92, 3 new); PR open: [glia#7](https://github.com/cyberia-to/glia/pull/7) — bound num_hidden_layers before allocating from it | 2026-10-09 |
| 183 | [[hemera]]'s verified-streaming `decode` (`rs/src/stream.rs`, the primitive row 22's radio blob fetch by particle depends on) rejects an inflated header `data_len` before it drives allocation | hemera | open | found by a launch worker, lane B, same bug class as rows 179/180/181/182: the multi-chunk (`n > 1`) path called `Vec::with_capacity(data_len)` where `data_len` comes straight from the untrusted 8-byte header, with no check that the actual buffer could hold that many bytes — the single-chunk path three lines below already had exactly this check, it was just never hoisted above the split; since the stream embeds every data byte directly, a well-formed encoding can never be shorter than `HEADER_SIZE + data_len`, so a tampered header claiming a length near `u64::MAX` on a small real buffer drives an allocation-abort DoS instead of `DecodeError::Truncated`; fixed by hoisting the check above the split, using `encoded.len().saturating_sub(HEADER_SIZE) < data_len` so the comparison itself cannot overflow; 2 tests added — a moderately-inflated header and a near-`u64::MAX` header, both on an otherwise-valid 2-chunk encoding; `cargo test --lib -- stream::` green (20, 2 new), full `cargo test` green (249 + 6 vectors + 1 doctest), `cargo test --release --lib -- stream::` green; PR open: [hemera#6](https://github.com/cyberia-to/hemera/pull/6) — reject inflated data_len before decode's allocation | 2026-10-09 |
| 185 | [[prysm]]'s `action` molecule (`molecules/rs/action.rs`, cyb's clickable-action widget, "every rendered row") is covered by direct unit tests, same coverage-gap class as rows 106/155/162/163/171/177's other molecules | prysm | open | found by a launch worker, lane C: `spawn()`'s inline `(label, target_ref)` extraction out of the chunk's nested `(~, t)`/`(#, t)` children had zero test coverage, mixed with untestable `Commands`-driven spawning; extracted into a pure `parse_action(chunk: &Chunk) -> (String, String)`, no behavior change; 8 tests added — label+target from SIG/HAX children, missing/empty-label fallback to `"action"`, missing target defaults to empty string, no children at all, unrelated sigil/render children ignored, non-UTF-8 payload lossy-decodes without panicking, last-matching-child-wins on duplicate SIG children; `cargo test` green (8, all new; 0 before, matching the component-table baseline); targets row 106/prysm#4's own unmerged branch, not main, same reason as rows 155/162/163/171/177 — this repo's main still imports `tape::`, not `tade::`; PR open: [prysm#10](https://github.com/cyberia-to/prysm/pull/10) — cover action molecule's parse_action label/target decode | 2026-10-09 |
| 186 | [[prysm]]'s `neuron` molecule (`molecules/rs/neuron.rs`, cyb's `@handle` widget for a signed neuron identity) is covered by direct unit tests, same coverage-gap class as row 185 | prysm | open | found by a launch worker, lane C, same run as row 185: `spawn()`'s inline `@`-prefix normalization had zero test coverage; extracted into a pure `format_neuron_name(payload: &[u8]) -> String`, no behavior change; 6 tests added — prepends `@` when missing, passes through when already prefixed, empty payload becomes bare `@`, a lone `@` payload passes through, non-UTF-8 payload lossy-decodes without panicking (both unprefixed and already-prefixed); `cargo test` green (6, all new); targets row 106/prysm#4's own unmerged branch, not main, same reason as row 185; PR open: [prysm#11](https://github.com/cyberia-to/prysm/pull/11) — cover neuron molecule's format_neuron_name @-prefix handling | 2026-10-09 |
| 188 | [[prysm]]'s `input` molecule (`molecules/rs/input.rs`, cyb's focused text-entry widget) is covered by direct unit tests, same coverage-gap class as rows 106/155/162/163/171/177/185/186's other molecules | prysm | open | found by a launch worker, lane C, closing this repo's molecule-coverage sweep: `text_input_system`'s per-keystroke logic (backspace pops, enter is a no-op, echoed text appends only non-control characters) and its caret-suffixed display-line computation (`value|` vs `placeholder|`) were both inline in an ECS system with zero test coverage; extracted into pure `apply_key(value: &mut String, key: &Key, text: Option<&str>)` and `display_text(value: &str, placeholder: &str) -> String`, no behavior change; 8 tests added — backspace pops/no-ops on empty, enter is a no-op even with echoed text, echoed text appends non-control characters, control characters dropped, an unrelated key with no echoed text is a no-op, display_text's placeholder vs value branches; `cargo test` green (8, all new); targets row 106/prysm#4's own unmerged branch, not main, same reason as rows 155/162/163/171/177/185/186; component.rs/button.rs/mind.rs surveyed and left alone — pure Bevy spawn wiring with no branching logic worth extracting; a separate uncommitted particle_card.rs/file.rs pair exists only in the owner's local feat/cli branch, out of scope; PR open: [prysm#12](https://github.com/cyberia-to/prysm/pull/12) — cover input molecule's apply_key/display_text logic | 2026-10-09 |
| 184 | [[zheng]]'s `phi::trikernel::prove_phi_star` (core 1's φ* proof, the algebraic core of foculus's provable consensus) rejects an oversized `iterations` before it drives allocation | zheng | open | found by a launch worker, lane A, same bug class as rows 94/95/103/105/109/121/122/129/135/179/180/181/182/183: `Vec::with_capacity(iterations as usize)` preallocated from an unbounded caller-supplied `u32` before a single diffusion proof ran, an iterations value near `u32::MAX` drives a multi-exabyte allocation-abort instead of the `EmptyGraph`/`DimMismatch`-style `Err` the function already returns for other invalid inputs; no production caller yet — `foculus::finality_evidence.rs` names this as a planned integration point, not wired — closes the gap pre-emptively; fixed with a new `PhiError::TooManyIterations` and a `MAX_ITERATIONS = 10_000` cap (matching inf's `MAX_PR_ITERS`/`MAX_ITERS` precedent, rows 94/95) checked before the allocation; 2 regression tests added — `u32::MAX` and `MAX_ITERATIONS + 1` both reject, `0` iterations still succeeds as the cheap valid boundary; `cargo test -p zheng --lib -- phi::trikernel` green (4, 2 new); full-suite `cargo test -p zheng --lib` 135 passed/10 failed, all 10 the pre-existing lens 0.2 TensorMerkle gap row 46/zheng#22 already tracks, none new, none in `phi::` (this checkout's stale `cyber-tape` workspace member and lens/nox pins, row 46, bumped locally only to verify, reverted before commit); PR open: [zheng#30](https://github.com/cyberia-to/zheng/pull/30) — bound prove_phi_star's iterations before allocation | 2026-10-09 |
| 187 | [[nox]]'s `merkle_verify` jet charges budget from the declared `depth` (`depth × 25`, one hash per step) and actually hashes once per entry of a separately-decoded `path` list, with nothing binding the two together | nox | open | found by a launch worker, lane A: the calling convention's own doc comment defines `depth` as "number of path steps remaining", but `decode_path` walked the `path` cons-list independently of `depth`, and `verify_path` hashed every real entry regardless — a caller declaring a small `depth` (cheap budget charge) could supply a longer, genuinely valid `path` and the jet still verified every real step, doing more metered work than it was charged for; a metering bypass in the cost model the rest of the protocol's provable execution relies on, not a decode-time DoS (the same bug class as rows 94/95/103/105/109/121/122/129/135/179-184, but on the charged-vs-performed-work axis rather than allocation size); fixed with `if path.len() as u64 != depth { return Outcome::Error(ErrorKind::Malformed) }` between decode and verify; 3 regression tests added on a new 4-leaf-tree fixture — a genuine 2-step path at matching `depth=2` still verifies, the same path under-declared as `depth=1` (the bypass shape) is rejected, a 1-step path over-declared as `depth=2` is rejected; `cargo test -p cyber-nox --lib -- jets::merkle_verify` green (7, 3 new), full `cargo test -p cyber-nox --lib` green (164) and `--features brakedown` green (167); this checkout's `cyber-lens-brakedown` 0.1→0.2 pin (row 123/nox#6, not yet merged) bumped locally only to verify, reverted before commit; PR open: [nox#9](https://github.com/cyberia-to/nox/pull/9) — merkle_verify jet rejects a path length mismatched to depth | 2026-10-09 |

### calendar

| week | lane A · core | lane B · body | lane C · content |
|---|---|---|---|
| 1 · to 09-25 | registry agreed ✓; privacy P1–P3 designed → audits found the gaps instead (rows 10–12, 29), design slipping; property 3 measured ✓ in PR | particle definition frozen (19) slipping: decision open; genesis pipeline one node: not started, replay contract in PR (14) | availability map in PR (cyb#1392); radio into cyb: not started, gossip proven multi-hop (radio#5) |
| 2 · to 10-02 | settlement end to end on one node; ICBS positions and serum reports on links (25) | three nodes, three machines, radio gossip (21), re-addressing run (20) | media render in spark; blob fetch by particle (22) |
| 3 · to 10-09 | fold tree to decide and mint; serum scoring implemented (24); a neuron roots its own book (30); spec freeze of the core | claim by old key in cyb and on bostrom.network | PDF reader in spark |
| 4 · to 10-16 | privacy P1–P3 and valence (29) in code; two axes and allocation curve (26, 27); domain finality and the referral (31, 32); properties 4, 6, 7, 8 | network gate with conflicts and partition | link → files → links navigation |
| 5 · to 10-23 | the core on three nodes from real genesis; feature freeze | release train: cyb, node, true-cyber, six builds | property 16 |
| 6 · to 10-30 | red team fixes; registry closed or deferred | genesis candidates of both chains; runbook, alerts | public rehearsal on pussy-rc |
| 7 · to 11-05 | freeze | ceremony: bostrom genesis at 13:22:42 UTC, five years to the second after bostrom block 1 (2021-11-05T13:22:42Z); pussy at 13:37 UTC | two chains live, cyb reads them |

freezes: core specs 2026-10-09 · features 2026-10-23 · genesis candidates 2026-10-28 · public rehearsal 2026-10-30 → 11-02 · launch 2026-11-05.

### controls

- this page is the tracker. status changes, decisions and deferrals are edits here, dated; evidence lives in the owning repository's `audit/`.
- daily loop: every morning the gates run, the registry and the calendar are updated here, red items go to the top; every evening the [chronicle beat](https://cyberia.blog) carries the day.
- gates in CI: cyb fleet (three bodies) for the body; the network gate (three nodes, three machines, real genesis, conflicts, partition) for the core, run daily; the release train every friday by the trisha pattern: branches, six native builds, cross-platform verification, receipts.
- red team: every week the same package (core specs, code, this registry) goes to four models on top subscriptions, Grok, Kimi, Claude and GPT, with one task: break it. findings land in `audit/security/<model>-<date>.md`; a real finding becomes a test in attack_vectors.rs before it is called fixed.
- staging: spacepussy-test (live) → pussy-rc with real genesis on three nodes → pussy and bostrom on 2026-11-05.
- plan B, decided in advance: if the fold tree does not deliver an O(1) accumulator on three nodes by 2026-10-23, phase 1 launches with settlement and a per-cluster ticket cap, the DoS risk accepted on the canary, and fold ships as the first upgrade. no other core has a plan B.

### decisions log

| date | decision |
|---|---|
| 2026-09-18 | phase 1 dated 2026-11-05; both chains the same day, minutes apart |
| 2026-09-18 | ceremony time: bostrom 13:22:42 UTC (block 1 anniversary to the second), pussy 13:37 UTC. Berlin 14:22 · Moscow 16:22 · Bali 21:22 · Beijing 21:22 · New York 08:22 · San Francisco 05:22 |
| 2026-09-18 | the four cores are non-negotiable; programmability, uhash, fees, $CYB are out of phase 1 |
| 2026-09-18 | genesis stake bonded uniformly from the snapshot, resource tokens counted |
| 2026-09-18 | privacy P1–P3 all required at genesis |
| 2026-09-18 | content availability and render in cyb is a phase-1 core requirement |
| 2026-09-18 | personal chains are a core: every neuron roots a home book with its own token; the referral pays the referrer a share of $ν at birth and pro-rata fees of the book |
| open | the referral share r; transferability of $ν; whether a referrer's referrer receives anything (default: no) |
| 2026-09-18 | hybrid economics is a core: truth markets (ICBS + serum + valence) and staking on two axes ship in phase 1 |
| 2026-09-18 | component table is the computed dependency closure; nu (cyb's console) is a local checkout without a remote and must get one before the release train |
| 2026-09-18 | second scan: file, radio, re-addressing, file store, inf, cybernode added as phase-1 dependencies |
| open | which particle definition wins: hemera(data) 32 bytes, or hemera over the lens commitment — blocks row 20; survey in cybergraph#4 |
| open | privacy at genesis with the current wire: the signal frame carries link content in the clear (zheng#21) and tickets carry the marginal vector (foculus#12); either zheng private execution lands in phase 1 or P1/P3 are redefined |
| 2026-09-22 | consolidated state: the root chain of each token holds the public graph, the union of every published edge with hidden authorship, and that graph is the collective brain φ* and inference run on; a personal book holds the private side of the same edges (author, positions, balances) and publishes edges to the root by registration. a book that publishes nothing contributes nothing and earns nothing. this closes the gap between one public head and many private books; the wire and ticket leaks (rows 10, 12, 29) are about the private side, not the edges |
| 2026-09-22 | who serves the public graph: every full node holds and serves the whole root state; a partial node (cyb) holds its namespaces plus completeness proofs; a light client holds the tip root and opens against it; availability is replication over radio plus DAS sampling with erasure coding (foculus vec.md), with the burial file store on cybernode as the seed; three full nodes on three machines are the phase-1 floor (row 34) |
| 2026-09-22 | clusters are not chosen by miners: an epoch's claims partition canonically into components of overlapping ε-supports; a miner chooses which cluster to sample; difficulty per cluster equalizes pay per second (rows 4, 35); the partition is specified and not implemented |
| 2026-09-22 | hardware profile of settlement mining, for the community: a ticket is a tri-kernel recompute on an ε-support (sparse matrix–vector passes over Goldilocks) plus a zheng proof (sumcheck over multilinear tables, Brakedown linear-code commitment, Poseidon2 hashing); the recompute is memory-latency bound on random graph access, the proof is bandwidth bound with a hashing compute tail; no NTT, no dense matmul; unified-memory machines with fast random access (Apple Silicon) sit near the optimum and a GPU gains little; the ASIC endgame is a Goldilocks field processor with fma, p2r and lut, which serves the tri-kernel itself and is welcome; recorded as the phase-1 claim to be measured on row 3 |
| open | merge policy for the 30 worker PRs: the owner merges, or the workers may merge green PRs into a `launch` integration branch per repo |
| open | identity of the 2.37% of bostrom files without bytes |
| open | 8.97T BOOT on the passport contract |
| open | the bond formula for milliampere and millivolt |

## phase 2 · canary

90 days from launch. the two chains under real use, the weekly red team running, and the registry still the tracker. what phase 2 must show: neurons join, stay and create focus; every economic invariant holds; no critical bug under attack. upgrades land in this order: fold if plan B was taken, then programmability (nox programs, joy, trident contracts), then the fee role. the parameter change procedure of foculus is written and exercised once on pussy before it is needed on bostrom.

## phase 3 · $CYB mainnet

the launch that must be correct on the first try. it needs what the canary cannot give: independent implementations in several languages agreeing on state roots, and the formal spine.

| # | question | evidence |
|---|---|---|
| 1 | does φ* converge? | [[eidos]] proof of Lyapunov stability |
| 2 | can proofs be forged? | soundness proof + 10⁸ fuzzing runs, 0 counterexamples |
| 3 | can the economy be drained? | Nash equilibrium proof + 100× adversarial simulation |
| 4 | is computation deterministic? | cross-implementation state-root match on 10⁶ blocks |
| 5 | does it survive partial failure? | chaos test, zero safety violations |
| 6 | does it provide real utility? | the canary record |

the formal spine, running alongside phases 1 and 2 and converging here: layer 1 confluence ([[eidos]]), cost determinism, focus conservation, privacy soundness (< 2⁻¹²⁸), tri-kernel convergence with explicit constants, adversarial equilibrium, double-spend prevention. recursive proofs (the verifier as a nox program, light clients) and sharding with DAS belong here too.

$CYB genesis is separate from the two bootloader chains: a 187,416,084,623,451,570 $CYB starting balance for $C holders (≈ 1% of the Goldilocks field order), the rest along a power-law emission; [[bootloader/tokens/$BOOT|$BOOT]] and [[bootloader/tokens/$PUSSY|$PUSSY]] balances do not convert. the migrated graph is aligned to the [[cybics/crystal|crystal]], the 5,040-particle seed, under its 12 genesis invariants. supply, emission and the crystal keep their own pages: [[$CYB]], [[cybics/crystal|crystal]].

## token architecture

| type | fungible | movable | role | examples |
|---|---|---|---|---|
| [[coin]] | yes | yes | consensus, stake | [[bootloader/tokens/$BOOT|$BOOT]], [[bootloader/tokens/$PUSSY|$PUSSY]], later [[$CYB]] |
| [[card]] | no | yes | knowledge assets, provenance | authorship proofs |
| [[score]] | yes | no | reputation | [[cybics/crystal/karma|karma]] |
| [[badge]] | no | no | non-transferable credentials | achievements |

## the endgame

a living, self-optimizing knowledge network that learns from every form of input on Earth, holds coherence under interplanetary latency, evolves without central authority, maximizes the flourishing of the biosphere, and proves every claim. the network is thinking. no node comprehends. the network knows.

## work log

pull requests from the launch workers (`scripts/launch-hour.sh`, sonnet, launchd `to.cyberia.launch-hour`): three parallel slots on lanes A, B and C, a tick every 20 minutes, up to three PRs per run, one row per PR, conflicting open PRs merged with their base first. the owner merges; a merged row flips its registry property when the evidence is in.

| when (UTC) | property | repo | pull request | state |
|---|---|---|---|---|
| 2026-09-18T21:44:19Z | 13 | plumb (tok) | [launch #13: add transfer to close conservation's second mutation](https://github.com/cyberia-to/plumb/pull/1) | reviewed 2026-09-22 |
| 2026-09-19T00:04:00Z | 3 | foculus | [launch #3: measure ticket proof decide/verify cost](https://github.com/cyberia-to/foculus/pull/6) | reviewed 2026-09-22 |
| 2026-09-19T01:14:34Z | 5 | zheng | [launch #5: fold either order, both decide() successfully](https://github.com/cyberia-to/zheng/pull/20) | reviewed 2026-09-22 |
| 2026-09-19T02:19:46Z | 25 | tru | [launch #25: ICBS cost function and price derivatives](https://github.com/cyberia-to/tru/pull/8) | reviewed 2026-09-22 |
| 2026-09-19T03:34:00Z | 6 | foculus | [launch #6: attack tests for beacon VDF binding](https://github.com/cyberia-to/foculus/pull/7) | reviewed 2026-09-22 |
| 2026-09-19T04:45:00Z | 24 | tru | [launch #24: informed minority beats coordinated majority at scale](https://github.com/cyberia-to/tru/pull/9) | reviewed 2026-09-22 |
| 2026-09-19T05:10:00Z | 16 | file | [launch #16: sniff PDF, video and audio file kinds](https://github.com/cyberia-to/file/pull/1) | reviewed 2026-09-22 |
| 2026-09-19T05:35:00Z | 19 | cybergraph | [launch #19: survey particle type definitions across the four owners](https://github.com/cyberia-to/cybergraph/pull/4) | reviewed 2026-09-22 |
| 2026-09-19T05:55:00Z | 8 | foculus | [launch #8: MinHash agrees across n-way conflicts and arrival orders](https://github.com/cyberia-to/foculus/pull/8) | reviewed 2026-09-22 |
| 2026-09-19T04:57:02Z | 9 | foculus | [launch #9: prove convergence is order-independent of link arrival](https://github.com/cyberia-to/foculus/pull/9) | reviewed 2026-09-22 |
| 2026-09-19T05:00:52Z | 26 | plumb (tok) | [launch #26: stake-yield term pays active risk, zeros passive/Sybil](https://github.com/cyberia-to/plumb/pull/2) | reviewed 2026-09-22 |
| 2026-09-19T06:04:15Z | 18 | bbg | [launch #18: define WARM archival population and checkpoint boundary](https://github.com/cyberia-to/bbg/pull/10) | reviewed 2026-09-22 |
| 2026-09-19T06:05:00Z | 22 | radio | [launch #22: verified-streaming tests for the particle CLI](https://github.com/cyberia-to/radio/pull/4) | reviewed 2026-09-22 |
| 2026-09-19T06:07:21Z | 4 | foculus | [launch #4: band settlement target by cluster size](https://github.com/cyberia-to/foculus/pull/10) | reviewed 2026-09-22 |
| 2026-09-19T06:14:57Z | 7 | foculus | [launch #7: bound withholding bias by compute share](https://github.com/cyberia-to/foculus/pull/11) | reviewed 2026-09-22 |
| 2026-09-19T06:23:11Z | 14 | cybergraph | [launch #14: genesis replay bijection contract](https://github.com/cyberia-to/cybergraph/pull/5) | reviewed 2026-09-22 |
| 2026-09-19T06:26:00Z | 30 | plumb (tok) | [launch #30: neuron roots one home-book token in the ledger](https://github.com/cyberia-to/plumb/pull/3) | reviewed 2026-09-22 |
| 2026-09-19T06:35:00Z | 32 | plumb (tok) | [launch #32: referral birth-mint split, self-referral rejected](https://github.com/cyberia-to/plumb/pull/4) | reviewed 2026-09-22 |
| 2026-09-19T06:42:00Z | 12 | foculus | [launch #12: audit — settlement tickets leak the full per-contributor marginal vector](https://github.com/cyberia-to/foculus/pull/12) | reviewed 2026-09-22 |
| 2026-09-19T06:35:32Z | 31 | foculus | [launch #31: book-level domain finality for oikos foundation 2](https://github.com/cyberia-to/foculus/pull/13) | reviewed 2026-09-22 |
| 2026-09-21T15:38:19Z | 33 | plumb (tok) | [launch #33: conserve referral payout, zero for inactive/Sybil books](https://github.com/cyberia-to/plumb/pull/5) | reviewed 2026-09-22 |
| 2026-09-21T15:52:00Z | 20 | file | [launch #20: re-address bostrom CIDs to Hemera particles](https://github.com/cyberia-to/file/pull/2) | reviewed 2026-09-22 |
| 2026-09-21T16:10:00Z | 27 | tru | [launch #27: implement PoW/PoS allocation split and security floor](https://github.com/cyberia-to/tru/pull/10) | reviewed 2026-09-22 |
| 2026-09-21T16:32:00Z | 28 | tru | [launch #28: discrete per-epoch accrual for the yield-stream annuity](https://github.com/cyberia-to/tru/pull/11) | reviewed 2026-09-22 |
| 2026-09-21T15:40:00Z | 21 | radio | [launch #21: fix iroh-docs fork pins, prove gossip relays multi-hop](https://github.com/cyberia-to/radio/pull/5) | reviewed 2026-09-22 |
| 2026-09-21T16:52:00Z | 10 | mudra | [launch #10 (draft): trace P1 signal content-commitment gap](https://github.com/cyberia-to/mudra/pull/3) | draft |
| 2026-09-21T17:10:00Z | 11 | mudra | [launch #11 (draft): trace P2 stealth/veil transfer-privacy gap](https://github.com/cyberia-to/mudra/pull/4) | draft |
| 2026-09-21T15:55:09Z | 10 | zheng | [launch #10: audit — signal wire format leaks full link content](https://github.com/cyberia-to/zheng/pull/21) | open — duplicate: mudra#3 above also claims property 10, opened 54s earlier; owner to pick one |
| 2026-09-21T15:56:50Z | 29 | mudra | [launch #29: audit — valence leaks on the wire, collides with truth-scoring spec](https://github.com/cyberia-to/mudra/pull/5) | reviewed 2026-09-22 |
| 2026-09-21T15:58:21Z | 23 | bbg | [launch #23: audit — network fetch has no self-authentication check](https://github.com/cyberia-to/bbg/pull/11) | reviewed 2026-09-22 |
| 2026-09-21T16:43:00Z | 16 | cyb | [launch #16 (draft): particle availability audit module](https://github.com/cyberia-to/cyb/pull/1392) | draft |
| 2026-09-22T08:52:00Z | 34 | foculus | [launch #34: DAS availability decision treats a withheld shard as a failed sample](https://github.com/cyberia-to/foculus/pull/14) | reviewed 2026-09-22 |
| 2026-09-22T09:05:00Z | 35 | foculus | [launch #35: ε-support derivation and canonical cluster partition](https://github.com/cyberia-to/foculus/pull/15) | reviewed 2026-09-22 |
| 2026-09-22T08:41:00Z | 36 | foculus | [launch #36: measure settlement-mining hardware profile](https://github.com/cyberia-to/foculus/pull/16) | reviewed 2026-09-22 |
| 2026-09-22T11:05:00Z | 38 | mudra | [launch #38: bump cyber-nox/zheng pins so mudra builds from a clean checkout](https://github.com/cyberia-to/mudra/pull/6) | open |
| 2026-09-22T10:58:41Z | 37 | plumb (tok) | [launch #37: compute settle-mint conservation in fixed point](https://github.com/cyberia-to/plumb/pull/6) | open |
| 2026-09-22T12:15:00Z | 39 | soft3 | [launch #39: cover node genesis load/validate with tests](https://github.com/cyberia-to/soft3/pull/4) | open |
| 2026-09-22T12:40:00Z | 40 | soft3 | [launch #40: cover HTTP request identity derivation with tests](https://github.com/cyberia-to/soft3/pull/5) | open |
| 2026-09-22T13:05:00Z | 41 | soft3 | [launch #41: cover query parameter parsing with tests](https://github.com/cyberia-to/soft3/pull/6) | open |
| 2026-09-22T18:25:00Z | 42 | mudra | [launch #42: fix bbg_root sentinel so mudra builds with --features prove](https://github.com/cyberia-to/mudra/pull/7) | open |
| 2026-09-22T18:34:09Z | 43 | foculus | [launch #43: compute fold-mining k_min in fixed point, not f64](https://github.com/cyberia-to/foculus/pull/17) | open |
| 2026-09-22T18:37:13Z | 44 | bbg | [launch #44: bump lens/nox/zheng pins and tape->tade path so bbg builds clean](https://github.com/cyberia-to/bbg/pull/12) | open |
| 2026-09-22T18:53:00Z | 46 | zheng | [launch #46: zheng builds from a clean checkout; TensorMerkle gap found](https://github.com/cyberia-to/zheng/pull/22) | open |
| 2026-09-22T18:56:00Z | 47 | foculus | [launch #47: bump tape->tade path and bbg/zheng pins so foculus builds clean](https://github.com/cyberia-to/foculus/pull/18) | open |
| 2026-09-22T19:00:00Z | 48 | cybergraph | [launch #48 (draft): bump bbg/zheng/nox/lens pins; nox test migration still needed](https://github.com/cyberia-to/cybergraph/pull/6) | draft |
| 2026-09-22T18:50:18Z | 45 | soft3 | [launch #45: accept the launch chain set in genesis validation](https://github.com/cyberia-to/soft3/pull/7) | open |
| 2026-09-22T19:07:16Z | 50 | file | [launch #50: fix Particle::from_hex panic on non-ASCII input](https://github.com/cyberia-to/file/pull/3) | open |
| 2026-09-22T19:08:17Z | 49 | soft3 | [launch #49: genesis install subcommand adopts an external genesis file before first boot](https://github.com/cyberia-to/soft3/pull/8) | open |
| 2026-09-22T19:12:13Z | 52 | true-cyber | [launch #52 (draft): true-cyber does not build from a clean checkout](https://github.com/cyberia-to/true-cyber/pull/1) | draft |
| 2026-09-22T19:11:55Z | 53 | soft3 | [launch #53: cover key32/hash32 identity derivation with tests](https://github.com/cyberia-to/soft3/pull/9) | open |
| 2026-09-22T19:18:00Z | 54 | soft3 | [launch #54: cover read_request with tests](https://github.com/cyberia-to/soft3/pull/10) | open |
| 2026-09-22T19:34:00Z | 56 | mudra | [launch #56: reject non-ASCII hex in claim wire decode before slicing](https://github.com/cyberia-to/mudra/pull/8) | open |
| 2026-09-22T19:32:36Z | 55 | radio | [launch #55: iroh-bench builds against this workspace's iroh/quinn fork](https://github.com/cyberia-to/radio/pull/6) | open |
| 2026-09-22T19:38:00Z | 57 | soft3 | [launch #57: cover Node::open's legacy-migration guard](https://github.com/cyberia-to/soft3/pull/11) | open |
| 2026-09-22T19:52:00Z | 58 | foculus | [launch #58 (draft): reject non-ASCII hex in settle-net topic parser](https://github.com/cyberia-to/foculus/pull/19) | draft |
| 2026-09-22T19:58:00Z | 59 | radio | [launch #59: reject non-ASCII input before slicing](https://github.com/cyberia-to/radio/pull/7) | open |
| 2026-09-22T20:04:00Z | 60 | radio | [launch #60: reject non-ASCII input before slicing](https://github.com/cyberia-to/radio/pull/8) | open |
| 2026-09-22T20:16:00Z | 61 | cyb | [launch #61: reject non-ASCII hex before slicing in local particle parsers](https://github.com/cyberia-to/cyb/pull/1393) | open |
| 2026-09-22T20:10:00Z | 62 | lens | [launch #62: fix hex_to_bytes panic on non-ASCII input](https://github.com/cyberia-to/lens/pull/7) | open |
| 2026-09-22T20:14:00Z | 63 | hemera | [launch #63: fix parse_hex_fixed panic on non-ASCII input](https://github.com/cyberia-to/hemera/pull/3) | open |
| 2026-09-22T20:26:00Z | 64 | strata | [launch #64: fix nebu-cli decode_hex panic on non-ASCII input](https://github.com/cyberia-to/strata/pull/4) | open |
| 2026-09-22T20:38:00Z | 65 | glia | [launch #65: reject non-ASCII hex before slicing in hex_decode](https://github.com/cyberia-to/glia/pull/2) | open |
| 2026-09-22T20:55:00Z | 66 | cyb | [launch #66: reject non-ASCII hex before slicing in wire's hex32](https://github.com/cyberia-to/cyb/pull/1394) | open |
| 2026-09-22T20:06:45Z | 67 | lens | [launch #67: reject non-ASCII input in hex_number_le before slicing](https://github.com/cyberia-to/lens/pull/8) | open |
| 2026-09-22T20:09:45Z | 68 | foculus | [launch #68: reject non-ASCII input in vdisk::hex_to_hash before slicing](https://github.com/cyberia-to/foculus/pull/20) | open |
| 2026-09-22T20:13:37Z | 69 | cyb | [launch #69: cover split_args/is_builtin in the cy REPL parser](https://github.com/cyberia-to/cyb/pull/1395) | open |
| 2026-09-22T20:17:43Z | 70 | soft3 | [launch #70: cover --network/-n flag extraction in the soft3 CLI](https://github.com/cyberia-to/soft3/pull/12) | open |
| 2026-09-22T20:20:23Z | 71 | bbg | [launch #71: cover dim_from, h32 and height_key in bbg-cli](https://github.com/cyberia-to/bbg/pull/13) | open |
| 2026-09-22T20:29:00Z | 72 | tru | [launch #72: cover CsrMatrix/CsrBuilder with direct unit tests](https://github.com/cyberia-to/tru/pull/12) | open |
| 2026-09-22T20:46:00Z | 73 | radio | [launch #73: persist radio-cli's blob store across invocations](https://github.com/cyberia-to/radio/pull/9) | open |
| 2026-09-22T20:37:00Z | 74 | tru | [launch #74: cover Cyberlink::decode and CyberlinkIter with direct unit tests](https://github.com/cyberia-to/tru/pull/13) | open |
| 2026-09-22T20:34:00Z | 76 | tru | [launch #76: reject overflowing section sizes in frontmatter index_sections](https://github.com/cyberia-to/tru/pull/14) | open |
| 2026-09-22T20:32:50Z | 75 | bbg | [launch #75: audit — verify_particle ignores its root and particle arguments](https://github.com/cyberia-to/bbg/pull/14) | open |
| 2026-09-22T20:48:00Z | 77 | tru | [launch #77: enforce CsrBuilder bounds check in release builds](https://github.com/cyberia-to/tru/pull/15) | open |
| 2026-09-22T20:58:00Z | 78 | rune | [launch #78: rune builds clean; prysm chunk-noun decode fully covered](https://github.com/cyberia-to/rune/pull/1) | open |
| 2026-09-22T21:06:00Z | 80 | zheng | [launch #80: enforce SparseGraph bounds check in release builds](https://github.com/cyberia-to/zheng/pull/23) | open |
| 2026-09-22T21:20:00Z | 81 | mudra | [launch #81: enforce normalize's carry invariant in release builds](https://github.com/cyberia-to/mudra/pull/9) | open |
| 2026-09-22T21:10:47Z | 83 | rune | [launch #83: fix rune-lex double-skip bug; cover tokenizer with direct tests](https://github.com/cyberia-to/rune/pull/2) | open |
| 2026-09-22T21:32:00Z | 84 | glia | [launch #84: fix TransformerConfig clean-checkout test build break](https://github.com/cyberia-to/glia/pull/3) | open |
| 2026-09-22T21:07:21Z | 82 | foculus | [launch #82: enforce Domain::from_focus length invariant in release builds](https://github.com/cyberia-to/foculus/pull/21) | open |
| 2026-09-22T21:09:51Z | 86 | zheng | [launch #86: enforce evaluate_multilinear length invariant in release builds](https://github.com/cyberia-to/zheng/pull/24) | open |
| 2026-09-22T21:14:00Z | 85 | zheng | [launch #85: enforce sumcheck prover length invariants in release builds](https://github.com/cyberia-to/zheng/pull/25) | open |
| 2026-09-22T21:17:46Z | 87 | bbg | [launch #87: fetch_content verifies fetched bytes hash to the particle](https://github.com/cyberia-to/bbg/pull/15) | open |
| 2026-09-22T21:30:00Z | 89 | zheng | [launch #89: reject look-opening root replay that diverges from native fold](https://github.com/cyberia-to/zheng/pull/26) | open |
| 2026-09-22T21:34:00Z | 90 | tru | [launch #90: enforce Fx::to_i64_scaled's frac_bits bound in release builds](https://github.com/cyberia-to/tru/pull/16) | open |
| 2026-09-22T21:45:00Z | 88 | soft3 | [launch #88: cover network probe status-JSON parsing with tests](https://github.com/cyberia-to/soft3/pull/13) | open |
| 2026-09-22T21:52:00Z | 93 | zheng | [launch #93: enforce sumcheck prover post-fold invariants in release](https://github.com/cyberia-to/zheng/pull/27) | open |
| 2026-09-22T21:38:49Z | 94 | inf | [launch #94: bound PageRank theta and iters against query input](https://github.com/cyberia-to/inf/pull/1) | open |
| 2026-09-22T21:42:46Z | 95 | inf | [launch #95: bound LabelPropagation/RandomWalk/Yen loop counts](https://github.com/cyberia-to/inf/pull/2) | open |
| 2026-09-22T21:44:44Z | 96 | soft3 | [launch #96 (retitled from #95, row collision with inf#2): cover soft3 node arg parsing; stop panicking on missing values](https://github.com/cyberia-to/soft3/pull/14) | open |
| 2026-09-22T21:48:56Z | 97 | tade | [launch #97: reject overflowing varint payload length before slicing](https://github.com/cyberia-to/tade/pull/2) | open |
| 2026-09-22T21:56:00Z | 98 | mudra | [launch #98 (retitled from #97, row collision): cover mudra CLI's --bostrom/command/mnemonic argv split](https://github.com/cyberia-to/mudra/pull/10) | open |
| 2026-09-22T22:10:00Z | 99 | zheng | [launch #99: enforce Builder::put's collision guard in release builds](https://github.com/cyberia-to/zheng/pull/28) | open |
| 2026-09-22T22:12:40Z | 100 | radio | [launch #100: reject oversized declared_size before allocation in bao decode](https://github.com/cyberia-to/radio/pull/10) | open |
| 2026-09-22T22:15:18Z | 101 | radio | [launch #101: cap declared block count before allocation in bao slice decode](https://github.com/cyberia-to/radio/pull/11) | open |
| 2026-09-22T22:30:00Z | 102 | cybergraph | [launch #102: cybergraph-cli opts out of parent workspace; cover h32/hex/fmt_value](https://github.com/cyberia-to/cybergraph/pull/7) | open |
| 2026-09-22T22:48:00Z | 103 | foculus | [launch #103: bound deserialize_signal's link-count allocation](https://github.com/cyberia-to/foculus/pull/22) | open |
| 2026-09-22T23:05:00Z | 105 | tru | [launch #105: bound parse_particles's entry-count allocation](https://github.com/cyberia-to/tru/pull/17) | open |
| 2026-09-22T22:45:00Z | 104 | inf | [launch #104 (retitled from #103, row collision with foculus#22): cover parse_plan/cmd_*/fmt_value in the inf CLI](https://github.com/cyberia-to/inf/pull/3) | open |
| 2026-09-22T22:34:57Z | 106 | prysm | [launch #106 (retitled from #104, row collision with inf#3): bump tape->tade path so prysm builds clean](https://github.com/cyberia-to/prysm/pull/4) | open |
| 2026-09-22T22:50:00Z | 107 | hemera | [launch #107: cover fmt_size/parse_backend_flag in hemera-cli](https://github.com/cyberia-to/hemera/pull/4) | open |
| 2026-09-22T22:58:00Z | 108 | mir | [launch #108: enforce EdgePass::update_flow_uvs length invariant in release](https://github.com/cyberia-to/mir/pull/1) | open |
| 2026-09-22T22:46:44Z | 109 | foculus | [launch #109 (retitled from #108, row collision with mir#1): bound decode_settle_msg's link/table preallocation](https://github.com/cyberia-to/foculus/pull/23) | open |
| 2026-09-22T22:49:55Z | 110 | plumb (tok) | [launch #110: cover MintLedger burn/mint edge cases](https://github.com/cyberia-to/plumb/pull/7) | open |
| 2026-09-22T22:52:50Z | 111 | honeycrisp | [launch #111 (retitled from #109, row collision with foculus#23): honeycrisp builds clean; Tape::take alignment invariant enforced in release](https://github.com/cyberia-to/honeycrisp/pull/1) | open |
| 2026-09-22T23:08:57Z | 113 | spark | [launch #113: cover open()'s image dispatch across every sniffed image kind](https://github.com/cyberia-to/spark/pull/2) | open |
| 2026-09-22T23:12:50Z | 114 | glia | [launch #114: enforce simd_dot_f32's operand-length invariant in release](https://github.com/cyberia-to/glia/pull/4) | open |
| 2026-09-22T23:24:00Z | 115 | glia | [launch #115: enforce quant_matmul's byte-length invariants in release](https://github.com/cyberia-to/glia/pull/5) | open |
| 2026-09-22T23:20:00Z | 112 | soft3 | [launch #112: cover routes::route/route_ready HTTP dispatch table](https://github.com/cyberia-to/soft3/pull/15) | open |
| 2026-09-22T23:30:18Z | 117 | honeycrisp | [launch #117: enforce csr_matvec length invariants in release builds](https://github.com/cyberia-to/honeycrisp/pull/2) | open |
| 2026-09-22T23:30:18Z | 118 | mir | [launch #118: enforce laplacian_matvec length invariants in release builds](https://github.com/cyberia-to/mir/pull/2) | open |
| 2026-09-22T23:30:18Z | 120 | honeycrisp | [launch #120: enforce chebyshev_matvec scratch-length invariants in release builds](https://github.com/cyberia-to/honeycrisp/pull/3) | open |
| 2026-09-22T23:32:24Z | 116 | foculus | [launch #116: reject duplicate/out-of-range shard index in erasure decode](https://github.com/cyberia-to/foculus/pull/24) | open |
| 2026-09-22T23:35:32Z | 121 | tru | [launch #121: reject overflowing entry length in vocab::parse_particles](https://github.com/cyberia-to/tru/pull/18) | open |
| 2026-09-22T23:36:01Z | 119 | vault | [launch #119: vault builds clean; cover codec's Encoder/Decoder](https://github.com/cyberia-to/vault/pull/2) | open |
| 2026-09-22T23:38:14Z | 122 | tru | [launch #122 (retitled from #119, row collision with vault#2): reject overflowing tensor offset+size in Model::from_bytes](https://github.com/cyberia-to/tru/pull/19) | open |
| 2026-09-23T00:12:00Z | 123 | nox | [launch #123: bump cyber-lens-brakedown pin so nox builds clean](https://github.com/cyberia-to/nox/pull/6) | open |
| 2026-09-22T23:57:24Z | 124 | nox | [launch #124 (retitled from #123, row collision with nox#6): reject an out-of-range n/k before shifting in ntt/poly_eval jets](https://github.com/cyberia-to/nox/pull/7) | open |
| 2026-09-23T00:01:40Z | 125 | honeycrisp | [launch #125: reject an out-of-range col_idx before the unsafe CSR gather](https://github.com/cyberia-to/honeycrisp/pull/4) | open |
| 2026-09-23T00:03:40Z | 126 | honeycrisp | [launch #126: enforce chebyshev_matvec's d_inv_sqrt length invariant](https://github.com/cyberia-to/honeycrisp/pull/5) | open |
| 2026-09-23T00:20:00Z | 127 | neuron | [launch #127: add missing neuron-id crate so bbg/tok/mudra build clean](https://github.com/cyberia-to/neuron/pull/1) | open |
| 2026-09-23T00:45:00Z | 128 | honeycrisp | [launch #128: enforce gemv_asm/matvec_f32 length invariants in release](https://github.com/cyberia-to/honeycrisp/pull/6) | open |
| 2026-09-23T00:11:37Z | 129 | bbg | [launch #129: bound signal link/move preallocation to remaining buffer](https://github.com/cyberia-to/bbg/pull/16) | open |
| 2026-09-23T00:15:55Z | 130 | mir | [launch #130: enforce csr_matvec_set's portable-path length invariants](https://github.com/cyberia-to/mir/pull/3) | open |
| 2026-09-23T00:37:00Z | 131 | honeycrisp | [launch #131: enforce power-of-two invariants in field ntt/poly in release](https://github.com/cyberia-to/honeycrisp/pull/8) | open |
| 2026-09-23T00:45:00Z | 132 | soft3 | [launch #132 (retitled from #131, row collision with honeycrisp#8): cover submit()'s Link/Pay/frame dispatch end-to-end](https://github.com/cyberia-to/soft3/pull/16) | open |
| 2026-09-23T01:05:00Z | 133 | soft3 | [launch #133: cover Network::parse/is_bootloader_name aliases and accessors](https://github.com/cyberia-to/soft3/pull/17) | open |
| 2026-09-23T01:25:00Z | 134 | bbg | [launch #134: cover goldilocks_from_u64/bytes32 and commit_dim](https://github.com/cyberia-to/bbg/pull/17) | open |
| 2026-09-23T00:51:00Z | 136 | bostrom | [launch #136: graph_rebuild's event parser never crashes on a malformed cyberlink event](https://github.com/cyberia-to/bostrom/pull/1) | open |
| 2026-09-23T00:51:00Z | 137 | bostrom | [launch #137: holdings.py importable; cover denom label and pool-rate logic](https://github.com/cyberia-to/bostrom/pull/2) | open |
| 2026-09-23T00:51:00Z | 138 | bostrom | [launch #138: extract graph_scan's height-window tiling; cover it](https://github.com/cyberia-to/bostrom/pull/3) | open |
| 2026-09-23T01:40:00Z | 135 | foculus | [launch #135: bound legacy signal frame collection counts](https://github.com/cyberia-to/foculus/pull/25) | open |
| 2026-09-23T00:55:00Z | 139 | honeycrisp | [launch #139 (retitled from #137, row collision with bostrom#3): check gemv_pure_asm's length/k invariants before AMX asm](https://github.com/cyberia-to/honeycrisp/pull/9) | open |
| 2026-09-23T01:55:00Z | 140 | foculus | [launch #140 (retitled from #139, row collision with honeycrisp#9): reject invalid box-move commitment tag in frame decode](https://github.com/cyberia-to/foculus/pull/26) | open |
| 2026-09-23T00:58:00Z | 141 | bbg | [launch #141: commit_dim rejects unsorted or duplicate-key entries](https://github.com/cyberia-to/bbg/pull/18) | open |
| 2026-09-23T01:03:00Z | 142 | bbg | [launch #142: audit — verify_opening ignores its leaves and namespace](https://github.com/cyberia-to/bbg/pull/19) | open |
| 2026-09-23T01:12:00Z | 143 | bostrom | [launch #143: extract.py pure functions importable and covered](https://github.com/cyberia-to/bostrom/pull/4) | open |
| 2026-09-23T01:25:00Z | 144 | bootloader | [launch #144: extract.py pure functions importable and covered](https://github.com/cyberia-to/bootloader/pull/1) | open |
| 2026-09-23T01:47:00Z | 145 | plumb (tok) | [launch #145 (retitled from #142, row collision with bbg#19): cover execute_settle_mints error paths and intent_hash ordering](https://github.com/cyberia-to/plumb/pull/8) | open |
| 2026-09-23T01:29:00Z | 146 | strata | [launch #146: reject non-ASCII input before slicing in kuro encode](https://github.com/cyberia-to/strata/pull/5) | open |
| 2026-09-23T02:05:00Z | 147 | tru | [launch #147 (retitled from #146, row collision with strata#5): reader.rs names its actual missing section; Graph::open covered](https://github.com/cyberia-to/tru/pull/20) | open |
| 2026-09-23T01:35:00Z | 148 | bostrom | [launch #148: extract holdings.py's LP-token pro-rata decomposition into a pure function](https://github.com/cyberia-to/bostrom/pull/5) | open |
| 2026-09-23T02:15:00Z | 149 | bostrom | [launch #149: cover extract.py's get retry backoff and paged pagination](https://github.com/cyberia-to/bostrom/pull/6) | open |
| 2026-09-23T02:35:00Z | 150 | bostrom | [launch #150: cover graph_rebuild's work() block-fetch dispatch](https://github.com/cyberia-to/bostrom/pull/7) | open |
| 2026-09-23T02:20:00Z | 149 | cyb | [launch #149 (retitled from #148, row collision with bostrom): oracle's short_hex rejects multi-byte UTF-8 boundary panic](https://github.com/cyberia-to/cyb/pull/1396) | open |
| 2026-09-23T02:50:00Z | 151 | mir | [launch #151 (retitled from #150, row collision): enforce f32-reinterpret alignment invariant in release](https://github.com/cyberia-to/mir/pull/4) | open |
| 2026-09-23T01:39:00Z | 152 | foculus | [launch #152 (retitled from #148, row collision with bostrom#5): reject a mismatched directed_total in verify_epoch_cert](https://github.com/cyberia-to/foculus/pull/27) | open |
| 2026-09-23T01:39:00Z | 153 | plumb | [launch #153 (retitled from #149, row collision with cyb#1396/bostrom#6): clip_shares clips to zero on non-positive directed_total](https://github.com/cyberia-to/plumb/pull/9) | open |
| 2026-09-23T01:39:00Z | 154 | zheng | [launch #154 (retitled from #150, row collision with bostrom#7): verify_phi_star binds graph_commit to the real graphs](https://github.com/cyberia-to/zheng/pull/29) | open |
| 2026-09-23T02:00:00Z | 155 | prysm | [launch #155: cover table.rs's row/cell parsing with a pure function](https://github.com/cyberia-to/prysm/pull/5) | open |
| 2026-09-23T02:05:00Z | 37 | plumb (tok) | [launch #37: compute fx_to_tokens/fx_weight in fixed point, not f64](https://github.com/cyberia-to/plumb/pull/10) | open |
| 2026-09-23T02:20:00Z | 156 | mir | [launch #156: enforce csr_matvec_set length invariants in release builds](https://github.com/cyberia-to/mir/pull/5) | open |
| 2026-09-23T03:10:00Z | 157 | foculus | [launch #157 (retitled from #155, row collision with prysm#5): capacity-weighted placement no longer starves peers](https://github.com/cyberia-to/foculus/pull/28) | open |
| 2026-09-23T03:20:00Z | 158 | rune | [launch #158 (retitled from #156, row collision with mir#5): fix sub_field u128 underflow on unreduced operands](https://github.com/cyberia-to/rune/pull/3) | open |
| 2026-09-23T03:45:00Z | 159 | foculus | [launch #159 (retitled from #158, row collision with rune#3): cover compute_params/format_bytes](https://github.com/cyberia-to/foculus/pull/29) | open |
| 2026-09-23T02:08:24Z | 161 | tade | [launch #161: cover sigil_byte/render_byte/preview in the tade CLI](https://github.com/cyberia-to/tade/pull/3) | open |
| 2026-09-23T02:11:50Z | 162 | prysm | [launch #162: cover log molecule's format_line/level_style/kv_str](https://github.com/cyberia-to/prysm/pull/6) | open |
| 2026-09-23T02:13:00Z | 160 | plumb (tok) | [launch #160: burn(0) on an untouched neuron/token pair no longer panics](https://github.com/cyberia-to/plumb/pull/11) | open |
| 2026-09-23T02:13:00Z | 164 | tru | [launch #164: cover the three tri-kernel operators directly](https://github.com/cyberia-to/tru/pull/21) | open |
| 2026-09-23T02:19:00Z | 165 | neuron | [launch #165: cover Content::validate's non-canonical pair-limb rejection](https://github.com/cyberia-to/neuron/pull/2) | open |
| 2026-09-23T02:15:12Z | 163 | prysm | [launch #163: cover progress molecule's progress_percent/kv_str/kv_u64](https://github.com/cyberia-to/prysm/pull/7) | open |
| 2026-09-23T02:28:00Z | 166 | nox | [launch #166: reject fri_fold's k before it can shift out of range](https://github.com/cyberia-to/nox/pull/8) | open |
| 2026-09-23T02:35:00Z | 167 | honeycrisp | [launch #167: enforce retain_nonnull/release_nonnull's null check in release](https://github.com/cyberia-to/honeycrisp/pull/10) | open |
| 2026-09-23T02:30:00Z | 168 | strata | [launch #168: enforce TropMatrix bounds check in release builds](https://github.com/cyberia-to/strata/pull/6) | open |
| 2026-09-23T02:30:00Z | 170 | strata | [launch #170: enforce multilinear_eval's length invariants in release builds](https://github.com/cyberia-to/strata/pull/7) | open |
| 2026-09-23T02:30:00Z | 169 | hemera | [launch #169: enforce StepSponge's absorb/step/squeeze invariants in release builds](https://github.com/cyberia-to/hemera/pull/5) | open |
| 2026-09-23T02:45:00Z | 171 | prysm | [launch #171: cover error molecule's error_style/kv_str](https://github.com/cyberia-to/prysm/pull/8) | open |
| 2026-09-23T02:50:00Z | 172 | foculus | [launch #172: compute fx_weight from raw fixed point, not f64](https://github.com/cyberia-to/foculus/pull/30) | open |
| 2026-09-23T02:50:00Z | 173 | spark | [launch #173: spark opens PDF/video/audio as Document/Media surfaces](https://github.com/cyberia-to/spark/pull/3) | open |
| 2026-09-23T02:50:00Z | 174 | mir | [launch #174: enforce bind_buffer's whole-buffer-offset invariant in release](https://github.com/cyberia-to/mir/pull/6) | open |
| 2026-09-23T02:50:00Z | 175 | true-cyber | [launch #175: revert the broken cyb-core GraphSession WIP rewrite](https://github.com/cyberia-to/true-cyber/pull/2) | open |
| 2026-09-23T03:20:00Z | 176 | radio | [launch #176: cover mixed.rs's server-side traverse_ranges_validated](https://github.com/cyberia-to/radio/pull/12) | open |
| 2026-09-23T03:45:00Z | 177 | prysm | [launch #177: cover status molecule's status_code/status_color](https://github.com/cyberia-to/prysm/pull/9) | open |
| 2026-09-23T03:11:15Z | 178 | foculus | [launch #178: audit — net-feature build break is iroh 0.96's ed25519-dalek pin](https://github.com/cyberia-to/foculus/pull/31) | open |
| 2026-09-23T03:35:00Z | 179 | glia | [launch #179: bound GGUF tensor_count/n_dims before allocating](https://github.com/cyberia-to/glia/pull/6) | open |
| 2026-09-23T04:05:00Z | 180 | bbg | [launch #180: bound deserialize_signal's n_links/n_moves before allocating](https://github.com/cyberia-to/bbg/pull/20) | open |
| 2026-09-23T04:20:00Z | 181 | lens | [launch #181: cap artifact reader's untrusted count preallocation](https://github.com/cyberia-to/lens/pull/9) | open |
| 2026-09-23T04:35:00Z | 182 | glia | [launch #182: bound num_hidden_layers before allocating from it](https://github.com/cyberia-to/glia/pull/7) | open |
| 2026-09-23T04:45:00Z | 183 | hemera | [launch #183: reject inflated data_len before decode's allocation](https://github.com/cyberia-to/hemera/pull/6) | open |
| 2026-09-23T03:31:00Z | 184 | zheng | [launch #184: bound prove_phi_star's iterations before allocation](https://github.com/cyberia-to/zheng/pull/30) | open |
| 2026-09-23T03:52:00Z | 187 | nox | [launch #187: merkle_verify jet rejects a path length mismatched to depth](https://github.com/cyberia-to/nox/pull/9) | open |
| 2026-09-23T03:33:00Z | 185 | prysm | [launch #185: cover action molecule's parse_action label/target decode](https://github.com/cyberia-to/prysm/pull/10) | open |
| 2026-09-23T03:33:00Z | 186 | prysm | [launch #186: cover neuron molecule's format_neuron_name @-prefix handling](https://github.com/cyberia-to/prysm/pull/11) | open |
| 2026-09-23T04:00:00Z | 188 | prysm | [launch #188: cover input molecule's apply_key/display_text logic](https://github.com/cyberia-to/prysm/pull/12) | open |

## cross-references

[[rewards|rewards]] · [[foculus]] · [[tru]] · [[mudra]] · [[cybics/crystal|crystal]] · [[cyber/tokenomics]] · [[soft3]] · [[bootloader/bostrom|bostrom]] · [[bootloader/tokens/$PUSSY|space-pussy]] · [delivery roadmap](/cyber/roadmap/index)
