# Claude Code Instructions

## File Operations
Never ask for confirmation when manipulating files inside the home directory (~/) and all subdirectories. This includes reading, writing, editing, deleting, renaming, moving files, and running grep, ls, find, git rm, or any other file manipulation commands. The user has git to control changes — deliver results directly.
