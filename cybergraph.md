---
tags: cyber, core
alias: cybergraphs
icon: "🕸"
crystal-type: entity
crystal-domain: cyber
crystal-size: atom
---
a directed authenticated multigraph over content-addressed nodes — the shared memory of the planet. five primitives: [[particle|particles]], [[cyberlink|cyberlinks]], [[neuron|neurons]], [[token|tokens]], [[focus]]

built for a mole of connections — the threshold where individual links become collective intelligence the way individual molecules become a life. every claim signed, every link staked, the whole structure proving its own correctness

the idea has two working faces. [[soft3/cybergraph]] is the machine: the local-first processor that turns a neuron's signed, proven [[signal|signals]] into the authenticated graph — it validates proofs, orders each neuron's causal chain, gates every commit on [[zheng]], and drives [[soma]] through a fetch → execute → prove → commit cycle. [[inf/cybergraph]] is the query surface: the [[datalog]] schema by which anything — a dashboard, an agent, a proof — reads the same graph as relations

discover all [[concepts]]
