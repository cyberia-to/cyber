---
name: ctx
repo: ../ctx
subgraph: true
visibility: private
archived: false
---

# ctx

A 64k-token knowledge graph context document distilled from the full [[cyber]] stack, optimized for Claude Sonnet. Builds from the `cyber` repo as a sibling and is used as a system prompt for AI sessions working across the stack. Provides a compact, semantically dense window into the entire [[cybergraph]] for LLM-assisted development.
