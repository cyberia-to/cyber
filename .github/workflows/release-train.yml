name: Release train
on:
  schedule:
    - cron: '0 12 * * 5'
  workflow_dispatch:
    inputs:
      candidate:
        description: 'candidate-YYYYMMDD.N (empty selects today .1)'
        default: ''
        type: string
      cut:
        description: 'Create a draft candidate after recording all gates'
        default: true
        type: boolean
permissions:
  contents: write
concurrency:
  group: release-train
  cancel-in-progress: false
jobs:
  candidate:
    uses: cyberia-to/soft3/.github/workflows/candidate.yml@79923c925c24d79be2e08e3720a18ee6d9a729de
    with:
      component: cyber
      manager_ref: 79923c925c24d79be2e08e3720a18ee6d9a729de
      candidate: ${{ inputs.candidate || '' }}
      cut: ${{ github.event_name == 'schedule' || inputs.cut == true }}
